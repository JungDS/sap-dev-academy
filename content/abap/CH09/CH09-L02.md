---
id: CH09-L02
title: "Value Table과 Foreign Key의 차이"
direction: Domain의 Value Table은 '제안', Foreign Key는 '실제 관계'.
keywords: [Value Table, Foreign Key, Domain, Check Table, 변환루틴]
order: 2
introduces: ["Value Table↔Foreign Key 차이(검증 강제 아님)", "Domain 변환 루틴(ALPHA)"]
prereq: ["CH09-L01", "CH03"]
prevRel: parallel
---

## 비슷해 보이는 둘

[[Domain]]을 만들 때 **Value Table**을 지정할 수 있고, 테이블 필드엔
[[Foreign Key]]를 건다. 둘 다 "어떤 테이블의 값으로 제한"하는 것 같은데 뭐가
다른가? 헷갈리기 쉬운 지점이라 분명히 짚는다.

## Value Table = Domain의 '제안'

- [[Value Table]]은 **Domain 수준**에서 "이 도메인 값은 보통 이 테이블에서 온다"고
  **제안**하는 것이다.
- 그 자체로는 검증을 **강제하지 않는다.** 어디까지나 기본 제안.

## Foreign Key = 테이블 필드의 '실제 관계'

- [[Foreign Key]]는 **테이블 필드 수준**에서 거는 **실제 관계**다.
- 이게 있어야 입력검증·F4가 **실제로 동작**한다.
- Foreign Key를 만들 때, Domain에 Value Table이 있으면 시스템이 그걸
  **Check Table로 자동 제안**한다 → 둘은 협력 관계.

| | Value Table | Foreign Key |
|---|---|---|
| 정의 위치 | Domain | 테이블 필드 |
| 역할 | 제안(기본값) | 실제 관계·검증 |
| 검증 강제 | ✗ | ✓ |

## 보너스 — Domain의 변환 루틴(ALPHA)

[[Domain]]에는 값의 형식을 자동으로 바꿔 주는 **변환 루틴(Conversion Routine)** 도
달 수 있다. 대표가 `ALPHA`다. 사번·고객번호처럼 **숫자로만 된 ID**를,
**내부 저장은 앞자리 0을 채우고**(`0000000042`) **화면 표시는 0을 떼서**(`42`)
보여 주는 식으로 오간다.

여기서 "숫자로만"이 중요하다. `ALPHA`는 값이 **처음부터 끝까지 숫자일 때만** 0을
채운다. 글자가 하나라도 섞이면 아무것도 하지 않고 값을 그대로 둔다. 그래서 우리
공연 ID `C001`은 앞에 `C`가 붙어 있어 `ALPHA`의 대상이 **아니다.** 이 앱에서는
쓸 일이 없지만, 표준 테이블을 조회할 땐 만나게 되니 알아 두자.

- 같은 ID라도 "내부 형식"과 "표시 형식"이 다를 수 있다는 점을 기억해 둔다.
- 코드에서 조회·비교 시 형식이 안 맞아 "없음"이 나오면 변환 루틴(`ALPHA`) 때문일 수
  있다. 형식을 맞춰 주는 **표준 함수**가 따로 있는데, 함수를 부르는 법 자체를 아직
  안 배웠다. 지금은 **"조회가 헛돌면 형식부터 의심한다"** 만 챙기면 된다(함수 호출은
  [Chapter 10 · CALL FUNCTION 기본 구조](CH10-L03.html)에서).

## 직접 해 보기

두 스위치(Value Table·Foreign Key)를 조합해 보자. 없는 값 `C999`를 넣었을 때 그냥 통과하는지, 아니면 거부되는지 직접 확인할 수 있다. ALPHA 변환도 함께 만져 볼 수 있다.

::embed CH09-L02-S01 | 제안 vs 검증 스위치보드 — Value Table·Foreign Key·ALPHA::

## 흔한 실수와 점검

- **Value Table만 믿기** → 그것만으론 검증 안 된다. 필드에 Foreign Key를 걸어야.
- **Check Table ≠ Value Table** → 보통 같게 쓰지만, Foreign Key에서 다른 Check
  Table을 지정할 수도 있다.

---

값의 **존재**는 관계로 제한했다. 그런데 코드만 든 마스터에 사람이 읽을 **이름**을
붙이려면? → 다음 레슨(Text Table).
