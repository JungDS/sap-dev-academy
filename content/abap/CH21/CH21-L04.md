---
id: CH21-L04
title: "Static Method와 Instance Method"
direction: 클래스로 직접(=>) vs 객체로(->) — 객체 상태가 필요한가로 가른다.
keywords: [Static, Instance, "=>", "->", me]
order: 4
introduces: ["Static vs Instance 멤버", "me->"]
prereq: ["CH21-L02"]
prevRel: deepening
---

## 객체가 필요한가, 아닌가 ⚖️

모든 메서드에 객체가 필요한 건 아니다. 공연 ID를 대문자로 정리하는 일은 **특정 예약 객체의 상태 없이도** 가능하다. 반대로 잔여석 계산은 *그 공연·회차*를 알아야 하니 객체 상태가 필요하다.

- **인스턴스 멤버**(`METHODS`·`DATA`) — 객체마다 따로. 객체로 `->` 호출.
- **정적 멤버**(`CLASS-METHODS`·`CLASS-DATA`) — 클래스 공용. 객체 없이 `=>` 호출.

```abap
" 인스턴스 — 객체가 필요
DATA(lo_mgr) = NEW zcl_booking_manager( iv_concert = 'C001' iv_perf = '01' ).
DATA(lv_remaining) = lo_mgr->remaining( ).

" 정적 — 클래스로 직접(객체 없이)
DATA(lv_concert) = zcl_booking_manager=>normalize_concert_id( 'c001' ).
```

::embed CH21-L04-S01 | Arrow Selector Trainer (=> vs -> 호출 조립)::

## me-> — 메서드 안에서 자기 자신 🪞

메서드 안에서 자기 객체의 다른 멤버를 가리킬 때 `me->`를 쓴다. 그 객체의 상태(`mv_concert` 등)로 잔여석을 계산하는 인스턴스 메서드는 이렇게 생겼다.

```abap
METHOD remaining.
  SELECT COALESCE( SUM( seats ), 0 )
    FROM zbooking
    WHERE concert_id = @mv_concert
      AND perf_no    = @mv_perf
      AND status    <> 'C'
    INTO @DATA(lv_booked).

  rv_remaining = mv_cap - lv_booked.
ENDMETHOD.
```

`me->`는 대부분 생략할 수 있지만, 지역 변수·파라미터와 이름이 비슷할 때 `me->mv_cap`처럼 쓰면 의도가 분명해진다.

## 언제 정적, 언제 인스턴스 🧭

- 객체 상태가 **필요 없는** 유틸리티(포맷·계산) → **정적**.
- 객체별 상태(잔여석 등)를 다루면 → **인스턴스**.

> 🧷 `=>`와 `->`를 기계적으로 외우지 말고 **왼쪽에 무엇이 오는지** 보자. 클래스명이 왼쪽이면 `=>`, 참조 변수가 왼쪽이면 `->`다.

## 흔한 실수와 점검 🚧

- **객체 상태를 읽는 메서드를 정적으로** → 잔여석은 공연·회차마다 다르니 인스턴스여야 한다.
- **단순 helper를 전부 인스턴스로** → 객체 상태가 필요 없는 포맷 변환은 정적이 더 자연스럽다(괜히 객체를 만들지 않는다).
- **정적 메서드에서 인스턴스 멤버** → 정적 메서드 안엔 자기 객체(`me`)가 없다. 인스턴스 멤버를 못 쓴다.
- **`CLASS-DATA`를 전역 변수처럼** → 여러 객체가 공유하므로, 한 객체의 변경이 다른 객체에도 영향을 줄 수 있다.

---

호출 방식을 정리했다. 이제 종류가 달라도 **같은 규약**을 따르게 하는 Interface →
[Interface 기본 설계](CH21-L05.html).
