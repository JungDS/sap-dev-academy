---
id: CH21-L03
title: "Grid ALV Column 제어 심화"
direction: Field Catalog로 컬럼을 숨기고·합계·정렬·편집 표시.
keywords: [Field Catalog, Column, hide, sum, edit]
order: 3
introduces: ["fieldcat 컬럼 속성 심화"]
prereq: ["CH17", "CH20"]
prevRel: deepening
---

## 컬럼을 세밀하게

CH17의 [[Field Catalog]]를 다시 꺼내, 컬럼별 속성을 더 세밀하게 준다.

```abap
LOOP AT lt_fcat REFERENCE INTO DATA(lr_fc).
  CASE lr_fc->fieldname.
    WHEN 'MANDT'.   lr_fc->no_out   = abap_true.   " 숨김
    WHEN 'SEATS'.   lr_fc->do_sum   = abap_true.   " 합계
    WHEN 'STATUS'.  lr_fc->just     = 'C'.         " 가운데 정렬
  ENDCASE.
ENDLOOP.
```

- `no_out` 숨김 · `do_sum` 합계 · `just`(L/C/R) 정렬 · `key` 키컬럼 고정.
- `edit = abap_true`로 편집 가능 표시도 가능(본격 편집은 CH28).

## 흔한 실수와 점검

- **합계 불가 타입** → 문자 컬럼엔 `do_sum`이 안 먹는다(숫자만).
- **숨김 vs 미포함** → `no_out`은 카탈로그엔 있고 화면에만 숨김(다시 표시 가능).

---

컬럼을 제어했다. 이제 **셀 하나하나에 색**을 입혀 매진을 강조하자 → 다음 레슨.
