---
id: CH25-L05
title: "Lock Object와 COMMIT/ROLLBACK 연결"
direction: 잠금–읽기–변경–커밋–해제의 한 흐름.
keywords: [Lock, COMMIT, ROLLBACK, 패턴]
order: 5
introduces: ["잠금-변경-커밋-해제 통합 패턴"]
prereq: ["CH25-L02", "CH24-L02"]
prevRel: deepening
---

## 안전한 변경의 정석

CH24(DML·COMMIT)와 CH25(잠금)를 묶으면 안전한 변경 흐름이 완성된다.

```abap
" 1) 잠금
CALL FUNCTION 'ENQUEUE_EZ_BOOKING'
  EXPORTING booking_id = lv_id
  EXCEPTIONS foreign_lock = 1 OTHERS = 2.
IF sy-subrc <> 0.
  MESSAGE '편집 중입니다' TYPE 'E'.
ENDIF.

" 2) 읽기 → 3) 변경
SELECT SINGLE * FROM zbooking WHERE booking_id = @lv_id INTO @DATA(ls).
ls-seats = lv_new_seats.
ls-changed_by = sy-uname. ls-changed_at = sy-uzeit.
UPDATE zbooking FROM @ls.

" 4) 확정 / 취소
IF sy-subrc = 0.
  COMMIT WORK.
ELSE.
  ROLLBACK WORK.
ENDIF.

" 5) 해제(보통 COMMIT/ROLLBACK이 자동 해제하나 명시도 안전)
CALL FUNCTION 'DEQUEUE_EZ_BOOKING' EXPORTING booking_id = lv_id.
```

## 흔한 실수와 점검

- **순서 어긋남** → 잠금은 변경 **전**, 해제는 커밋 **후**.
- **롤백 시 잠금** → ROLLBACK도 잠금을 해제한다(보통). 흐름을 일관되게.

---

동시성까지 안전하게 다뤘다. 이제 코드 구조를 더 정교하게 — OO 고급 설계·패턴 →
**CH26**.
