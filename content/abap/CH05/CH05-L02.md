---
id: CH05-L02
title: "DDIC Structure"
direction: 구조체 모양도 전역으로 — SE11에서 DDIC Structure로 공유한다.
keywords: [DDIC Structure, SE11, Data Element, 구조체, 중첩 구조, ".INCLUDE", ".APPEND"]
order: 2
introduces: ["DDIC Structure", "중첩 구조", ".INCLUDE / .APPEND(구조 재사용)"]
prereq: ["CH05-L01", "CH03"]
prevRel: parallel
---

## 구조도 전역으로

CH03에서 타입을 전역(Domain·Data Element)으로 올렸듯, **구조체 모양**도 전역으로
올릴 수 있다. 여러 프로그램·화면·테이블이 같은 구조를 공유하게 하는 게
[[DDIC]] Structure다.

## SE11에서 Structure 만들기

`SE11` → Data Type → **Structure** → `ZST_PERSON` → Create.

- 컴포넌트(필드)를 추가하고, 각 필드 타입은 보통 [[Data Element]]로 준다
  (라벨·도움말까지 상속).

| Component | Typing | 예 |
|---|---|---|
| NAME | Data Element | `ZDE_NAME` |
| AGE | Data Element | `ZDE_AGE` |
| AMOUNT | Data Element | `ZDE_AMOUNT` |

활성화하면 전역 구조 타입이 된다.

## 프로그램에서 쓰기

```abap
DATA ls_person TYPE zst_person.

ls_person-name = '정훈영'.
WRITE ls_person-name.
```

로컬 구조(`BEGIN OF`)와 **쓰는 법은 똑같다** — 차이는 "정의가 어디 사느냐"뿐.
전역이라 여러 곳에서 같은 모양을 공유하고, 한 곳만 고치면 전부 반영된다.

## DDIC 타입으로 쓸 수 있는 것 — Domain만 빼고

변수나 컴포넌트의 타입으로 줄 수 있는 DDIC 객체는 여럿이다. 단 **Domain은 직접 못 준다**(CH03 — Domain은 Data Element를 통해서만).

| DDIC 객체 | 변수/컴포넌트 타입으로? | 비고 |
|---|---|---|
| **Data Element** | ✅ | 가장 흔함(라벨·도움말 상속) |
| **Structure** | ✅ | 구조 안에 구조(중첩) |
| **Transparent Table** | ✅ | 그 테이블의 **행 한 줄** 모양 |
| **View** | ✅ | 뷰의 행 모양 |
| **Table Type** | ✅ | 내부 테이블 타입(→ CH06) |
| **Domain** | ❌ | 변수 타입 직접 불가 — DE 경유 |

그래서 컴포넌트에 Data Element 대신 **다른 구조**를 줄 수도 있다 — 그러면 **중첩 구조**가 된다:

```abap
DATA: BEGIN OF ls_order,
        head TYPE zst_order_head,   " 구조를 컴포넌트로(중첩)
        qty  TYPE i,
      END OF ls_order.

ls_order-head-customer = '정훈영'.   " 중첩은 -로 한 단계 더 파고든다
```

- 구조 안에 구조를 넣으면 **중첩 구조** — `ls_order-head-customer`처럼 `-`로 단계마다 들어간다.

> 💡 **심화(지금은 가볍게)** — 컴포넌트가 **전부 문자**(C·N 등)인 구조는, 통째로 한 문자 변수처럼 다룰 수도 있다(예: `year`·`month`·`day` 8자리 구조에 `sy-datum`을 통째로 대입). "그런 트릭이 있다" 정도만 알아 두자.

## 구조 재사용 — `.INCLUDE`와 `.APPEND`

같은 필드 묶음(예: 주소·감사필드)을 여러 구조에 반복해 적기는 번거롭다. DDIC는 기존 구조를 **통째로 끼워 넣어** 재사용한다.

- **`.INCLUDE`** — Structure 편집 화면에서 다른 구조를 `.INCLUDE`로 포함하면, 그 구조의 필드들이 **평평하게(직접 필드처럼)** 들어온다.
  - 중첩(`head TYPE zst_head`)은 `ls-head-customer`로 들어가지만, `.INCLUDE zst_head`는 필드가 펼쳐져 **`ls-customer`**로 바로 접근한다.
- **`.APPEND`**(Append Structure) — SAP **표준** 구조·테이블을 직접 고치지 않고, 내 필드를 **별도로 덧붙이는** 구조. 표준을 안 건드려 업그레이드에 안전하다(확장 주제 — 자세히는 CH29).

> 끼워 넣기(`.INCLUDE`)=남의 필드를 내 것처럼 **펼쳐 담기**, 덧붙이기(`.APPEND`)=표준에 **안전하게 더하기**로 기억하면 쉽다.

## 언제 로컬, 언제 DDIC?

- 그 프로그램에서만 쓰는 임시 묶음 → **로컬**(`BEGIN OF`).
- 여러 프로그램·테이블이 공유 → **DDIC Structure**.
- (참고) 다음 챕터의 투명 테이블도 사실 이 구조 위에 행이 쌓이는 형태다.

## 흔한 실수와 점검

- **필드를 표준 타입으로 직접** → 가능하지만 라벨·도움말을 잃는다. 가능하면 Data
  Element 참조.
- **이름 규칙/활성화** → `Z`/`Y` 시작, 활성화(Ctrl+F3) 안 하면 사용 불가.

---

이제 한 건을 로컬·전역 양쪽으로 묶을 수 있다. 마지막으로 묶은 구조체를 **다루는
요령**(복사·초기화)을 보고 가자 → 다음 레슨.
