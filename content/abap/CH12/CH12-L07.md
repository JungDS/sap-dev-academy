---
id: CH12-L07
title: "실습 — 공연·상태로 예매 필터"
direction: 콘서트앱 4단계 — SELECT-OPTIONS로 필요한 예매만.
keywords: [실습, 콘서트앱, SELECT-OPTIONS, 필터]
order: 7
introduces: []
prereq: ["CH12-L02", "CH11-L06"]
prevRel: next-step
advanceUse: ["REF TO", "TRY/CATCH cx_salv_msg", "START-OF-SELECTION"]
---

## 🎫 4단계 — 필요한 예매만

CH11에서 전체 목록을 봤지만, 보통 "특정 공연·특정 상태"만 필요하다. SELECT-OPTIONS
(CH12)로 거른다.

```abap
TABLES zbooking.
SELECT-OPTIONS: s_conc FOR zbooking-concert_id,
                s_stat FOR zbooking-status.

DATA: lt_book TYPE TABLE OF zbooking,
      lo_alv  TYPE REF TO cl_salv_table.

START-OF-SELECTION.
  SELECT * FROM zbooking INTO TABLE lt_book
    WHERE concert_id IN s_conc
      AND status     IN s_stat.

  TRY.
      cl_salv_table=>factory(
        IMPORTING r_salv_table = lo_alv CHANGING t_table = lt_book ).
      lo_alv->display( ).
    CATCH cx_salv_msg.
  ENDTRY.
```

::embed code-learning/select-query-simulator.html | 조건을 바꿔 결과가 어떻게 줄어드는지 보기::

## 직접 해 볼 과제

1. 공연ID 하나만 골라 그 공연 예매만 나오는지 확인.
2. 상태(STATUS)에 "예약(N)"만 Include해 본다.
3. (도전) 회차 날짜용 SELECT-OPTIONS를 `ZPERF-PERF_DATE`로 추가한다.

## 흔한 실수와 점검

- **빈 SELECT-OPTIONS** → 비우면 전체 조회(IN 조건이 모두 통과).
- **classic** → `IN s_conc`는 classic range. modern `@`는 CH19.

---

원하는 예매만 추렸다. 이제 "공연별 예매현황"을 **집계**해 보자 → **CH13(JOIN·집계)**.
