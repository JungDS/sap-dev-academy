---
id: CH12-L01
title: "Range Table 구조"
direction: 범위·다중 조건을 담는 그릇 — Range Table의 4컬럼(SIGN/OPTION/LOW/HIGH).
keywords: [Range Table, SIGN, OPTION, LOW, HIGH, CP]
order: 1
introduces: ["Range Table","SIGN","OPTION","LOW","HIGH"]
prereq: ["CH03-L03","CH06","CH08"]
foreshadow: ["SELECT-OPTIONS"]
prevRel: pain-solution
---

## 단일 값으론 부족하다

[Chapter 11 · SALV](CH11-L01.html)에서 우리는 예매 목록 전체를 표로 볼 수 있게 됐다. 그런데 실제 사용자는
**전체 목록만** 보지 않는다.

- 강호동 담당자: "공연 **C001만** 보고 싶어요."
- 운영 담당자: "**예약 상태**인 것만요."
- 회계 담당자: "C001부터 **C003까지** 보되, **취소된 건은 빼고** 주세요."

[Chapter 03 · PARAMETERS](CH03-L03.html)에서 배운 [[PARAMETERS]]는 **값 하나**를 받는 데 좋다.
하지만 "여러 값", "범위", "제외"를 한 번에 담기엔 부족하다. 이런 조건들을 **행으로 쌓아
두는 전용 그릇**이 [[Range Table]](범위·다중 조건을 담는 특수한 [[Internal Table]])다.

이 레슨의 목표는 Range Table을 "어려운 내부 구조"가 아니라
**"조건 문장을 표로 적는 방법"으로** 이해하는 것이다.

## Range Table — 조건을 행으로 쌓는 표

[[Range Table]]의 각 행은 **하나의 조건**이고, 모든 행에는 항상 **네 칸**이 있다.

| 컬럼 | 뜻 | 예 |
|---|---|---|
| **[[SIGN]]** | 이 조건을 **포함(I)** 할지 **제외(E)** 할지 | `I`, `E` |
| **[[OPTION]]** | **어떻게** 비교할지 | `EQ`(같다), `BT`(사이), `CP`(패턴) … |
| **LOW** | 비교할 값 / 범위의 **시작** | `C001`, `20` |
| **HIGH** | 범위의 **끝** (`BT`일 때만 주로 사용) | `C003`, `30` |

> 칸 크기도 정해져 있다 — `SIGN`은 **한 글자**(`I`/`E`), `OPTION`은 **두 글자**(`EQ`·`BT`·`CP`…).
> 그래서 `SIGN`에 `BT`를, `OPTION`에 `I`를 넣는 건 칸을 바꿔 쓴 실수다.

가장 먼저 기억할 한 문장은 이것이다.

> **Range Table은 조건 여러 개를 행으로 쌓는 표이고, 각 행은 `SIGN + OPTION + LOW + HIGH`로 읽는다.**

## 사람 말을 행으로 바꿔 보기

사람 말을 그대로 네 칸에 나눠 적으면 이렇게 된다.

| 사람 말 | SIGN | OPTION | LOW | HIGH |
|---|---|---|---|---|
| 공연 C001만 포함 | I | EQ | C001 |  |
| 공연 C001부터 C003까지 포함 | I | BT | C001 | C003 |
| 취소 상태 C는 제외 | E | EQ | C |  |
| 고객명이 '정'으로 시작하는 건 포함 | I | CP | `정*` |  |

세 번째 줄만 `SIGN`이 `E`다. 나머지는 "넣어 달라"는 조건이고, 이 줄만 "빼 달라"는
조건이기 때문이다.

`HIGH`가 **비어 있는 행**을 보고 당황하지는 말자. `EQ`처럼 값 하나로 판단하는 옵션은
보통 `LOW`만 쓴다. 시작과 끝이 둘 다 필요한 `BT`에서 `HIGH`가 의미를 갖는다.

> 표 마지막 줄의 `CP`와 `정*`는 아직 안 배운 비교 방식이다. 지금은 **`*`가 "여기부터는
> 아무 글자나 와도 된다"는 뜻**이라는 것만 알고 넘어가자. 자세한 건
> [EQ / BT / CP 옵션 이해](CH12-L05.html)에서 다룬다.

이번엔 반대로, 행만 보고 무슨 조건인지 읽어 보자.

| SIGN | OPTION | LOW | HIGH | 해석 |
|---|---|---|---|---|
| I | EQ | N |  | 상태 N 포함 |
| I | BT | 20 | 30 | 20부터 30까지 포함 |
| E | EQ | C |  | C는 제외 |
| I | CP | `C*` |  | C로 시작하는 값 포함 |

조건 카드를 누르면 네 칸이 어떻게 채워질까? 직접 눌러 확인해 보자.

::embed CH12-L01-S01 | 조건 카드 → Range Table 행 빌더::

## 흔한 실수와 점검

- **`SIGN`과 `OPTION`을 섞는다** → `SIGN`은 *방향*(포함 I·제외 E), `OPTION`은
  *비교 방식*(EQ·BT·CP…). `I`/`E`는 방향, `EQ`/`BT`/`CP`는 비교 방식이다.
- **`HIGH`를 항상 채운다** → `EQ`는 값 하나만 비교하므로 `LOW`만 있으면 된다.
  `HIGH`는 `BT` 같은 **범위** 조건에서만 쓴다.
- **여러 행을 "모두 만족(AND)"으로 오해한다** → 여기서 한 번 멈춰 생각해 보자. 포함(I)
  행이 두 줄이면 **둘 다** 맞아야 할까, **하나만** 맞아도 될까? 그리고 제외(E) 행만
  덩그러니 있으면 결과는 어떻게 될까? 답은
  [Multiple Selection과 Include/Exclude](CH12-L04.html)에서 확인한다.

## 정리

- [[Range Table]]은 조건을 **행으로 쌓는** 특수한 [[Internal Table]]이다.
- 한 행은 `SIGN`·`OPTION`·`LOW`·`HIGH` 네 칸으로 구성된다.
- `SIGN`은 포함/제외, `OPTION`은 비교 방식, `LOW`·`HIGH`는 비교값이다.
- `EQ`는 `LOW`만, `BT`는 `LOW`와 `HIGH`를 함께 쓴다.

---

구조는 알았다. 하지만 사용자가 매번 `SIGN`·`OPTION`을 손으로 적게 할 순 없다.
이 Range Table을 **화면에서 자동으로** 만들어 주는 게 SELECT-OPTIONS다 → 다음 레슨.
