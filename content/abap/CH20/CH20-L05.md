---
id: CH20-L05
title: "Window Expression — 행을 유지하며 그룹 계산"
direction: GROUP BY는 행을 접지만, window는 상세 행을 두고 합계·순번을 붙인다.
keywords: [Window, OVER, "PARTITION BY", "ROW_NUMBER", RANK]
order: 5
introduces: ["Window expression", "OVER/PARTITION BY", "ROW_NUMBER/RANK/DENSE_RANK"]
prereq: ["CH20-L01", "CH13-L01"]
prevRel: deepening
---

## 왜 — GROUP BY는 상세 행이 사라진다 🪟

[`GROUP BY`](CH13-L01.html)는 강력하지만 대가가 있다. **상세 행이 사라진다.**

예약 상세를 보면서 같은 공연의 총 예약 좌석도 함께 보고 싶다고 하자. `GROUP BY concert_id`를 쓰면 공연별 합계만 남고 개별 예약 행은 없어진다. 개별 행을 **유지하면서** 그룹 합계를 붙이고 싶을 때 [[Window|Window Expression]]을 쓴다.

## 무엇 — OVER · PARTITION BY 🎛️

[[Window|Window Expression]](상세 행을 유지한 채 그룹 계산을 붙이는 SQL 식)은 함수 뒤에 `OVER( … )`를 붙인다.

```abap
SELECT booking_id,
       concert_id,
       seats,
       SUM( seats ) OVER( PARTITION BY concert_id )                       AS booked_total,
       ROW_NUMBER( ) OVER( PARTITION BY concert_id ORDER BY booking_id )  AS row_in_concert
  FROM zbooking
  WHERE status <> 'C'
  INTO TABLE @DATA(gt_window).
```

읽는 법은 이렇다.

| 부분 | 의미 |
|---|---|
| `SUM( seats )` | 좌석 수를 합산 |
| `OVER( … )` | 일반 집계가 아니라 window로 계산 |
| `PARTITION BY concert_id` | 같은 공연을 하나의 계산 묶음으로 |
| `ROW_NUMBER( )` | 묶음 안에서 행 번호를 붙임 |
| `ORDER BY booking_id` | 행 번호를 어떤 순서로 붙일지 |

`GROUP BY`와의 차이는 표로 확인해야 한다.

입력(`zbooking`):

| booking_id | concert_id | seats | status |
|---|---|---:|---|
| 0001 | C001 | 2 | N |
| 0002 | C001 | 3 | N |
| 0003 | C001 | 3 | N |
| 0004 | C002 | 4 | N |
| 0005 | C002 | 1 | **C** |

`WHERE status <> 'C'`가 취소 예약 `0005`를 먼저 걸러낸다. 아래 결과 표들은 남은 4행 기준이다.

`GROUP BY` 결과(행이 접힘):

| concert_id | sum_seats |
|---|---:|
| C001 | 8 |
| C002 | 4 |

Window 결과(상세 행 유지):

| booking_id | concert_id | seats | booked_total | row_in_concert |
|---|---|---:|---:|---:|
| 0001 | C001 | 2 | 8 | 1 |
| 0002 | C001 | 3 | 8 | 2 |
| 0003 | C001 | 3 | 8 | 3 |
| 0004 | C002 | 4 | 4 | 1 |

::embed CH20-L05-S01 | Window Partition Simulator (GROUP BY vs PARTITION · 순번)::

## 순위 함수 — RANK · DENSE_RANK 🏅

집계 말고 **순위**도 붙일 수 있다.

```abap
SELECT booking_id,
       concert_id,
       seats,
       RANK( )       OVER( PARTITION BY concert_id ORDER BY seats DESCENDING ) AS seat_rank,
       DENSE_RANK( ) OVER( PARTITION BY concert_id ORDER BY seats DESCENDING ) AS seat_rank_dense
  FROM zbooking
  WHERE status <> 'C'
  INTO TABLE @DATA(gt_ranked).
```

`RANK`는 같은 값이 있으면 다음 순위를 **건너뛴다**(1, 1, 3). `DENSE_RANK`는 **촘촘히 이어 간다**(1, 1, 2). 둘 다 `OVER( … ORDER BY … )`의 순서 정의가 매우 중요하다.

마침 위 입력에 동점이 있다. C001의 `0002`·`0003`이 똑같이 3석이다. 결과 표로 차이를 확인하자.

| booking_id | concert_id | seats | seat_rank | seat_rank_dense |
|---|---|---:|---:|---:|
| 0002 | C001 | 3 | 1 | 1 |
| 0003 | C001 | 3 | 1 | 1 |
| 0001 | C001 | 2 | **3** | **2** |
| 0004 | C002 | 4 | 1 | 1 |

3석 두 건이 나란히 공동 1위. 그다음 2석이 `RANK`로는 3위(2위를 건너뜀), `DENSE_RANK`로는 2위(촘촘히 이어짐)다.

## 어떻게 확인 ✅

1. `PARTITION BY`가 어떤 묶음을 만드는지 표시한다.
2. 묶음 안에서 `ORDER BY`가 어떤 순서를 만드는지 표시한다.
3. 함수 결과가 각 상세 행에 **반복되어 붙는지** 확인한다.
4. `OVER( … ORDER BY … )`와 문장 끝 `ORDER BY`를 혼동하지 않는다. 앞은 **계산 순서**, 뒤는 **표시 순서**로 서로 독립이다.

## 흔한 실수와 점검 🚧

- **`WHERE`에서 window로 필터** → window는 `SELECT` list에서만 쓴다. `WHERE`·`GROUP BY`에 직접 쓰지 못한다.
- **`PARTITION BY` 빠뜨림** → 빼면 **전체가 한 묶음**이 된다. 전체 합계가 모든 행에 붙는 게 의도인지 확인한다.
- **순위인데 `ORDER BY` 없음** → `RANK`·`DENSE_RANK`엔 `OVER` 안 `ORDER BY`가 필수다. 없으면 순위가 무의미하다.
- **릴리스 지원 가정** → window 같은 최신 SQL 기능은 시스템의 ABAP 릴리스가 낮으면 아예 없을 수 있다. 내 시스템에서 syntax check로 먼저 확인한다.

---

네 도구를 다 봤다. 이제 **언제 무엇을 쓸지**, 그리고 언제 멈출지 정리하자 →
[선택 기준과 멈춤 기준](CH20-L06.html).
