---
id: CH18-L06
title: "Legacy 코드의 Modern ABAP 리팩터링"
direction: classic 코드를 모던으로 — 짧고 안전하게, += 까지.
keywords: [리팩터링, "+=", Modern, before-after]
order: 6
introduces: ["복합 대입 += -= *= /=", "classic→modern 리팩터링"]
prereq: ["CH18-L01", "CH18-L02"]
prevRel: deepening
---

## 배운 모던을 모아 — 리팩터링

지금까지 배운 인라인·VALUE·CORRESPONDING·Table Expression·String Template을 모아,
앞 챕터의 classic 코드를 **짧고 읽기 좋게** 바꿔 본다.

```abap
" --- classic ---
DATA ls TYPE ty_line.
DATA lt TYPE ty_line_tab.
ls-dan = 2. ls-mul = 1. ls-result = 2.
APPEND ls TO lt.

" --- modern ---
DATA(lt) = VALUE ty_line_tab( ( dan = 2 mul = 1 result = 2 ) ).
```

## 복합 대입 — += -= *= /=

`lv = lv + 10`을 더 짧게 쓴다(CH04에서 예고 없이 미뤄 둔 그 문법).

```abap
lv_total += ls-result.    " lv_total = lv_total + ls-result
lv_n     -= 1.
```

::embed code-learning/diff-mapper.html | 같은 로직의 classic ↔ modern 코드를 짝지어 비교::

## 언제까지 모던, 언제 classic

- 새 코드는 **모던 우선**(짧고 안전).
- 기존 classic 코드를 읽고 유지보수할 줄도 알아야 한다(둘 다 현장에 있다).

## 흔한 실수와 점검

- **과한 한 줄** → 너무 많은 식을 한 줄에 몰면 오히려 안 읽힌다. 적당히 끊는다.
- **SELECT 모던 혼동** → SELECT 안의 modern 구문(콤마·escape)은 **다음 챕터(CH19)** 다.

---

ABAP 코드를 모던으로 쓰게 됐다. 배운 걸 **콘서트앱**에 적용해 리팩터해 보자 →
**CH18-L07(실습)**.
