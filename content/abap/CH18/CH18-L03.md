---
id: CH18-L03
title: "CORRESPONDING과 구조 매핑"
direction: 이름이 같은 필드를 새 타입으로 옮긴다 — CORRESPONDING·MAPPING·EXCEPT.
keywords: ["CORRESPONDING #()", MAPPING, EXCEPT, MOVE-CORRESPONDING]
order: 3
introduces: ["CORRESPONDING #( )", "MAPPING", "EXCEPT"]
prereq: ["CH18-L02", "CH05"]
prevRel: parallel
---

## 같은 데이터를 다른 모양으로

업무 프로그램은 같은 데이터를 다른 모양으로 옮기는 일이 많다. DB 행을 화면 표시용
구조로 옮기고, 입력 구조를 저장 구조로 옮긴다. classic은 같은 이름 필드를 자동으로 옮기는
`MOVE-CORRESPONDING`을 썼다([Local Structure (BEGIN OF ~ END OF)](CH05-L01.html)).

```abap
" classic
MOVE-CORRESPONDING ls_source TO ls_target.
```

modern에서는 **값을 만드는 식** 흐름 안에서 같은 일을 한다: `CORRESPONDING` constructor.

## 같은 이름은 자동, 다른 이름은 MAPPING, 제외는 EXCEPT

원본(저장용 구조)에는 마지막 처리자 ID(`audit_user`)까지 들어 있고, 표시용 구조에는
이름이 다른 필드(`display_status`)가 있다고 하자. `audit_user`는 **양쪽에 같은 이름으로
있지만**, 감사(audit)용 내부 정보라 화면 구조로 **그대로 복사되면 곤란한** 필드다.

```abap
" 원본 — 저장용 구조. 감사용 처리자 ID까지 들고 있다
TYPES: BEGIN OF ts_booking_db,
         booking_id TYPE zbooking-booking_id,
         customer   TYPE zbooking-customer,
         seats      TYPE zbooking-seats,
         status     TYPE zbooking-status,
         audit_user TYPE syuname,       " 내부 정보 — 화면 노출 금지
       END OF ts_booking_db.
DATA ls_booking TYPE ts_booking_db.

" 대상 — 화면 표시용 구조
TYPES: BEGIN OF ts_booking_ui,
         booking_id     TYPE zbooking-booking_id,
         customer       TYPE zbooking-customer,
         seats          TYPE zbooking-seats,
         display_status TYPE zbooking-status,   " 이름 다름(원본은 status)
         audit_user     TYPE syuname,           " 이름 같음 — 자동 복사 대상!
       END OF ts_booking_ui.

DATA(ls_ui) = CORRESPONDING ts_booking_ui(
  ls_booking
  MAPPING display_status = status      " 대상 = 원본
  EXCEPT  audit_user ).
```

- `booking_id`·`customer`·`seats` — **같은 이름**이라 자동으로 옮긴다.
- `MAPPING display_status = status` — 이름이 다른 필드 연결. 문법은 **`대상필드 = 원본필드`** 다.
- `EXCEPT audit_user` — 이름이 같아 **자동 복사될 대상 필드** `audit_user`를 결과에서
  채우지 않는다(초기값으로 둠). 원본의 내부 정보가 화면 구조로 흘러드는 것을 막는다.

> 💡 `syuname`은 시스템 변수 `sy-uname`(로그인 사용자 ID)이 쓰는 **타입 이름**이다.
> 필드 접근이 아니라 타입이라서 하이픈 없이 붙여 쓴다 — 오타가 아니다.

내부 테이블도 같은 규칙이 각 행에 적용된다.

```abap
TYPES tt_booking_db TYPE STANDARD TABLE OF ts_booking_db WITH EMPTY KEY.
TYPES tt_booking_ui TYPE STANDARD TABLE OF ts_booking_ui WITH EMPTY KEY.
DATA lt_booking TYPE tt_booking_db.    " 조회해 온 원본 테이블이라 하자

DATA(lt_ui) = CORRESPONDING tt_booking_ui(
  lt_booking MAPPING display_status = status EXCEPT audit_user ).
```

> 💡 `CORRESPONDING`은 **모든 것을 복사하는 도구가 아니다.** **대상 타입 기준**으로 옮길 수
> 있는 필드만 결과에 들어간다. 원본에만 있는 필드(`created_by` 등)는 **조용히 버려진다.**

## 직접 매핑해 보기 — Field Mapping Board

원본·대상 구조의 필드가 어떻게 연결되는지 보자. 같은 이름은 자동, `status`→`display_status`는
`MAPPING`으로, `audit_user`는 `EXCEPT`로. 생성되는 `CORRESPONDING` 코드와 결과 Structure가 어떻게 바뀌는지 확인한다.

::embed CH18-L03-S01 | Field Mapping Board (CORRESPONDING) | 620::

## 흔한 실수와 점검

- **`MAPPING` 방향 혼동** → 문법은 `대상 = 원본`이다. `display_status = status`는 대상 `display_status`에 원본 `status`를 넣는다.
- **`EXCEPT`를 원본 기준으로 생각** → `EXCEPT` 목록은 **대상 필드** 기준이다. 그리고
  원본에 같은 이름이 없는 대상 필드는 어차피 초기값으로 남으니, `EXCEPT`는 **"원본에도
  같은 이름이 있어 자동 복사될 필드"를 막을 때** 의미가 있다.
- **이름 같다고 의미도 같다고 가정** → 한 구조의 `status`(예매 상태)와 다른 구조의 `status`(처리 상태)는 의미가 다를 수 있다. 이름은 같고 의미가 다르면 자동 복사는 위험하다.

> 💡 `CORRESPONDING`은 변환 로직을 **숨기기 쉽다.** 단순 이름 매칭엔 좋지만, 값 변환·조건부
> 매핑이 많아지면 `LOOP`와 명시 대입이 더 읽기 쉽다. 이 챕터에선 "같은 이름 또는 간단한 이름 변경" 범위에서만 쓴다.

## 정리

- `CORRESPONDING`은 **같은 이름 필드**를 기준으로 Structure·테이블을 새 타입으로 옮기는 constructor다.
- `MAPPING`은 이름이 다른 필드를 연결(`대상 = 원본`), `EXCEPT`는 **대상 필드**를 결과에서 제외한다.
- 강력하지만, **이름은 같고 의미가 다른** 구조에는 주의해야 한다.

---

구조 매핑까지 식으로 했다. 이제 테이블의 한 행을 **대괄호로 콕 집는** Table Expression
→ [Table Expression](CH18-L04.html).
