---
id: CH18-L07
title: "COND·SWITCH 조건 표현식"
direction: 조건에 따라 값 하나를 만드는 표현식 — 흐름을 나누는 IF/CASE와 구분한다.
keywords: [COND, SWITCH, 조건 표현식, constructor expression]
order: 7
introduces: ["COND", "SWITCH"]
prereq: ["CH18-L01", "CH04-L03"]
prevRel: parallel
---

## 값 하나 정하려고 IF를 여러 줄 쓴다

[[IF]]와 [[CASE]]는 **흐름을 나눈다.** 그런데 실무 코드에는 흐름 전체를 나누기보다
**"상태 문구 하나", "색 코드 하나"** 를 조건에 따라 정하고 싶은 경우가 많다. 이때 `IF`로
변수를 채우면, 정작 하려는 일(값 하나 정하기)보다 **대입 과정**이 더 크게 보인다.

```abap
DATA lv_status_text TYPE string.
IF lv_remaining = 0.
  lv_status_text = '매진'.
ELSEIF lv_remaining < 10.
  lv_status_text = '마감 임박'.
ELSE.
  lv_status_text = '예매 가능'.
ENDIF.
```

## COND — 값을 만드는 IF

[[COND]]는 조건을 차례로 검사해, **처음으로 참이 된 `WHEN` 조건**에 딸린 `THEN` 값을 돌려준다. `IF`에 가깝다.

```abap
DATA(lv_status_text) = COND string(
  WHEN lv_remaining = 0  THEN '매진'
  WHEN lv_remaining < 10 THEN '마감 임박'
  ELSE                        '예매 가능' ).
```

## SWITCH — 값을 만드는 CASE

[[SWITCH]]는 **하나의 값**이 어떤 상수와 같은지 비교해 결과를 돌려준다. `CASE`에 가깝다.

```abap
DATA(lv_status_text) = SWITCH string( lv_status
  WHEN 'N' THEN '예약'
  WHEN 'C' THEN '취소'
  ELSE          '알 수 없음' ).
```

> 💡 꼭 구분할 점 — `COND`/`SWITCH`는 **문장 묶음을 실행**하는 도구가 아니라 **값을
> 만드는** 도구다. 조건마다 저장·화면 호출·복잡한 처리를 넣고 싶다면 표현식이 아니라
> `IF`/`CASE`가 맞다.

## 직접 골라 보기 — 조건 표현식 선택기

잔여석·상태값을 바꿔 가며 `COND`와 `SWITCH`가 각각 어떤 `WHEN`을 고르는지, 어떤 상황엔
`COND`만 가능한지(범위 조건) 확인하자.

::embed CH18-L07-S01 | 조건 표현식 선택기 (COND·SWITCH) | 580::

## 흔한 실수와 점검

- **모든 `IF`를 `COND`로** → 짧다고 다 바꾸지 않는다. 흐름을 나누는 건 여전히 `IF`/`CASE`다.
- **`SWITCH`에 복잡한 조건** → `SWITCH`는 하나의 값을 상수들과 **같은지** 비교할 때만 맞다.
  범위·복합 조건은 `COND`.
- **`ELSE` 생략** → 조건에 안 맞으면 결과가 **초기값**이 된다(빈 문자·0). 필요하면 `ELSE`를 둔다.
- **결과 타입 불일치** → `THEN` 뒤 결과들은 결과 타입으로 변환 가능해야 한다.

> 🔎 공식 문서를 보면 `COND`/`SWITCH`의 결과 자리에 `THROW`(예외 일으키기)를 둘 수도 있다.
> 다만 예외 클래스와 `TRY/CATCH`를 알아야 쓸 수 있는 형태라, `ELSE THROW …`는
> [Exception Class — TRY/CATCH/CX 계층](CH21-L06.html)에서 다시 배운다. 지금 외울 필요는 없다.

## 정리

- `COND`는 **값을 만드는 IF**, `SWITCH`는 **값을 만드는 CASE**다.
- 흐름을 나누는 `IF`/`CASE`를 대체하는 게 아니라, **단순 조건 대입**을 더 가까운 위치에
  드러내는 도구다.

---

조건으로 값을 골랐다. 이제 **테이블 여러 행을 하나로 줄이거나 걸러 내는** 표현식으로
가자 → [REDUCE·FILTER 테이블 표현식](CH18-L08.html).
