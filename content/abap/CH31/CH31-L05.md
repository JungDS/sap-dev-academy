---
id: CH31-L05
title: "OData V2 EntitySet 조회 구현"
direction: GET_ENTITYSET로 목록을 돌려준다.
keywords: [OData, GET_ENTITYSET, DPC_EXT, 필터]
order: 5
introduces: ["GET_ENTITYSET 구현", "OData 필터 매핑"]
prereq: ["CH31-L04", "CH08"]
prevRel: deepening
---

## 목록 조회 — GET_ENTITYSET

DPC_EXT 클래스에서 EntitySet의 `..._GET_ENTITYSET` 메서드를 재정의해 데이터를 돌려준다.
CH08의 SELECT가 여기 쓰인다.

```abap
METHOD concertset_get_entityset.
  " URL 필터를 ABAP 조건으로
  DATA(lt_filter) = io_tech_request_context->get_filter( )->get_filter_select_options( ).

  SELECT concert_id, artist, venue, capacity
    FROM zconcert
    INTO CORRESPONDING FIELDS OF TABLE @et_entityset
    UP TO iv_top ROWS.        " $top 페이징
ENDMETHOD.
```

- `et_entityset` — 돌려줄 목록(모델 구조).
- `$filter`·`$top`·`$skip`·`$orderby` 같은 OData 쿼리를 ABAP 조건으로 매핑.
- 단건은 `GET_ENTITY`.

## 흔한 실수와 점검

- **필터 무시** → `$filter`를 안 받으면 전체 반환(성능·정합 문제). 매핑한다.
- **페이징 무시** → 대량은 `$top/$skip`을 SELECT에 반영.

> 신규 개발은 **RAP(CH23)** 이 OData를 더 적은 코드로 노출한다 — SEGW는 기존 자산 이해용.

---

시스템 연계(IDoc·OData)를 다뤘다. 이제 느린 프로그램을 빠르게 — 성능 분석·튜닝 →
**CH32**.
