---
id: CH26-L04
title: "MVC 기반 Report 구조화"
direction: 화면·로직·데이터를 분리한다 — MVC.
keywords: [MVC, Model, View, Controller, 구조화]
order: 4
introduces: ["MVC 구조화"]
prereq: ["CH26-L01", "CH11"]
prevRel: deepening
---

## 한 프로그램에 다 섞으면

조회·계산·화면·이벤트가 한 리포트에 뒤섞이면 유지보수가 지옥이다. **MVC**로 역할을
나눈다.

| 계층 | 역할 | 예 |
|---|---|---|
| **Model** | 데이터·업무 로직 | `ZCL_BOOKING_MODEL`(조회·계산) |
| **View** | 표시 | ALV/Dynpro 출력 |
| **Controller** | 흐름 조율 | 이벤트 받아 Model 호출 → View 갱신 |

```abap
" Controller가 조율
DATA(lo_model) = NEW zcl_booking_model( iv_concert = p_concert ).
DATA(lt) = lo_model->get_bookings( ).
mo_view->display( lt ).
```

- 화면을 바꿔도 Model은 그대로, 로직을 바꿔도 View는 그대로.
- 테스트도 Model만 떼어 검증 가능(다음 레슨).

## 흔한 실수와 점검

- **Model에 화면 코드** → 표시 관심사는 View로. Model은 순수 로직.
- **과분리** → 작은 리포트까지 3계층은 과할 수 있다. 규모에 맞게.

---

구조를 나눴다. 그 덕에 **테스트 가능한** 설계가 가능해진다 → 다음 레슨.
