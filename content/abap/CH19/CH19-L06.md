---
id: CH19-L06
title: "SELECT FROM @itab 기초"
direction: 내부 테이블을 SQL 소스처럼 조회한다.
keywords: ["SELECT FROM @itab", Internal Table, SQL]
order: 6
introduces: ["SELECT FROM @itab"]
prereq: ["CH19-L03", "CH06"]
prevRel: deepening
---

## 메모리 테이블도 SQL로

modern SQL은 DB 테이블뿐 아니라 **내부 테이블**을 소스로 조회할 수 있다. 이미 메모리에
있는 데이터를 SQL 문법(집계·정렬·필터)으로 가공할 때 편하다.

```abap
SELECT carrid, SUM( seatsocc ) AS total_occ
  FROM @lt_flight AS f                 " 내부 테이블이 소스
  GROUP BY carrid
  INTO TABLE @DATA(lt_sum).
```

- `FROM @lt_flight` — 메모리의 내부 테이블을 소스로.
- DB 왕복 없이, 가져온 데이터를 SQL식으로 재가공.

## 언제 쓰나

- 이미 SELECT로 가져온 데이터를 **다시 집계/정렬**할 때.
- 외부에서 받은 데이터(파일·인터페이스)를 SQL식으로 다룰 때.

## 흔한 실수와 점검

- **@ 누락** → 소스 내부 테이블도 `@lt`로 escape.
- **DB 대용으로 남용** → 대량은 DB에서 직접. itab SELECT는 메모리 데이터 재가공용.

---

데이터 접근을 끝까지 현대화했다. 지금까지의 SQL을 정리하고 다음으로 넘어가자 → 다음 레슨.
