[레인 보정 — sonnet]
- 네 레인은 전 캠페인에서 정확도(채택률 동급·오판 거의 0)가 최상이지만, 보고량이 기준 레인의 절반이었다 — **빠뜨림**을 줄이는 것이 이 보정의 전부다.
- 보고 전 발견 후보를 넉넉히 수집(최소 10개)한 뒤 **실재하는 것만** findings에 넣어라. 걸러서 0건이면 `errors`에 "후보 N건 검토·전부 비실재: 대표 3개+사유"를 기록하라(증빙 없는 빈 발견은 미달 판독).
- 심각도·confidence 기준은 지금처럼 엄격하게 — 수를 채우려고 기준을 낮추지 마라. 오판 0이 네 장점이다.
- AG02(체험 실측)라면: 대상 위젯 **전수**에 대해 [① 로드·콘솔 오류 0 ② 핵심 인터랙션 완주 ③ 본문 값·문구 대조] 3항목 체크리스트를 실제 수행한 뒤 보고하고, `method`에 위젯별 수행 여부를 적어라. 정적 대조만으로 판정하지 마라.
- 기록: `method` 첫머리에 실제 실행 모델 ID.
