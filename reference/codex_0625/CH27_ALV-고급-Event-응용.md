# CH27 · ALV 고급 Event 응용 — codex_0625 강의 개선·보강본

> 생성 시각: 2026-06-25-13:22 KST  
> 기준 소스: `content/abap/CH27/_chapter.md` + 해당 챕터 레슨 5개  
> 목적: 원본 `content/abap`을 덮어쓰지 않고, 이후 리빌드/강의자료 작성에 바로 사용할 수 있는 고밀도 보강안을 제공한다.

## 1. 프로젝트 분석 기준

- 학습 대상: 개발·전공 경험 없는 입문자. 설명은 압축하지 않고 "왜 필요한가 -> 무엇인가 -> 어떻게 확인하는가 -> 실수/주의 -> 정리" 순서로 풀어쓴다.
- R15 게이팅: 이미 배운 것만으로 이해할 수 있어야 한다. 후속 개념은 L1 예고까지만 허용하고, 코드나 정의를 앞당기지 않는다.
- R2 코드=체험: 코드가 1줄이라도 있으면 같은 페이지에서 조작하거나, 최소한 어떤 시뮬레이터/다이어그램/버튼 흐름으로 체험할지 명시한다.
- 참고 우선순위: 로컬 `content/abap`과 `.project-docs`가 NotebookLM보다 최신이다. NotebookLM은 큰 방향 참고로만 사용한다.

## 2. 이 챕터의 강의 목표

Grid ALV 사용자의 행동을 이벤트로 받아 처리한다.

**반드시 강화할 지점**
- Double Click, Hotspot, Toolbar, USER_COMMAND의 발생 지점 분리
- Event Handler Class의 SET HANDLER 연결을 시각화
- 데이터 변경이 아닌 UI 반응과 업무 이동을 중심으로 설명
- Editable Grid 변경 이벤트는 CH28로 넘긴다

**대표 체험 설계**
- ALV 셀을 클릭하면 이벤트 객체와 handler method가 어떤 값을 받는지 배선도로 보여준다.

**이전/다음 연결**
- 이전: CH26 · OO ABAP 고급 설계와 패턴
- 다음: CH28 · Editable Grid ALV와 입력 검증

## 3. 원본 진단 요약

| 항목 | 값 |
|---|---:|
| 레슨 수 | 5 |
| 본문 총 글자(공백 제외) | 4039 |
| 코드 블록 | 14 |
| embed 지시문 | 5 |
| 용어 마킹 | 0 |
| 진단 플래그 총합 | 7 |

| 레슨 | 제목 | 본문자 | 코드 | embed | 진단 |
|---|---|---:|---:|---:|---|
| CH27-L01 | Double Click Event | 844 | 4 | 1 | 본문빈약 |
| CH27-L02 | Hotspot Click Event | 673 | 4 | 1 | 본문빈약 |
| CH27-L03 | Toolbar Event | 650 | 2 | 1 | 본문빈약, 흐름압축 |
| CH27-L04 | USER_COMMAND 처리 | 793 | 2 | 1 | 본문빈약, 흐름압축 |
| CH27-L05 | ALV Event Handler Class 설계 | 1079 | 2 | 1 | 본문주의 |

## 4. 게이팅·품질 리스크

- Track-2 실무 구간이다. 성공 예제보다 오류, 재처리, 운영 로그, 권한, 이송 리스크를 함께 다룬다.
- 본문이 짧은 레슨이 있다. 왜 필요한가, 실수하면 무엇이 깨지는가, 어디서 확인하는가를 별도 섹션으로 늘린다.

## 5. 레슨별 개선·보강안

### CH27-L01 · Double Click Event

**원본 신호**
- 파일: `content/abap/CH27/CH27-L01.md`
- 방향: 행을 더블클릭하면 상세로 — ALV 이벤트의 시작.
- 키워드: double_click, Event, SET HANDLER, ALV
- introduces: ALV double_click 이벤트, 이벤트 핸들러 연결
- prereq: CH17, CH20-L09
- 진단: 본문 844자 · 섹션 5개 · 코드 4개 · embed 1개 · 본문빈약

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 Double Click Event를 제시한다.
- 핵심 설명: 실행 전 예측 -> 한 줄 실행 -> 변수 변화 확인 -> 왜 그렇게 됐는지 설명하는 디버거 루프를 고정한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 기존 위젯 `CH27-L01-S01`을 중심에 둔다. 본문은 위젯 앞에서 조작 목표를 3단계로 제시하고, 위젯 뒤에서 결과 해석과 흔한 실수를 회수한다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: Double Click Event의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- `C:/ABAP_DOCU_HTML/abapclass.htm`
- `C:/ABAP_DOCU_HTML/abapmethods.htm`
- `C:/ABAP_DOCU_HTML/abapcall_method_static.htm`
- `C:/ABAP_DOCU_HTML/abapinterface.htm`
- `C:/ABAP_DOCU_HTML/abapevents.htm`

### CH27-L02 · Hotspot Click Event

**원본 신호**
- 파일: `content/abap/CH27/CH27-L02.md`
- 방향: 셀을 링크처럼 — 한 번 클릭으로 이동.
- 키워드: hotspot, hotspot_click, Event
- introduces: hotspot 컬럼, hotspot_click 이벤트
- prereq: CH27-L01
- 진단: 본문 673자 · 섹션 5개 · 코드 4개 · embed 1개 · 본문빈약

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 Hotspot Click Event를 제시한다.
- 핵심 설명: Grid ALV 사용자의 행동을 이벤트로 받아 처리한다. 이 레슨은 그 큰 흐름 안에서 "왜 필요한가 -> 무엇인가 -> 어떻게 확인하는가 -> 실수하면 어떻게 보이는가" 순서로 다시 풀어야 한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 기존 위젯 `CH27-L02-S01`을 중심에 둔다. 본문은 위젯 앞에서 조작 목표를 3단계로 제시하고, 위젯 뒤에서 결과 해석과 흔한 실수를 회수한다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: Hotspot Click Event의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- 이 레슨은 주로 프로젝트 커리큘럼/실습 맥락을 따른다. 관련 키워드가 추가되면 ABAP_DOCU sitemap에서 문서명을 확인한다.

### CH27-L03 · Toolbar Event

**원본 신호**
- 파일: `content/abap/CH27/CH27-L03.md`
- 방향: ALV 툴바에 내 버튼을 추가한다.
- 키워드: toolbar, Event, 커스텀 버튼
- introduces: toolbar 이벤트, 커스텀 툴바 버튼
- prereq: CH27-L01
- 진단: 본문 650자 · 섹션 3개 · 코드 2개 · embed 1개 · 본문빈약, 흐름압축

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 Toolbar Event를 제시한다.
- 핵심 설명: Internal Table이 화면 표로 변환되는 순간을 보여준다. 표시 편의, 사용자 조작, 개발 복잡도 사이의 균형을 설명한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 기존 위젯 `CH27-L03-S01`을 중심에 둔다. 본문은 위젯 앞에서 조작 목표를 3단계로 제시하고, 위젯 뒤에서 결과 해석과 흔한 실수를 회수한다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: Toolbar Event의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- 이 레슨은 주로 프로젝트 커리큘럼/실습 맥락을 따른다. 관련 키워드가 추가되면 ABAP_DOCU sitemap에서 문서명을 확인한다.

### CH27-L04 · USER_COMMAND 처리

**원본 신호**
- 파일: `content/abap/CH27/CH27-L04.md`
- 방향: 툴바 버튼이 눌리면 — 명령을 분기 처리한다.
- 키워드: user_command, Event, 선택 행
- introduces: user_command 이벤트, 선택 행 가져오기
- prereq: CH27-L03
- 진단: 본문 793자 · 섹션 3개 · 코드 2개 · embed 1개 · 본문빈약, 흐름압축

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 USER_COMMAND 처리를 제시한다.
- 핵심 설명: Grid ALV 사용자의 행동을 이벤트로 받아 처리한다. 이 레슨은 그 큰 흐름 안에서 "왜 필요한가 -> 무엇인가 -> 어떻게 확인하는가 -> 실수하면 어떻게 보이는가" 순서로 다시 풀어야 한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 기존 위젯 `CH27-L04-S01`을 중심에 둔다. 본문은 위젯 앞에서 조작 목표를 3단계로 제시하고, 위젯 뒤에서 결과 해석과 흔한 실수를 회수한다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: USER_COMMAND 처리의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- 이 레슨은 주로 프로젝트 커리큘럼/실습 맥락을 따른다. 관련 키워드가 추가되면 ABAP_DOCU sitemap에서 문서명을 확인한다.

### CH27-L05 · ALV Event Handler Class 설계

**원본 신호**
- 파일: `content/abap/CH27/CH27-L05.md`
- 방향: 이벤트 처리를 한 클래스로 모아 깔끔하게.
- 키워드: Event Handler, Class, 설계
- introduces: ALV 이벤트 핸들러 클래스 통합 설계
- prereq: CH27-L01, CH26
- 진단: 본문 1079자 · 섹션 4개 · 코드 2개 · embed 1개 · 본문주의

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 ALV Event Handler Class 설계를 제시한다.
- 핵심 설명: Internal Table이 화면 표로 변환되는 순간을 보여준다. 표시 편의, 사용자 조작, 개발 복잡도 사이의 균형을 설명한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 기존 위젯 `CH27-L05-S01`을 중심에 둔다. 본문은 위젯 앞에서 조작 목표를 3단계로 제시하고, 위젯 뒤에서 결과 해석과 흔한 실수를 회수한다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: ALV Event Handler Class 설계의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- `C:/ABAP_DOCU_HTML/abapclass.htm`
- `C:/ABAP_DOCU_HTML/abapmethods.htm`
- `C:/ABAP_DOCU_HTML/abapcall_method_static.htm`
- `C:/ABAP_DOCU_HTML/abapinterface.htm`
- `C:/ABAP_DOCU_HTML/abapevents.htm`


## 6. 이 챕터를 감탄나는 자료로 만들기 위한 마무리 체크

- 각 레슨 도입부는 "무엇을 배운다"가 아니라 "지금 무엇이 불편한가"로 시작한다.
- 레슨 끝에는 다음 레슨이 진짜 해결책일 때만 pain -> solution으로 연결한다. 병렬/심화 관계는 억지 불편을 만들지 않는다.
- ABAP 고유 용어는 영어 원문을 살리고 괄호로 쉬운 풀이를 붙인다.
- 코드 예제는 짧은 한 줄 설명, 실행 전 예상, 실행 후 결과 해석, 실패 케이스 순서로 닫는다.
- 공식 문서는 로컬 ABAP_DOCU 파일명으로 재확인하고, 예제는 프로젝트의 관통 예제(구구단, SFLIGHT, 콘서트 예매, 정훈영)를 우선 사용한다.
