# CH10 · 모듈화 기초 — codex_0625 강의 개선·보강본

> 생성 시각: 2026-06-25-13:22 KST  
> 기준 소스: `content/abap/CH10/_chapter.md` + 해당 챕터 레슨 7개  
> 목적: 원본 `content/abap`을 덮어쓰지 않고, 이후 리빌드/강의자료 작성에 바로 사용할 수 있는 고밀도 보강안을 제공한다.

## 1. 프로젝트 분석 기준

- 학습 대상: 개발·전공 경험 없는 입문자. 설명은 압축하지 않고 "왜 필요한가 -> 무엇인가 -> 어떻게 확인하는가 -> 실수/주의 -> 정리" 순서로 풀어쓴다.
- R15 게이팅: 이미 배운 것만으로 이해할 수 있어야 한다. 후속 개념은 L1 예고까지만 허용하고, 코드나 정의를 앞당기지 않는다.
- R2 코드=체험: 코드가 1줄이라도 있으면 같은 페이지에서 조작하거나, 최소한 어떤 시뮬레이터/다이어그램/버튼 흐름으로 체험할지 명시한다.
- 참고 우선순위: 로컬 `content/abap`과 `.project-docs`가 NotebookLM보다 최신이다. NotebookLM은 큰 방향 참고로만 사용한다.

## 2. 이 챕터의 강의 목표

길어진 코드를 이름 붙인 처리 단위로 나누는 장이다.

**반드시 강화할 지점**
- FORM은 초보용 분리 도구, Function Module은 재사용 단위, Class는 설계 단위로 구분
- USING/CHANGING의 데이터 이동 방향을 그림으로 설명
- classic 구간이므로 NEW/inline DATA 금지
- 잔여석 계산 같은 업무 규칙을 모듈화 예제로 통일

**대표 체험 설계**
- 입력값이 FORM으로 들어가고 CHANGING 값이 되돌아오는 호출 스택을 단계별로 보여준다.

**이전/다음 연결**
- 이전: CH09 · DDIC 관계와 입력도움말(F4)
- 다음: CH11 · SALV 1차 (간단 ALV)

## 3. 원본 진단 요약

| 항목 | 값 |
|---|---:|
| 레슨 수 | 7 |
| 본문 총 글자(공백 제외) | 6160 |
| 코드 블록 | 20 |
| embed 지시문 | 1 |
| 용어 마킹 | 19 |
| 진단 플래그 총합 | 10 |

| 레슨 | 제목 | 본문자 | 코드 | embed | 진단 |
|---|---|---:|---:|---:|---|
| CH10-L01 | FORM / PERFORM 기본 호출 | 546 | 4 | 0 | R2-체험누락, 본문빈약 |
| CH10-L02 | USING / CHANGING 파라미터 | 1194 | 2 | 0 | R2-체험누락, 본문주의 |
| CH10-L03 | CALL FUNCTION 기본 구조 | 678 | 2 | 0 | R2-체험누락, 본문빈약 |
| CH10-L04 | Local Class로 모듈화 — 정적 기준 | 1210 | 4 | 0 | R2-체험누락 |
| CH10-L05 | Global Class 호출 기초 | 825 | 4 | 0 | R2-체험누락, 본문빈약 |
| CH10-L06 | Subroutine / Function / Class 선택 기준 | 501 | 0 | 0 | 본문빈약 |
| CH10-L07 | 실습 — 잔여석 계산·예매 판정 모듈화 | 1206 | 4 | 1 | OK |

## 4. 게이팅·품질 리스크

- CH17까지는 classic-first 구간이다. inline DATA, VALUE, NEW, @ host variable, string template을 새로 끌어오지 않는다. 단 CH04의 && 예외는 허용된다.
- 코드가 있으나 embed가 없는 레슨이 있다. 본문 보강 시 코드 주변에 체험/시뮬레이터 또는 최소한 조작형 퀴즈 설명을 붙여야 한다.
- 본문이 짧은 레슨이 있다. 왜 필요한가, 실수하면 무엇이 깨지는가, 어디서 확인하는가를 별도 섹션으로 늘린다.

## 5. 레슨별 개선·보강안

### CH10-L01 · FORM / PERFORM 기본 호출

**원본 신호**
- 파일: `content/abap/CH10/CH10-L01.md`
- 방향: 반복 코드를 묶는 첫 도구 — 서브루틴(FORM/PERFORM).
- 키워드: Subroutine, FORM, PERFORM, 모듈화
- introduces: Subroutine, FORM, PERFORM, 로컬변수 vs 전역변수
- prereq: CH04
- 진단: 본문 546자 · 섹션 4개 · 코드 4개 · embed 0개 · R2-체험누락, 본문빈약

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 FORM / PERFORM 기본 호출를 제시한다.
- 핵심 설명: 값, 타입, 이름, 재사용 범위를 각각 다른 색 카드로 분리한다. 초보자가 "변수명=값"과 "타입=규칙"을 섞지 않게 한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 필요 학습수단: 코드 실행 스텝퍼. 한 줄씩 실행하면서 입력값, 중간값, 출력값을 나란히 보여준다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: FORM / PERFORM 기본 호출의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- `C:/ABAP_DOCU_HTML/abapdata.htm`
- `C:/ABAP_DOCU_HTML/abapdata_simple.htm`
- `C:/ABAP_DOCU_HTML/abapdata_referring.htm`
- `C:/ABAP_DOCU_HTML/abaptypes.htm`
- `C:/ABAP_DOCU_HTML/abapif.htm`
- `C:/ABAP_DOCU_HTML/abapcase.htm`

### CH10-L02 · USING / CHANGING 파라미터

**원본 신호**
- 파일: `content/abap/CH10/CH10-L02.md`
- 방향: 서브루틴에 값을 주고(USING) 결과를 돌려받는다(CHANGING).
- 키워드: USING, CHANGING, Subroutine, 파라미터
- introduces: USING, CHANGING, Call by Value/Value-Result/Reference, RETURN, STATICS
- prereq: CH10-L01
- 진단: 본문 1194자 · 섹션 5개 · 코드 2개 · embed 0개 · R2-체험누락, 본문주의

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 USING / CHANGING 파라미터를 제시한다.
- 핵심 설명: 긴 코드를 함수 상자로 접어 넣는 느낌을 준다. 입력, 출력, 바뀌는 값, 사라지는 지역 변수를 화살표로 분리한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 필요 학습수단: 코드 실행 스텝퍼. 한 줄씩 실행하면서 입력값, 중간값, 출력값을 나란히 보여준다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: USING / CHANGING 파라미터의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- 이 레슨은 주로 프로젝트 커리큘럼/실습 맥락을 따른다. 관련 키워드가 추가되면 ABAP_DOCU sitemap에서 문서명을 확인한다.

### CH10-L03 · CALL FUNCTION 기본 구조

**원본 신호**
- 파일: `content/abap/CH10/CH10-L03.md`
- 방향: 여러 프로그램이 공유하는 재사용 단위 — Function Module.
- 키워드: Function Module, CALL FUNCTION, SE37, EXPORTING, IMPORTING
- introduces: Function Module, CALL FUNCTION, SE37, EXPORTING/IMPORTING/CHANGING, classic EXCEPTIONS
- prereq: CH10-L01
- 진단: 본문 678자 · 섹션 4개 · 코드 2개 · embed 0개 · R2-체험누락, 본문빈약

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 CALL FUNCTION 기본 구조를 제시한다.
- 핵심 설명: 긴 코드를 함수 상자로 접어 넣는 느낌을 준다. 입력, 출력, 바뀌는 값, 사라지는 지역 변수를 화살표로 분리한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 필요 학습수단: 책임/객체 다이어그램. 파일 또는 클래스별 책임을 카드로 분리하고 호출 흐름을 화살표로 보여준다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: CALL FUNCTION 기본 구조의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- 이 레슨은 주로 프로젝트 커리큘럼/실습 맥락을 따른다. 관련 키워드가 추가되면 ABAP_DOCU sitemap에서 문서명을 확인한다.

### CH10-L04 · Local Class로 모듈화 — 정적 기준

**원본 신호**
- 파일: `content/abap/CH10/CH10-L04.md`
- 방향: 클래스라는 또 다른 묶음 — 객체 없이 부르는 정적 메서드부터.
- 키워드: Class, Method, 로컬 클래스, Static
- introduces: ATTRIBUTE, METHOD, PUBLIC/PRIVATE, Class↔Object, Static 메서드(=>)
- prereq: CH10-L01
- 진단: 본문 1210자 · 섹션 5개 · 코드 4개 · embed 0개 · R2-체험누락

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 Local Class로 모듈화 — 정적 기준를 제시한다.
- 핵심 설명: 긴 코드를 함수 상자로 접어 넣는 느낌을 준다. 입력, 출력, 바뀌는 값, 사라지는 지역 변수를 화살표로 분리한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 필요 학습수단: 책임/객체 다이어그램. 파일 또는 클래스별 책임을 카드로 분리하고 호출 흐름을 화살표로 보여준다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: Local Class로 모듈화 — 정적 기준의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- `C:/ABAP_DOCU_HTML/abapclass.htm`
- `C:/ABAP_DOCU_HTML/abapmethods.htm`
- `C:/ABAP_DOCU_HTML/abapcall_method_static.htm`
- `C:/ABAP_DOCU_HTML/abapinterface.htm`
- `C:/ABAP_DOCU_HTML/abapevents.htm`

### CH10-L05 · Global Class 호출 기초

**원본 신호**
- 파일: `content/abap/CH10/CH10-L05.md`
- 방향: 이미 만들어진 전역 클래스의 메서드를 정적으로 불러 쓴다.
- 키워드: Global Class, SE24, Static Method, 블랙박스
- introduces: Global Class 호출(static 우선)
- prereq: CH10-L03, CH10-L04
- 진단: 본문 825자 · 섹션 5개 · 코드 4개 · embed 0개 · R2-체험누락, 본문빈약

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 Global Class 호출 기초를 제시한다.
- 핵심 설명: 객체는 문법 묶음이 아니라 책임 묶음이다. 속성은 상태, 메서드는 행동, visibility는 약속으로 풀어 설명한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 필요 학습수단: 책임/객체 다이어그램. 파일 또는 클래스별 책임을 카드로 분리하고 호출 흐름을 화살표로 보여준다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: Global Class 호출 기초의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- `C:/ABAP_DOCU_HTML/abapclass.htm`
- `C:/ABAP_DOCU_HTML/abapmethods.htm`
- `C:/ABAP_DOCU_HTML/abapcall_method_static.htm`
- `C:/ABAP_DOCU_HTML/abapinterface.htm`
- `C:/ABAP_DOCU_HTML/abapevents.htm`

### CH10-L06 · Subroutine / Function / Class 선택 기준

**원본 신호**
- 파일: `content/abap/CH10/CH10-L06.md`
- 방향: 셋 중 무엇을 — 상황별 모듈화 선택 기준.
- 키워드: Subroutine, Function Module, Class, 모듈화
- introduces: Subroutine/Function/Class 선택 기준
- prereq: CH10-L01, CH10-L03, CH10-L04
- 진단: 본문 501자 · 섹션 4개 · 코드 0개 · embed 0개 · 본문빈약

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 Subroutine / Function / Class 선택 기준를 제시한다.
- 핵심 설명: 긴 코드를 함수 상자로 접어 넣는 느낌을 준다. 입력, 출력, 바뀌는 값, 사라지는 지역 변수를 화살표로 분리한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 텍스트만 두지 말고, 개념 관계도 또는 의사결정 표를 넣는다. 버튼형 시뮬레이터가 과하면 "상황 카드 -> 선택 -> 해설" 구조의 미니 퀴즈로 충분하다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: Subroutine / Function / Class 선택 기준의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- `C:/ABAP_DOCU_HTML/abapclass.htm`
- `C:/ABAP_DOCU_HTML/abapmethods.htm`
- `C:/ABAP_DOCU_HTML/abapcall_method_static.htm`
- `C:/ABAP_DOCU_HTML/abapinterface.htm`
- `C:/ABAP_DOCU_HTML/abapevents.htm`

### CH10-L07 · 실습 — 잔여석 계산·예매 판정 모듈화

**원본 신호**
- 파일: `content/abap/CH10/CH10-L07.md`
- 방향: 콘서트앱 2단계 — 핵심 로직을 모듈로 묶는다.
- 키워드: 실습, 콘서트앱, 모듈화, 잔여석, FORM
- introduces: 미선언
- prereq: CH10-L01, CH08, CH06
- 진단: 본문 1206자 · 섹션 5개 · 코드 4개 · embed 1개 · 중대 플래그 없음

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 실습 — 잔여석 계산·예매 판정 모듈화를 제시한다.
- 핵심 설명: 긴 코드를 함수 상자로 접어 넣는 느낌을 준다. 입력, 출력, 바뀌는 값, 사라지는 지역 변수를 화살표로 분리한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 기존 위젯 `CH10-L07-S01`을 중심에 둔다. 본문은 위젯 앞에서 조작 목표를 3단계로 제시하고, 위젯 뒤에서 결과 해석과 흔한 실수를 회수한다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: 실습 — 잔여석 계산·예매 판정 모듈화의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- 이 레슨은 주로 프로젝트 커리큘럼/실습 맥락을 따른다. 관련 키워드가 추가되면 ABAP_DOCU sitemap에서 문서명을 확인한다.


## 6. 이 챕터를 감탄나는 자료로 만들기 위한 마무리 체크

- 각 레슨 도입부는 "무엇을 배운다"가 아니라 "지금 무엇이 불편한가"로 시작한다.
- 레슨 끝에는 다음 레슨이 진짜 해결책일 때만 pain -> solution으로 연결한다. 병렬/심화 관계는 억지 불편을 만들지 않는다.
- ABAP 고유 용어는 영어 원문을 살리고 괄호로 쉬운 풀이를 붙인다.
- 코드 예제는 짧은 한 줄 설명, 실행 전 예상, 실행 후 결과 해석, 실패 케이스 순서로 닫는다.
- 공식 문서는 로컬 ABAP_DOCU 파일명으로 재확인하고, 예제는 프로젝트의 관통 예제(구구단, SFLIGHT, 콘서트 예매, 정훈영)를 우선 사용한다.
