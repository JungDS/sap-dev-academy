# CH18 · Modern ABAP Syntax — codex_0625 강의 개선·보강본

> 생성 시각: 2026-06-25-13:22 KST  
> 기준 소스: `content/abap/CH18/_chapter.md` + 해당 챕터 레슨 7개  
> 목적: 원본 `content/abap`을 덮어쓰지 않고, 이후 리빌드/강의자료 작성에 바로 사용할 수 있는 고밀도 보강안을 제공한다.

## 1. 프로젝트 분석 기준

- 학습 대상: 개발·전공 경험 없는 입문자. 설명은 압축하지 않고 "왜 필요한가 -> 무엇인가 -> 어떻게 확인하는가 -> 실수/주의 -> 정리" 순서로 풀어쓴다.
- R15 게이팅: 이미 배운 것만으로 이해할 수 있어야 한다. 후속 개념은 L1 예고까지만 허용하고, 코드나 정의를 앞당기지 않는다.
- R2 코드=체험: 코드가 1줄이라도 있으면 같은 페이지에서 조작하거나, 최소한 어떤 시뮬레이터/다이어그램/버튼 흐름으로 체험할지 명시한다.
- 참고 우선순위: 로컬 `content/abap`과 `.project-docs`가 NotebookLM보다 최신이다. NotebookLM은 큰 방향 참고로만 사용한다.

## 2. 이 챕터의 강의 목표

classic 문법을 이해한 뒤 modern syntax로 같은 의도를 더 짧게 쓴다.

**반드시 강화할 지점**
- Modern은 대체가 아니라 리팩터링 도구라는 태도 유지
- inline DATA, VALUE, CORRESPONDING, Table Expression, String Template을 전후 비교
- NEW 객체 생성은 OO 본격 범위와 충돌하지 않게 통제
- 가독성과 릴리스 호환성 기준을 함께 다룸

**대표 체험 설계**
- classic 코드와 modern 코드를 줄 단위로 연결해 어떤 의도가 줄었는지 hover로 비교한다.

**이전/다음 연결**
- 이전: CH17 · Grid ALV 기초
- 다음: CH19 · New Open SQL / Modern ABAP SQL

## 3. 원본 진단 요약

| 항목 | 값 |
|---|---:|
| 레슨 수 | 7 |
| 본문 총 글자(공백 제외) | 5585 |
| 코드 블록 | 40 |
| embed 지시문 | 2 |
| 용어 마킹 | 1 |
| 진단 플래그 총합 | 14 |

| 레슨 | 제목 | 본문자 | 코드 | embed | 진단 |
|---|---|---:|---:|---:|---|
| CH18-L01 | Inline Declaration | 835 | 4 | 0 | R2-체험누락, 본문빈약 |
| CH18-L02 | VALUE Constructor Expression | 1300 | 10 | 0 | R2-체험누락 |
| CH18-L03 | CORRESPONDING과 구조 매핑 | 598 | 6 | 0 | R2-체험누락, 본문빈약, 흐름압축 |
| CH18-L04 | Table Expression | 674 | 4 | 0 | R2-체험누락, 본문빈약, 흐름압축 |
| CH18-L05 | String Template과 내장 함수 | 639 | 6 | 0 | R2-체험누락, 본문빈약 |
| CH18-L06 | Legacy 코드의 Modern ABAP 리팩터링 | 678 | 4 | 1 | 본문빈약 |
| CH18-L07 | 실습 — 콘서트앱 모던 리팩터 | 861 | 6 | 1 | 본문빈약, 흐름압축 |

## 4. 게이팅·품질 리스크

- Modern Syntax 첫 도입 장이다. classic 대비 없이 새 문법만 던지면 R15의 "왜 지금 배우는가"가 무너진다.
- 코드가 있으나 embed가 없는 레슨이 있다. 본문 보강 시 코드 주변에 체험/시뮬레이터 또는 최소한 조작형 퀴즈 설명을 붙여야 한다.
- 본문이 짧은 레슨이 있다. 왜 필요한가, 실수하면 무엇이 깨지는가, 어디서 확인하는가를 별도 섹션으로 늘린다.

## 5. 레슨별 개선·보강안

### CH18-L01 · Inline Declaration

**원본 신호**
- 파일: `content/abap/CH18/CH18-L01.md`
- 방향: 선언과 사용을 한 줄로 — DATA() 인라인 선언.
- 키워드: inline, DATA(), FINAL(), Modern
- introduces: 인라인 선언 DATA(), FINAL()
- prereq: CH17
- 진단: 본문 835자 · 섹션 5개 · 코드 4개 · embed 0개 · R2-체험누락, 본문빈약

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 Inline Declaration를 제시한다.
- 핵심 설명: 값, 타입, 이름, 재사용 범위를 각각 다른 색 카드로 분리한다. 초보자가 "변수명=값"과 "타입=규칙"을 섞지 않게 한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 필요 학습수단: 메모리 상태 그리드. 변수/구조체/내부 테이블의 값이 실행 단계마다 어떻게 바뀌는지 스냅샷으로 보여준다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: Inline Declaration의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- `C:/ABAP_DOCU_HTML/abapdata.htm`
- `C:/ABAP_DOCU_HTML/abapdata_simple.htm`
- `C:/ABAP_DOCU_HTML/abapdata_referring.htm`
- `C:/ABAP_DOCU_HTML/abaptypes.htm`

### CH18-L02 · VALUE Constructor Expression

**원본 신호**
- 파일: `content/abap/CH18/CH18-L02.md`
- 방향: 구조체·내부 테이블을 한 식으로 채운다 — VALUE #( ).
- 키워드: VALUE #(), Constructor, Internal Table, FOR
- introduces: VALUE #( ), 테이블 리터럴, BASE, FOR 반복 생성(table comprehension)
- prereq: CH18-L01, CH06
- 진단: 본문 1300자 · 섹션 6개 · 코드 10개 · embed 0개 · R2-체험누락

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 VALUE Constructor Expression를 제시한다.
- 핵심 설명: 행이 늘고 줄고 정렬되는 상태 변화를 눈으로 보여준다. 문법보다 "몇 행이 있고 어떤 키로 찾는가"를 먼저 묻는다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 필요 학습수단: 메모리 상태 그리드. 변수/구조체/내부 테이블의 값이 실행 단계마다 어떻게 바뀌는지 스냅샷으로 보여준다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: VALUE Constructor Expression의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- `C:/ABAP_DOCU_HTML/abapif.htm`
- `C:/ABAP_DOCU_HTML/abapcase.htm`
- `C:/ABAP_DOCU_HTML/abapdo.htm`
- `C:/ABAP_DOCU_HTML/abapwhile.htm`
- `C:/ABAP_DOCU_HTML/abaploop_at_itab.htm`
- `C:/ABAP_DOCU_HTML/abenitab.htm`

### CH18-L03 · CORRESPONDING과 구조 매핑

**원본 신호**
- 파일: `content/abap/CH18/CH18-L03.md`
- 방향: 이름이 같은 필드끼리 한 식으로 옮긴다 — CORRESPONDING #( ).
- 키워드: CORRESPONDING #(), MAPPING, EXCEPT
- introduces: CORRESPONDING #( ), MAPPING, EXCEPT
- prereq: CH18-L02, CH05
- 진단: 본문 598자 · 섹션 3개 · 코드 6개 · embed 0개 · R2-체험누락, 본문빈약, 흐름압축

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 CORRESPONDING과 구조 매핑를 제시한다.
- 핵심 설명: 같은 classic 코드를 먼저 읽게 한 뒤 modern 코드가 줄인 중복만 표시한다. "새 문법이 더 낫다"가 아니라 "의도가 더 선명하다"로 설명한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 필요 학습수단: 코드 실행 스텝퍼. 한 줄씩 실행하면서 입력값, 중간값, 출력값을 나란히 보여준다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: CORRESPONDING과 구조 매핑의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- 이 레슨은 주로 프로젝트 커리큘럼/실습 맥락을 따른다. 관련 키워드가 추가되면 ABAP_DOCU sitemap에서 문서명을 확인한다.

### CH18-L04 · Table Expression

**원본 신호**
- 파일: `content/abap/CH18/CH18-L04.md`
- 방향: 테이블 한 행을 대괄호로 콕 집는다 — tab[ ].
- 키워드: Table Expression, line_exists, line_index
- introduces: Table Expression tab[ ], line_exists( ), line_index( )
- prereq: CH18-L01, CH06
- 진단: 본문 674자 · 섹션 3개 · 코드 4개 · embed 0개 · R2-체험누락, 본문빈약, 흐름압축

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 Table Expression를 제시한다.
- 핵심 설명: 같은 classic 코드를 먼저 읽게 한 뒤 modern 코드가 줄인 중복만 표시한다. "새 문법이 더 낫다"가 아니라 "의도가 더 선명하다"로 설명한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 필요 학습수단: 코드 실행 스텝퍼. 한 줄씩 실행하면서 입력값, 중간값, 출력값을 나란히 보여준다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: Table Expression의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- 이 레슨은 주로 프로젝트 커리큘럼/실습 맥락을 따른다. 관련 키워드가 추가되면 ABAP_DOCU sitemap에서 문서명을 확인한다.

### CH18-L05 · String Template과 내장 함수

**원본 신호**
- 파일: `content/abap/CH18/CH18-L05.md`
- 방향: 문자열을 우아하게 — | … | 템플릿과 함수형 문자열 함수.
- 키워드: String Template, | |, to_upper, substring
- introduces: String Template |…|, { } 임베딩, 함수형 문자열 함수
- prereq: CH18-L01, CH04
- 진단: 본문 639자 · 섹션 4개 · 코드 6개 · embed 0개 · R2-체험누락, 본문빈약

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 String Template과 내장 함수를 제시한다.
- 핵심 설명: 실행 전 예측 -> 한 줄 실행 -> 변수 변화 확인 -> 왜 그렇게 됐는지 설명하는 디버거 루프를 고정한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 필요 학습수단: 코드 실행 스텝퍼. 한 줄씩 실행하면서 입력값, 중간값, 출력값을 나란히 보여준다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: String Template과 내장 함수의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- 이 레슨은 주로 프로젝트 커리큘럼/실습 맥락을 따른다. 관련 키워드가 추가되면 ABAP_DOCU sitemap에서 문서명을 확인한다.

### CH18-L06 · Legacy 코드의 Modern ABAP 리팩터링

**원본 신호**
- 파일: `content/abap/CH18/CH18-L06.md`
- 방향: classic 코드를 모던으로 — 짧고 안전하게, += 까지.
- 키워드: 리팩터링, +=, Modern, before-after
- introduces: 복합 대입 += -= *= /=, classic→modern 리팩터링
- prereq: CH18-L01, CH18-L02
- 진단: 본문 678자 · 섹션 4개 · 코드 4개 · embed 1개 · 본문빈약

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 Legacy 코드의 Modern ABAP 리팩터링를 제시한다.
- 핵심 설명: 같은 classic 코드를 먼저 읽게 한 뒤 modern 코드가 줄인 중복만 표시한다. "새 문법이 더 낫다"가 아니라 "의도가 더 선명하다"로 설명한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 기존 위젯 `CH18-L06-S01`을 중심에 둔다. 본문은 위젯 앞에서 조작 목표를 3단계로 제시하고, 위젯 뒤에서 결과 해석과 흔한 실수를 회수한다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: Legacy 코드의 Modern ABAP 리팩터링의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- 이 레슨은 주로 프로젝트 커리큘럼/실습 맥락을 따른다. 관련 키워드가 추가되면 ABAP_DOCU sitemap에서 문서명을 확인한다.

### CH18-L07 · 실습 — 콘서트앱 모던 리팩터

**원본 신호**
- 파일: `content/abap/CH18/CH18-L07.md`
- 방향: 콘서트앱 — classic 코드를 모던 ABAP으로 다듬는다.
- 키워드: 실습, 콘서트앱, 모던리팩터, 인라인, VALUE
- introduces: 미선언
- prereq: CH18-L01, CH18-L02, CH10-L07
- 진단: 본문 861자 · 섹션 3개 · 코드 6개 · embed 1개 · 본문빈약, 흐름압축

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 실습 — 콘서트앱 모던 리팩터를 제시한다.
- 핵심 설명: 같은 classic 코드를 먼저 읽게 한 뒤 modern 코드가 줄인 중복만 표시한다. "새 문법이 더 낫다"가 아니라 "의도가 더 선명하다"로 설명한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 기존 위젯 `CH18-L07-S01`을 중심에 둔다. 본문은 위젯 앞에서 조작 목표를 3단계로 제시하고, 위젯 뒤에서 결과 해석과 흔한 실수를 회수한다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: 실습 — 콘서트앱 모던 리팩터의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- 이 레슨은 주로 프로젝트 커리큘럼/실습 맥락을 따른다. 관련 키워드가 추가되면 ABAP_DOCU sitemap에서 문서명을 확인한다.


## 6. 이 챕터를 감탄나는 자료로 만들기 위한 마무리 체크

- 각 레슨 도입부는 "무엇을 배운다"가 아니라 "지금 무엇이 불편한가"로 시작한다.
- 레슨 끝에는 다음 레슨이 진짜 해결책일 때만 pain -> solution으로 연결한다. 병렬/심화 관계는 억지 불편을 만들지 않는다.
- ABAP 고유 용어는 영어 원문을 살리고 괄호로 쉬운 풀이를 붙인다.
- 코드 예제는 짧은 한 줄 설명, 실행 전 예상, 실행 후 결과 해석, 실패 케이스 순서로 닫는다.
- 공식 문서는 로컬 ABAP_DOCU 파일명으로 재확인하고, 예제는 프로젝트의 관통 예제(구구단, SFLIGHT, 콘서트 예매, 정훈영)를 우선 사용한다.
