# CH24 · 실무 데이터 변경과 트랜잭션 제어 — codex_0625 강의 개선·보강본

> 생성 시각: 2026-06-25-13:22 KST  
> 기준 소스: `content/abap/CH24/_chapter.md` + 해당 챕터 레슨 5개  
> 목적: 원본 `content/abap`을 덮어쓰지 않고, 이후 리빌드/강의자료 작성에 바로 사용할 수 있는 고밀도 보강안을 제공한다.

## 1. 프로젝트 분석 기준

- 학습 대상: 개발·전공 경험 없는 입문자. 설명은 압축하지 않고 "왜 필요한가 -> 무엇인가 -> 어떻게 확인하는가 -> 실수/주의 -> 정리" 순서로 풀어쓴다.
- R15 게이팅: 이미 배운 것만으로 이해할 수 있어야 한다. 후속 개념은 L1 예고까지만 허용하고, 코드나 정의를 앞당기지 않는다.
- R2 코드=체험: 코드가 1줄이라도 있으면 같은 페이지에서 조작하거나, 최소한 어떤 시뮬레이터/다이어그램/버튼 흐름으로 체험할지 명시한다.
- 참고 우선순위: 로컬 `content/abap`과 `.project-docs`가 NotebookLM보다 최신이다. NotebookLM은 큰 방향 참고로만 사용한다.

## 2. 이 챕터의 강의 목표

읽기 전용 세계를 넘어 DB 변경과 트랜잭션 책임을 배운다.

**반드시 강화할 지점**
- INSERT/UPDATE/MODIFY/DELETE의 의도 차이를 실패 케이스와 함께 설명
- COMMIT/ROLLBACK을 버튼 하나가 아니라 LUW 경계로 체득
- 표준 테이블 직접 변경 금지와 BAPI/API 기준을 명시
- 오류 로그/재처리/패키지 처리까지 운영 기준 포함

**대표 체험 설계**
- 성공, 중간 실패, rollback, 재처리 큐를 타임라인으로 조작하는 DML 플레이그라운드를 둔다.

**이전/다음 연결**
- 이전: CH23 · RAP / ABAP Cloud 입문
- 다음: CH25 · Lock Object와 동시성 제어

## 3. 원본 진단 요약

| 항목 | 값 |
|---|---:|
| 레슨 수 | 5 |
| 본문 총 글자(공백 제외) | 6834 |
| 코드 블록 | 14 |
| embed 지시문 | 5 |
| 용어 마킹 | 6 |
| 진단 플래그 총합 | 1 |

| 레슨 | 제목 | 본문자 | 코드 | embed | 진단 |
|---|---|---:|---:|---:|---|
| CH24-L01 | INSERT / UPDATE / MODIFY / DELETE 실무 기준 | 2073 | 6 | 1 | OK |
| CH24-L02 | COMMIT WORK / ROLLBACK WORK | 1246 | 2 | 1 | OK |
| CH24-L03 | DB LUW와 SAP LUW 차이 | 1224 | 2 | 1 | OK |
| CH24-L04 | 오류 로그와 재처리 구조 | 989 | 2 | 1 | 본문주의 |
| CH24-L05 | 대량 변경 시 Package 처리 | 1302 | 2 | 1 | OK |

## 4. 게이팅·품질 리스크

- Track-2 실무 구간이다. 성공 예제보다 오류, 재처리, 운영 로그, 권한, 이송 리스크를 함께 다룬다.

## 5. 레슨별 개선·보강안

### CH24-L01 · INSERT / UPDATE / MODIFY / DELETE 실무 기준

**원본 신호**
- 파일: `content/abap/CH24/CH24-L01.md`
- 방향: 직접 DB를 바꾼다 — 네 가지 DML과 감사필드.
- 키워드: INSERT, UPDATE, MODIFY, DELETE, 감사필드
- introduces: INSERT/UPDATE/MODIFY/DELETE, FROM TABLE 통째 변경, 감사필드 stamp, 표준테이블 BAPI 규율
- prereq: CH08, CH06
- 진단: 본문 2073자 · 섹션 7개 · 코드 6개 · embed 1개 · 중대 플래그 없음

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 INSERT / UPDATE / MODIFY / DELETE 실무 기준를 제시한다.
- 핵심 설명: 실행 전 예측 -> 한 줄 실행 -> 변수 변화 확인 -> 왜 그렇게 됐는지 설명하는 디버거 루프를 고정한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 기존 위젯 `CH24-L01-S01`을 중심에 둔다. 본문은 위젯 앞에서 조작 목표를 3단계로 제시하고, 위젯 뒤에서 결과 해석과 흔한 실수를 회수한다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: INSERT / UPDATE / MODIFY / DELETE 실무 기준의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- `C:/ABAP_DOCU_HTML/abapparameters.htm`
- `C:/ABAP_DOCU_HTML/abapselect-options.htm`
- `C:/ABAP_DOCU_HTML/abapselection-screen_definition.htm`
- `C:/ABAP_DOCU_HTML/abapat_selection-screen_events.htm`
- `C:/ABAP_DOCU_HTML/abapcommit.htm`
- `C:/ABAP_DOCU_HTML/abaprollback.htm`

### CH24-L02 · COMMIT WORK / ROLLBACK WORK

**원본 신호**
- 파일: `content/abap/CH24/CH24-L02.md`
- 방향: 변경을 확정하거나 되돌린다 — 트랜잭션 제어.
- 키워드: COMMIT WORK, ROLLBACK WORK, 트랜잭션
- introduces: COMMIT WORK, ROLLBACK WORK
- prereq: CH24-L01
- 진단: 본문 1246자 · 섹션 5개 · 코드 2개 · embed 1개 · 중대 플래그 없음

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 COMMIT WORK / ROLLBACK WORK를 제시한다.
- 핵심 설명: 성공 코드보다 실패했을 때 어디까지 되돌릴 수 있는지를 먼저 묻는다. 트랜잭션 경계를 색으로 표시한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 기존 위젯 `CH24-L02-S01`을 중심에 둔다. 본문은 위젯 앞에서 조작 목표를 3단계로 제시하고, 위젯 뒤에서 결과 해석과 흔한 실수를 회수한다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: COMMIT WORK / ROLLBACK WORK의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- `C:/ABAP_DOCU_HTML/abapcommit.htm`
- `C:/ABAP_DOCU_HTML/abaprollback.htm`
- `C:/ABAP_DOCU_HTML/abapinsert_dbtab.htm`
- `C:/ABAP_DOCU_HTML/abapupdate.htm`
- `C:/ABAP_DOCU_HTML/abapdelete_dbtab.htm`

### CH24-L03 · DB LUW와 SAP LUW 차이

**원본 신호**
- 파일: `content/abap/CH24/CH24-L03.md`
- 방향: 트랜잭션의 두 단위 — DB LUW와 SAP LUW.
- 키워드: LUW, SAP LUW, Update Module, CALL FUNCTION IN UPDATE TASK
- introduces: DB LUW vs SAP LUW, Update Module(IN UPDATE TASK)
- prereq: CH24-L02
- 진단: 본문 1224자 · 섹션 4개 · 코드 2개 · embed 1개 · 중대 플래그 없음

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 DB LUW와 SAP LUW 차이를 제시한다.
- 핵심 설명: 긴 코드를 함수 상자로 접어 넣는 느낌을 준다. 입력, 출력, 바뀌는 값, 사라지는 지역 변수를 화살표로 분리한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 기존 위젯 `CH24-L03-S01`을 중심에 둔다. 본문은 위젯 앞에서 조작 목표를 3단계로 제시하고, 위젯 뒤에서 결과 해석과 흔한 실수를 회수한다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: DB LUW와 SAP LUW 차이의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- `C:/ABAP_DOCU_HTML/abapcommit.htm`
- `C:/ABAP_DOCU_HTML/abaprollback.htm`
- `C:/ABAP_DOCU_HTML/abapinsert_dbtab.htm`
- `C:/ABAP_DOCU_HTML/abapupdate.htm`
- `C:/ABAP_DOCU_HTML/abapdelete_dbtab.htm`

### CH24-L04 · 오류 로그와 재처리 구조

**원본 신호**
- 파일: `content/abap/CH24/CH24-L04.md`
- 방향: 변경이 실패했을 때 — 기록하고 다시 처리한다.
- 키워드: 오류 로그, 재처리, BAL, sy-subrc
- introduces: 변경 오류 로깅, 재처리 패턴
- prereq: CH24-L02
- 진단: 본문 989자 · 섹션 5개 · 코드 2개 · embed 1개 · 본문주의

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 오류 로그와 재처리 구조를 제시한다.
- 핵심 설명: 읽기 전용 세계를 넘어 DB 변경과 트랜잭션 책임을 배운다. 이 레슨은 그 큰 흐름 안에서 "왜 필요한가 -> 무엇인가 -> 어떻게 확인하는가 -> 실수하면 어떻게 보이는가" 순서로 다시 풀어야 한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 기존 위젯 `CH24-L04-S01`을 중심에 둔다. 본문은 위젯 앞에서 조작 목표를 3단계로 제시하고, 위젯 뒤에서 결과 해석과 흔한 실수를 회수한다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: 오류 로그와 재처리 구조의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- `C:/ABAP_DOCU_HTML/abapmessage.htm`
- `C:/ABAP_DOCU_HTML/abapmessage_msg.htm`
- `C:/ABAP_DOCU_HTML/abaptry.htm`
- `C:/ABAP_DOCU_HTML/abapraise_exception.htm`

### CH24-L05 · 대량 변경 시 Package 처리

**원본 신호**
- 파일: `content/abap/CH24/CH24-L05.md`
- 방향: 수십만 건을 나눠서 — 패키지 단위 COMMIT.
- 키워드: Package, 대량처리, COMMIT, 메모리
- introduces: 패키지 단위 처리, 주기적 COMMIT
- prereq: CH24-L02
- 진단: 본문 1302자 · 섹션 5개 · 코드 2개 · embed 1개 · 중대 플래그 없음

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 대량 변경 시 Package 처리를 제시한다.
- 핵심 설명: 성공 코드보다 실패했을 때 어디까지 되돌릴 수 있는지를 먼저 묻는다. 트랜잭션 경계를 색으로 표시한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 기존 위젯 `CH24-L05-S01`을 중심에 둔다. 본문은 위젯 앞에서 조작 목표를 3단계로 제시하고, 위젯 뒤에서 결과 해석과 흔한 실수를 회수한다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: 대량 변경 시 Package 처리의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- `C:/ABAP_DOCU_HTML/abapcommit.htm`
- `C:/ABAP_DOCU_HTML/abaprollback.htm`
- `C:/ABAP_DOCU_HTML/abapinsert_dbtab.htm`
- `C:/ABAP_DOCU_HTML/abapupdate.htm`
- `C:/ABAP_DOCU_HTML/abapdelete_dbtab.htm`


## 6. 이 챕터를 감탄나는 자료로 만들기 위한 마무리 체크

- 각 레슨 도입부는 "무엇을 배운다"가 아니라 "지금 무엇이 불편한가"로 시작한다.
- 레슨 끝에는 다음 레슨이 진짜 해결책일 때만 pain -> solution으로 연결한다. 병렬/심화 관계는 억지 불편을 만들지 않는다.
- ABAP 고유 용어는 영어 원문을 살리고 괄호로 쉬운 풀이를 붙인다.
- 코드 예제는 짧은 한 줄 설명, 실행 전 예상, 실행 후 결과 해석, 실패 케이스 순서로 닫는다.
- 공식 문서는 로컬 ABAP_DOCU 파일명으로 재확인하고, 예제는 프로젝트의 관통 예제(구구단, SFLIGHT, 콘서트 예매, 정훈영)를 우선 사용한다.
