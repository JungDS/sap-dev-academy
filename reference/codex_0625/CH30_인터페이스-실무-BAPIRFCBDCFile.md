# CH30 · 인터페이스 실무: BAPI/RFC/BDC/File — codex_0625 강의 개선·보강본

> 생성 시각: 2026-06-25-13:22 KST  
> 기준 소스: `content/abap/CH30/_chapter.md` + 해당 챕터 레슨 5개  
> 목적: 원본 `content/abap`을 덮어쓰지 않고, 이후 리빌드/강의자료 작성에 바로 사용할 수 있는 고밀도 보강안을 제공한다.

## 1. 프로젝트 분석 기준

- 학습 대상: 개발·전공 경험 없는 입문자. 설명은 압축하지 않고 "왜 필요한가 -> 무엇인가 -> 어떻게 확인하는가 -> 실수/주의 -> 정리" 순서로 풀어쓴다.
- R15 게이팅: 이미 배운 것만으로 이해할 수 있어야 한다. 후속 개념은 L1 예고까지만 허용하고, 코드나 정의를 앞당기지 않는다.
- R2 코드=체험: 코드가 1줄이라도 있으면 같은 페이지에서 조작하거나, 최소한 어떤 시뮬레이터/다이어그램/버튼 흐름으로 체험할지 명시한다.
- 참고 우선순위: 로컬 `content/abap`과 `.project-docs`가 NotebookLM보다 최신이다. NotebookLM은 큰 방향 참고로만 사용한다.

## 2. 이 챕터의 강의 목표

외부/내부 시스템과 데이터를 주고받는 인터페이스 기술을 다룬다.

**반드시 강화할 지점**
- BAPI/RFC/BDC/File/Excel의 목적과 실패 복구 기준을 분리
- BAPIRET2 메시지 처리를 성공보다 중요하게 다룸
- RFC destination과 권한/네트워크 실패를 운영 관점으로 설명
- BDC는 레거시 대응 수단이며 남용 금지

**대표 체험 설계**
- 호출 요청, 반환 메시지, 재처리 큐를 연결한 인터페이스 플로우를 보여준다.

**이전/다음 연결**
- 이전: CH29 · Enhancement / BAdI / User Exit
- 다음: CH31 · IDoc / ALE / Gateway

## 3. 원본 진단 요약

| 항목 | 값 |
|---|---:|
| 레슨 수 | 5 |
| 본문 총 글자(공백 제외) | 4792 |
| 코드 블록 | 12 |
| embed 지시문 | 5 |
| 용어 마킹 | 3 |
| 진단 플래그 총합 | 7 |

| 레슨 | 제목 | 본문자 | 코드 | embed | 진단 |
|---|---|---:|---:|---:|---|
| CH30-L01 | BAPI 호출과 Return 처리 | 799 | 2 | 1 | 본문빈약, 흐름압축 |
| CH30-L02 | RFC Function Module 설계 | 824 | 2 | 1 | 본문빈약, 흐름압축 |
| CH30-L03 | BDC / Batch Input 실무 기준 | 1206 | 4 | 1 | OK |
| CH30-L04 | Excel Upload 처리 | 779 | 2 | 1 | 본문빈약, 흐름압축 |
| CH30-L05 | File Interface와 재처리 | 1184 | 2 | 1 | 본문주의 |

## 4. 게이팅·품질 리스크

- Track-2 실무 구간이다. 성공 예제보다 오류, 재처리, 운영 로그, 권한, 이송 리스크를 함께 다룬다.
- 본문이 짧은 레슨이 있다. 왜 필요한가, 실수하면 무엇이 깨지는가, 어디서 확인하는가를 별도 섹션으로 늘린다.

## 5. 레슨별 개선·보강안

### CH30-L01 · BAPI 호출과 Return 처리

**원본 신호**
- 파일: `content/abap/CH30/CH30-L01.md`
- 방향: 표준 데이터는 BAPI로 — 호출과 결과 처리.
- 키워드: BAPI, BAPIRET2, COMMIT, 표준 API
- introduces: BAPI 호출, BAPIRET2 처리, BAPI_TRANSACTION_COMMIT
- prereq: CH10, CH24
- 진단: 본문 799자 · 섹션 3개 · 코드 2개 · embed 1개 · 본문빈약, 흐름압축

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 BAPI 호출과 Return 처리를 제시한다.
- 핵심 설명: RAP는 파일 이름 암기가 아니라 비즈니스 객체의 생명주기다. 생성, 검증, 자동계산, 저장, 서비스 노출을 한 타임라인에 둔다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 기존 위젯 `CH30-L01-S01`을 중심에 둔다. 본문은 위젯 앞에서 조작 목표를 3단계로 제시하고, 위젯 뒤에서 결과 해석과 흔한 실수를 회수한다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: BAPI 호출과 Return 처리의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- `C:/ABAP_DOCU_HTML/abapcommit.htm`
- `C:/ABAP_DOCU_HTML/abaprollback.htm`
- `C:/ABAP_DOCU_HTML/abapinsert_dbtab.htm`
- `C:/ABAP_DOCU_HTML/abapupdate.htm`
- `C:/ABAP_DOCU_HTML/abapdelete_dbtab.htm`

### CH30-L02 · RFC Function Module 설계

**원본 신호**
- 파일: `content/abap/CH30/CH30-L02.md`
- 방향: 외부에서 호출 가능한 함수 — Remote-Enabled FM.
- 키워드: RFC, Remote-Enabled, SM59, 인터페이스
- introduces: RFC Function Module, Remote-Enabled
- prereq: CH10, CH30-L01
- 진단: 본문 824자 · 섹션 3개 · 코드 2개 · embed 1개 · 본문빈약, 흐름압축

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 RFC Function Module 설계를 제시한다.
- 핵심 설명: 긴 코드를 함수 상자로 접어 넣는 느낌을 준다. 입력, 출력, 바뀌는 값, 사라지는 지역 변수를 화살표로 분리한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 기존 위젯 `CH30-L02-S01`을 중심에 둔다. 본문은 위젯 앞에서 조작 목표를 3단계로 제시하고, 위젯 뒤에서 결과 해석과 흔한 실수를 회수한다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: RFC Function Module 설계의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- 이 레슨은 주로 프로젝트 커리큘럼/실습 맥락을 따른다. 관련 키워드가 추가되면 ABAP_DOCU sitemap에서 문서명을 확인한다.

### CH30-L03 · BDC / Batch Input 실무 기준

**원본 신호**
- 파일: `content/abap/CH30/CH30-L03.md`
- 방향: 화면을 자동 입력해 대량 등록한다.
- 키워드: BDC, Batch Input, CALL TRANSACTION, BDCDATA
- introduces: BDC/Batch Input, CALL TRANSACTION, Session Method(BDC_OPEN_GROUP), SHDB Recorder
- prereq: CH16, CH30-L01
- 진단: 본문 1206자 · 섹션 5개 · 코드 4개 · embed 1개 · 중대 플래그 없음

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 BDC / Batch Input 실무 기준를 제시한다.
- 핵심 설명: RAP는 파일 이름 암기가 아니라 비즈니스 객체의 생명주기다. 생성, 검증, 자동계산, 저장, 서비스 노출을 한 타임라인에 둔다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 기존 위젯 `CH30-L03-S01`을 중심에 둔다. 본문은 위젯 앞에서 조작 목표를 3단계로 제시하고, 위젯 뒤에서 결과 해석과 흔한 실수를 회수한다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: BDC / Batch Input 실무 기준의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- `C:/ABAP_DOCU_HTML/abapparameters.htm`
- `C:/ABAP_DOCU_HTML/abapselect-options.htm`
- `C:/ABAP_DOCU_HTML/abapselection-screen_definition.htm`
- `C:/ABAP_DOCU_HTML/abapat_selection-screen_events.htm`
- `C:/ABAP_DOCU_HTML/abapclass.htm`
- `C:/ABAP_DOCU_HTML/abapmethods.htm`

### CH30-L04 · Excel Upload 처리

**원본 신호**
- 파일: `content/abap/CH30/CH30-L04.md`
- 방향: 엑셀 파일을 읽어 내부 테이블로.
- 키워드: Excel, Upload, GUI_UPLOAD, 파일
- introduces: 엑셀/파일 업로드
- prereq: CH06, CH30-L01
- 진단: 본문 779자 · 섹션 3개 · 코드 2개 · embed 1개 · 본문빈약, 흐름압축

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 Excel Upload 처리를 제시한다.
- 핵심 설명: 인터페이스는 "보냈다"가 끝이 아니다. 요청, 응답, 상태, 재처리, 로그를 한 묶음으로 설계하게 한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 기존 위젯 `CH30-L04-S01`을 중심에 둔다. 본문은 위젯 앞에서 조작 목표를 3단계로 제시하고, 위젯 뒤에서 결과 해석과 흔한 실수를 회수한다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: Excel Upload 처리의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- 이 레슨은 주로 프로젝트 커리큘럼/실습 맥락을 따른다. 관련 키워드가 추가되면 ABAP_DOCU sitemap에서 문서명을 확인한다.

### CH30-L05 · File Interface와 재처리

**원본 신호**
- 파일: `content/abap/CH30/CH30-L05.md`
- 방향: 파일 기반 인터페이스의 공통 — 검증·로그·재처리.
- 키워드: File Interface, 재처리, 로그, 서버 파일
- introduces: 서버 파일 입출력(OPEN DATASET), 인터페이스 재처리 구조
- prereq: CH30-L04, CH24-L04
- 진단: 본문 1184자 · 섹션 4개 · 코드 2개 · embed 1개 · 본문주의

**개선 강의안**
- 도입 불편: 학습자가 이 레슨 직전에 어떤 답답함을 느꼈는지 먼저 묻고, 그 답답함의 해결책으로 File Interface와 재처리를 제시한다.
- 핵심 설명: 인터페이스는 "보냈다"가 끝이 아니다. 요청, 응답, 상태, 재처리, 로그를 한 묶음으로 설계하게 한다.
- 초보자 보호: 새 용어는 첫 등장 한 줄 풀이로 잡고, 이번 레슨의 주제 개념은 한 줄로 끝내지 말고 필요성, 정의, 구조, 확인 방법까지 풀어쓴다.
- 실무 감각: 단순히 실행되는 예제에서 끝내지 말고, 실패했을 때 어떤 화면, sy-subrc, 메시지, 로그, 권한 문제가 나타나는지 한 가지 이상 붙인다.

**예제·시각·상호작용 학습수단 설명**
- 기존 위젯 `CH30-L05-S01`을 중심에 둔다. 본문은 위젯 앞에서 조작 목표를 3단계로 제시하고, 위젯 뒤에서 결과 해석과 흔한 실수를 회수한다.
- 버튼/상태 설계: "입력 준비" -> "실행" -> "결과 확인" -> "오류 케이스" 4단 버튼 흐름을 기본으로 하고, 결과 영역에는 학습자가 방금 바꾼 값이 무엇인지 강조한다.
- 평가 과제: File Interface와 재처리의 목적을 한 문장으로 설명하게 한다. / 정상 케이스 1개와 실패/주의 케이스 1개를 구분하게 한다. / 다음 레슨에서 왜 이 지식이 필요한지 스스로 연결하게 한다.

**공식 문서 체크 힌트**
- `C:/ABAP_DOCU_HTML/abapwrite-.htm`
- `C:/ABAP_DOCU_HTML/abapwrite_to.htm`
- `C:/ABAP_DOCU_HTML/abenwrite_formats.htm`


## 6. 이 챕터를 감탄나는 자료로 만들기 위한 마무리 체크

- 각 레슨 도입부는 "무엇을 배운다"가 아니라 "지금 무엇이 불편한가"로 시작한다.
- 레슨 끝에는 다음 레슨이 진짜 해결책일 때만 pain -> solution으로 연결한다. 병렬/심화 관계는 억지 불편을 만들지 않는다.
- ABAP 고유 용어는 영어 원문을 살리고 괄호로 쉬운 풀이를 붙인다.
- 코드 예제는 짧은 한 줄 설명, 실행 전 예상, 실행 후 결과 해석, 실패 케이스 순서로 닫는다.
- 공식 문서는 로컬 ABAP_DOCU 파일명으로 재확인하고, 예제는 프로젝트의 관통 예제(구구단, SFLIGHT, 콘서트 예매, 정훈영)를 우선 사용한다.
