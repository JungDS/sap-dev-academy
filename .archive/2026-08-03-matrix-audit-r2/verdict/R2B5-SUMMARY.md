# 배치 5 R2 재감사 요약 — CH17～CH20 (2026-08-18)

> 목적: 배치 5 보강(본문 39레슨·위젯 15종·엔진 12종 + §6 결정 4건) 결과의 실질 검증. 방식: R1 동일 조건 블라인드 재감사(R2B2～R2B4와 동일 프로토콜).
> 브랜치 `audit/r2-ch17-20` · 산출물: `raw/CH17～20`(112벌) · `merged/CH17～20` · 이 문서.
> 신규 규율 적용: AG02 발사 문구에 **`file://` 직접 열람 금지·SERVE_URL 경유 강제**(사용자 확정 2026-08-18 — CH18-opus부터 적용).
> **수정(보강)은 미실행 — 사용자 결정 대기.**

## 1. 수거·수합 통계 (R1 대비)

| 챕터 | R2 발견/클러스터 | R1 발견/클러스터 | 볼륨비 | 비고 |
|---|---|---|---|---|
| CH17 | 97 / 59 | 149 / 66 | 65% | 높음 5(재발 1 포함)·conflict 1 |
| CH18 | 124 / 69 | 171 / 84 | 73% | 치명 0·높음 4(전부 위젯/신규)·conflict 2(전부 재발 계열) |
| CH19 | 94 / 55 | 150 / 69 | 63% | 높음 1(= ⑳ 재발)·conflict 3 |
| CH20 | 91 / 54 | 126 / 64 | 72% | 높음 4 중 conflict 3(전부 기각 확정) |
| 계 | **406 / 237** | 596 / 283 | **68%** | **112/112 · 결측 0 · dropped 0** |

- **치명 0**(R1 명목 3·실질 1 → R2 명목·실질 모두 0). 등급 의견 전 챕터 '경미 수정' 중심(CH20은 '유지' 9벌).
- AG09(코드 축) findings=0 벌 다수(CH17-s·CH19-s·CH20-s) — R1 활성화 차단 계열 해소 교차 확인.

## 2. 재발 오판 — 자동 재기각 (기각 사전 위력 실증)

| 클러스터 | 주장 | 근거 |
|---|---|---|
| CH19-C001 (x7 확실) | DATS_ADD_DAYS 3인자 필수 | **⑳ 그대로 재발**(R1 x7 → R2 x7 — 최대 재발) |
| CH17-C003 (x4) | LEFT OUTER JOIN ON `<>`·리터럴 불가 | ⑱ 재발(7.40 해제) |
| CH18-C037 (x4 conflict) | line_index -1 경로 없음 | ㉔ 재발(hash key → -1 명문) |
| CH20-C002 (x3 conflict) | UNION/INTERSECT/EXCEPT DISTINCT 표기 불가 | ㉓ 계열(ABAPUNION 명문 — B5 확정 재확인) |
| CH18-C038 (conflict) | CONV i('3.7')은 예외 | B5 확정 재확인(ABENCONVERSION_TYPE_C 상업 반올림) |
| CH19-C012 (conflict) | strict 모드 INTO 후치 강제는 오류 | B5 확정 재확인(ABAPINTO_CLAUSE:115 — 본문이 옳음) |
| **CH20-C001 (x5 conflict)** | EXISTS 서브쿼리는 SELECT * 필요 | **본선 신규 확정 기각** — abenwhere_logexp_exists: SELECT 목록은 "not important … explicit lists are not useful", 리터럴 1개 권장(문법 제약 아님) |
| **CH20-C003 (x2 conflict)** | OVER 안은 ASC/DESC만 허용 | **본선 신규 확정 기각** — abapselect_over 구문도: `ORDER BY col [ASCENDING\|DESCENDING]` 명문(본문이 옳음) |

→ 상위 20클러스터 중 8건이 재발/오판 계열. **기각 사전 신규 후보 2**: ㉕ 'EXISTS 서브쿼리 SELECT * 필요' ㉖ 'OVER 내부 ASC/DESC 한정'.

## 3. 실질 신규·잔존 (수정 후보 — 착수 시 확정)

### 보강 유발·불완전 (배치 5 보강이 원인)
1. **CH17-C001 (x10 확실) — L10 골격 RETURN 논리 결함**: B5 보강이 PBO 경로 E 메시지를 S+`RETURN`으로 교체했는데, FORM(build_fieldcat) 안 RETURN은 그 FORM만 종료 — merge 실패에도 호출자(build_alv)가 layout·display를 이어 실행해 빈 fieldcat으로 표시 호출. 교정 방향: 오류 깃발(예: `gv_error`) + 호출자 분기, 또는 merge 검사 위치 재설계. L04·L07 동형 포함. **R2B5 최중량 — R2B4의 '보강 지시가 만든 결함' 계열 재현.**
2. **CH17-C002 (x7) — L09 준비 코드 과부하**: B5 보강(집계 코드로 재현 가능화)의 트레이드오프 — 색상 레슨의 본문 코드 대부분이 색과 무관한 JOIN·SUM 준비 코드가 됨. 교정 방향: 준비 코드를 `details` 접기/부록화하고 색 관련 줄을 전면에.
3. **CH18-C004 — line_exists 괄호 안 무예외 설명 갭**: B5 보강이 line_exists 보호를 강조하면서, "왜 괄호 안 table expression은 예외가 안 나는가" 설명이 챕터에 없음. 1문단 보충.

### 신규 위젯 결함 (실측 기반·확실)
4. **CH17-C005** — L05 위젯 cwidth_opt 토글이 실제 iframe 폭(801px)에서 시각 변화 0(560px 이하에서만 잘림 — table-layout:fixed 원인). 대표 학습 포인트 헛체험.
5. **CH18-C001** — L06 위젯 '12A' 입력에서 CONV '변환 불가'/EXACT '예외' 비대칭 표시 — 본문이 교정하는 오해("CONV 관대")를 위젯이 강화(convI()가 exc 플래그 미설정).
6. **CH18-C002 (x3)** — L09 LET 위젯: 잔여석 음수면 '= 0 → 매진' 가지 선택 표시 — COND 순차 검사 원칙을 반대로 체험시킴.
7. **CH18-C003 (x3)** — L02 VALUE Builder unique key 모드 오동작(빈 상태 중복 시도 시 EMPTY KEY 안내 등 모드 반대 안내).

### 잔존·구조 (착수 시 재량)
8. CH17-C004 (x3): L10 골격에 색상(L09 개념) 미통합 — 챕터 통합 완료 경험 부재(R1 최소 처리 재지적).
9. CH20-C004 (x4): L07 '통합 실습'이 독립 SELECT 4개 복습 — 통합·전이 설계 부재(R1 구조 기각군과 동일 계열).
10. CH19-C003·C007 (conflict·중간): 집계식 중첩 릴리스 의존 미언급 · FROM @itab '메모리 처리' 문구 잔존 시비 — 착수 시 코퍼스 확정 후 미세 정합.
11. CH17-C025 (conflict): LVC merge 후 coltext 단독 수정 시 헤더 우선순위(SCRTEXT_*) — 코퍼스 밖(클래스 문서 영역), 착수 시 통설 기준 헤지 서술 재량.

## 4. 본선 종합 판정(의견)

| 챕터 | R1 판정 | R2 본선 의견 | 골자 |
|---|---|---|---|
| CH17 | 보강 권장(이행 완료) | **경미 수정(필수 2)** | C001 RETURN 논리·C005 위젯 + C002 구조 조정 |
| CH18 | 〃 | **경미 수정(필수 3)** | 위젯 3건(C001～C003) + C004 설명 갭 |
| CH19 | 〃 | **유지～경미**(실질 필수 0) | 높음 1 = ⑳ 재발, 잔여는 미세 정합 |
| CH20 | 〃 | **유지～경미**(실질 필수 0) | 높음 4 중 3 기각·1 구조 사안 |

**총평: 보강 효과 뚜렷**(볼륨 68%·치명 0·CH19/CH20 실질 필수 0). 잔여 실질은 CH17 RETURN 논리(x10)와 위젯 4건.

## 5. 사용자 결정 대기

1. **수정 착수 범위** — §3 전체(1～11) vs 필수·확실(1～7)만 vs 보류.
2. 기각 사전 ㉕·㉖ 등재(§2)는 원장 갱신 때 자동 반영 예정(규칙 개정급 아님).
