# CH03 content 반영 매핑 — 매트릭스 감사 백로그

> 작업일: 2026-08-03 · 실행자: Opus (CH03 본문 보강 실행자)
> 입력: `verdict/CH03.md` · `merged/CH03.json` · 대상 `content/abap/CH03/**`(4파일)
> 범위: **content .md 4파일만.** `reference/glossary.json` 키 추가 0건(새 마킹 `[[ALV]]`·`[[Search Help]]`·`[[$TMP]]`가 전부 기존 키). embeds·docs·타 챕터·`.project-docs` 미수정. git commit 미실행(본선 몫).
> 검증: `npm run build:abap` **통과**(glossary parity 미정의 0건 · pages 270) + 생성물 정적 점검(표 5/5·3/3 렌더 · 미변환 `**`/`[[ ]]` 0 · href 실존 6/6 · iframe 3/3).

## 집계

| 처리 | 수 |
|---|---|
| 반영 | **44** |
| 위젯 작업자 몫(미착수) | **5** (C010 · C015 · C031 · C033 · C048) |
| 보류 | **0** |
| 기각(지시대로 무수정) | 3 (C001 · C002 · C024) |

채택 49 = 반영 44 + 위젯몫 5. 누락 0.
분할 3건(C008·C020·C032)은 **본문 쪽만 반영**, 위젯 쪽은 아래 각 행에 몫 표기.

---

## `_chapter.md` (1)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C014 | 반영 | front-matter `intro` + 본문 문단 | intro를 front-matter 반복 한 줄에서 **보상 + 3계층 로드맵** 4문장으로 확장(불편 → Domain/Data Element 분담 → 화면에서 회수 → 라벨·F4). 본문 문단도 같은 문장으로 동기화(빌드는 `intro`만 읽지만 사람이 보는 본문과 어긋나면 안 됨). |

## `CH03-L01.md` (21)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C003 | 반영 | `## SE11에서 직접 만들기` | 챕터 보상을 자기 산출물로 재현 가능하게 — **상태 Domain(`ZDO_STATUS`, CHAR 1, Fixed Values O/C) 추가 생성**을 실습 문단으로 명시(위젯 '상태' 칩이 그대로 목표임을 연결) + L03에서 다시 쓴다는 링크. `## 정리`에 산출물 2개(`ZDO_AMOUNT`·`ZDO_STATUS`) 명시. |
| C007 | 반영 | `## SE11에서 직접 만들기` 💬 콜아웃 | 금액 DEC 8,2 관통 예제에 캐비앗 1개 — 실무 금액은 통화별 소수 자릿수 때문에 **통화 키를 짝으로 두는 전용 타입**을 쓴다는 예고 수준(타입명·코드·CURR 문법 미노출). front-matter `foreshadow` 등재. |
| C013 | 반영 | `## 활성화 — DDIC의 철칙` | '런타임 객체' 한 줄 풀이 2문장(시스템이 실행에 쓰려고 내부에 만드는 완성본 · SE11 정의=설계도, 런타임 객체=찍어 낸 실물). |
| C016 | 반영 | 도입 😵 콜아웃 | `DEC 8,2` 첫 등장 자리에 즉석 괄호 풀이("십진수 8자리, 그중 소수가 2자리" + "곧 배운다") 삽입. |
| C017 | 반영 | `## 값을 제한하는 두 가지` 💡 콜아웃 | **사실 관계는 유지**(C001 기각 준수) 하되 미학습 코드 표기 `VALUE CHECK` 를 제거하고 "입력 검증에도 쓰인다 + 켜는 방법은 이 챕터 마지막 레슨" 예고형으로 전환. 비교표 '이어짐' 행도 (뒤 챕터) → (이 챕터 마지막 레슨)으로 정정. front-matter `foreshadow`에 "VALUE CHECK 입력 검증(CH03-L03)" 등재. |
| C018 | 반영 | `## Domain의 역할` 🧭 콜아웃 신설 | 이름 충돌 각주 — 다른 분야의 "도메인"은 업무 의미 쪽이지만 DDIC Domain은 정반대로 **의미를 뺀 기술 계층**이라는 대비 1개. |
| C020 | **반영(본문 쪽)** | `## Domain의 속성` 표 + 🖱️ 칩 안내 | 속성 표에 **부호(Sign)** 행 신설(숫자 타입의 음수 허용 · NUMC는 음수 불가). 칩 안내를 실물 위젯 구성과 일치하는 **6종 전체 열거**(금액·상태·항공사·이메일·재고 증감·대기번호)로 교체. front-matter `introduces`·`goals`·`direction`에 부호 반영. ※ 위젯 쪽(칩 축소 여부) = **위젯 작업자 몫**. |
| C021 | 반영 | ⚠️ 헷갈리기 쉬운 점 | 'DDIC(도메인)에서는' → **'DDIC의 Domain에서는'**, '도메인 버전이 `CHAR`' → 'Domain 버전이 `CHAR`'. 한글 음차 '도메인' 제거로 DDIC=Domain 오독 차단. |
| C023 | 반영 | `## 값을 제한하는 두 가지` 표 아래 문단 | '다른 테이블' 한 줄 풀이(값이 줄줄이 저장된 표 · 정식은 뒤 챕터) + `SCARR` 정체 한 줄(SAP 기본 제공 연습용 항공사 목록 표). |
| C025 | 반영 | 활성화 ⚠️ 콜아웃 | **버전 공존·반영 시점** 보완 — 수정분은 새 비활성 버전으로 저장되고 기존 활성 버전은 남아, 재활성화 전까지 런타임은 예전 활성판을 계속 쓴다. |
| C028 | 반영 | 속성 표 대소문자 행 | 대문자 변환 주체를 **'화면에서 입력할 때'** 로 한정 + '코드에서 대입한 소문자는 그대로 남는다' 한 줄. |
| C030 | 반영(보수적) | — | 지시대로 **값 테이블 절 삭제·이동 없음**(`introduces` 선언 유지). 부하 완화는 C017(콜아웃 용어 정리)·C023(미학습어 풀이)·C036(스킵 안내)로 갈음. 내용 손실 0. |
| C034 | 반영 | Domain 직접 사용 ⚠️ 콜아웃 | 주술 불일치 비문("Domain은 … 보여서 주의가 필요하다")을 **두 문장으로 분리**. 단정 서술 "Domain은 항상 Data Element에만 사용된다"는 **원문 그대로 유지**(C024 기각). |
| C035 | 반영 | 절 제목·표 헤더·본문 전반 | 표기 방향을 **`Fixed Values(고정값)` / `Value Table(값 테이블)`** 영어 우선으로 통일(R3 "ABAP 고유 용어는 영어 원문" 정합). 절 제목도 동일 방향. ※ 표 셀 안에서는 `[[키\|표시]]` 파이프가 표를 깨뜨려 **평문 표기 + 산문에 마킹** 방식 사용(아래 '부수 발견' 참조). |
| C036 | 반영 | 속성 절 ⏩ + 값 제한 절 🧑‍💻 | 경력자 스킵 1줄(표 두 개만 훑고 다음 절로) + 매핑 1줄(Fixed Values≈`enum` · Value Table≈허용 값이 든 다른 표 참조). |
| C038 | 반영 | `## 흔한 실수와 점검` | 규칙 재진술 2건을 **증상 중심**으로 교체 — "CHAR↔c 혼동" → "검사에서 타입 오류가 난다", "값 테이블+고정값 동시 사용" → "허용 값을 적었는데 화면에서 아무 일도 안 일어난다". |
| C042 | 반영 | 타입 대응표 아래 📌 각주 | 키워드 대응일 뿐이라는 각주 — 길이·소수는 따로 지정(CH02 `LENGTH`·`DECIMALS` 링크), **단위 상이**(DDIC `DEC 8`=자릿수 8 vs ABAP `p LENGTH 8`=8바이트≈15자리). |
| C043 | 반영 | SE11 절 📦 콜아웃 신설 | 첫 저장 시 패키지 지정 대화상자 안내 + Local Object(`[[$TMP]]`) + [Chapter 1 · T-code와 SE38 첫 실행](CH01-L02.html) 링크(실제 $TMP 학습 레슨 grep 확인). front-matter `prereq`에 CH01-L02 추가. |
| C045 | 반영 | 속성 표 소수 행 | '(DEC 전용, …)' → **'(DEC 등 십진 타입에서 지정, …)'** 완화. |
| C049 | 반영 | ⚠️ 헷갈리기 쉬운 점 | '키워드가 `c` 다' → '키워드가 `c`이다' 조사 결합 교정. |
| C052 | 반영 | 활성화 절 🔁 콜아웃 신설 | '활성화는 껐다 켜는 스위치가 아니라 **한 번의 변환**(컴파일과 비슷)' 유추 1개. |

## `CH03-L02.md` (12)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C003 | 반영 | 코드블록 뒤 문단 | `ZDE_STATUS`(참조 `ZDO_STATUS`, 라벨 "예매 상태") 생성 한 문단 추가 — L03 위젯의 `zde_status`/'예매 상태'와 이름·라벨 일치. `## 정리`에도 산출물 2개 명시. |
| C004 | 반영 | `## 흔한 실수와 점검` | 'ALV' 첫 등장에 `[[ALV]]` 마킹 + 한 줄 풀이(ABAP List Viewer · 표 형태 출력 도구 · 뒤 챕터 정식). glossary 키 기존 존재 확인 후 마킹. front-matter `foreshadow` 등재. |
| C008 | 반영(연동) | 마무리 예고 문단 | '라벨·F4가 **공짜로** 따라온다' → '**체크 한 번으로 라벨이, Domain의 Fixed Values로 F4가** 따라온다'(L03 제목·direction 완화와 표현 정합). |
| C009 | 반영 | Parameter ID 💡 콜아웃 | 단정 완화(이름만 적어 둔다고 저절로 되지 않고 화면 쪽도 켜야 함) + **괄호 속 미학습 명칭 (SET/GET Parameter) 삭제** + "그런 속성이 있다" 수준 유지. |
| C012 | 반영 | `## SE11에서 Data Element 만들기` | 탭 진입 한 줄(**Data Type** 탭 → **Field Label** 탭) + **Field Label 4종 표 신설**(칸·최대 길이 10/20/40/55·쓰이는 곳·입력 예) + 왜 넷인지 설명 2문장. |
| C026 | 반영 | 상속 보드 임베드 직후 ⚠️ 콜아웃 신설 | Domain 변경이 참조 전수로 퍼진다는 운영 경고 + 길이 축소 시 값 잘림 + **변경 전 사용처 확인(Where-Used List)이 규율**. 심화 명칭(SE14·QCM) 미노출. |
| C027 | 반영 | 상속 보드 절 🧑‍💻 메모 | '상속'이 객체지향 클래스 상속이 아니라 **참조에 따른 속성 사용**임을 1줄로 한정(절 제목·위젯 명칭은 유지해 본문↔위젯 불일치 방지). |
| C037 | 반영 | `## 정리` 신설 | 5불릿(Data Element 정의 / Field Label 4종 / 1:N 재사용 / 변경 파장·사용처 확인 / 산출물 2개). L01 형식과 통일. |
| C040 | 반영 | Domain vs Data Element 표 아래 🧑‍💻 | 경력자 매핑 1줄 — Domain≈컬럼 **물리 타입**, Data Element≈그 위의 **의미 타입**(관심사 분리). |
| C041 | 반영 | 흔한 실수 항목 | 두 '길이' 구분(Field Label 최대 길이=**라벨 글자 수** vs Domain 길이=**값의 자릿수**)을 표 아래 📌로 명시 + 항목 제목을 증상 방향("화면에서 라벨 뒷글자가 잘린다")으로 통일. |
| C044 | 반영 | `## 한 Domain, 여러 Data Element` 첫 문단 | 번역투 재작성 — '여러 의미가 나눠 쓴다' → '**하나의 Domain을 여러 Data Element가 서로 다른 의미로** 나눠 쓴다', '예로' → 문장 분리. |
| C050 | 반영 | 첫 문단 + 재사용 절 마무리 | '기술만 안다' → '**기술 속성**만 안다', '기술을 한 번 정의' → '**기술 속성을 한 번** 정의'(2곳). 비교표 '담당' 셀도 '기술 속성'으로. |

## `CH03-L03.md` (15)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C003 | 반영 | 도입 절 + F4≠검증 콜아웃 | **"…만들었다고 가정한 Data Element 이름이다" 가정 문구 삭제**. 도입에 앞 두 레슨 산출물(`ZDO_AMOUNT`→`ZDE_AMOUNT` · `ZDO_STATUS`→`ZDE_STATUS`)을 그대로 쓴다고 명시해 자기 산출물 참조로 교체. |
| C005 | 반영 | 보상 절 🖱️ 콜아웃 신설 | Selection Text 진입 경로·조작 순서 4단계(**Goto → Text Elements → Selection Texts** → `PA_AMT` 행 → **Dictionary 참조** 체크 → 저장·활성화 후 재실행) + 직접 타이핑 시 연동이 끊긴다는 이유 1줄. |
| C006 | 반영 | 보상 절 ② + 흔한 실수 | '검색도움말' → `[[Search Help\|검색 도움말]]` 마킹 + 원어 병기 + 한 줄 풀이(F4 목록 설계 DDIC 객체 · 뒤 챕터 정식). front-matter `foreshadow` 등재. glossary 키 기존 존재. |
| C008 | **반영(본문 쪽)** | front-matter `title`·`direction` + 절 제목 | 제목 '라벨·F4 **자동 적용**' → '**이어받기**', direction의 '자동으로 붙는다' → '**체크 한 번으로 라벨이, Domain의 Fixed Values로 F4가 따라온다**', 절 제목 '공짜로 따라오는 것들' → '보상 — 라벨과 F4가 따라온다'. 본문의 사실 서술(수동 체크 1회 필요)은 유지. ※ 위젯 쪽(체크 단계 반영) = **위젯 작업자 몫**. |
| C011 | 반영 | `## PARAMETERS — 가장 단순한 입력 화면` | '선택화면' 첫 등장 정의 + **원어(Selection Screen) 병기** 3문장(실행 전에 무엇을 대상으로 할지 묻는 화면 · 이번엔 칸 하나짜리만). 이후 '입력 화면'↔'선택화면' 혼용 정리. front-matter `introduces` 표기도 갱신. |
| C019 | 반영 | 같은 절 🧭 콜아웃 신설 | PARAMETERS(선택화면 입력칸) ↔ 함수의 매개변수 ↔ 앞 레슨 Parameter ID 3자 구분 1개. |
| C022 | 반영 | 보상 절 ① | 이름 넷이 몰린 한 문장을 **①/② 구조 + 3문장**으로 분리(Selection Text가 무엇인지 → 안 하면 기술명 → 체크하면 Field Label 상속). |
| C029 | 반영 | 옵션 절 `LOWER CASE` 뒤 문단 | L01 Domain 대소문자 속성과 **같은 이야기**임을 연결 + **케이스 구분**(DE→Domain 참조 칸은 Domain 속성이 따라옴 / 표준 타입 직접 지정 칸은 선언에 `LOWER CASE`). |
| C032 | **반영(본문 쪽)** | 임베드 도입 문단 | 실험 필드가 **예매 상태 칸(`pa_stat` · `zde_status`)** 임을 명시하고 왜 금액이 아니라 상태인지(고정값 O/C 유무) 고지. 보상 절에도 라벨/F4 **조건 분리** 문단 추가. ※ 위젯 쪽 = **위젯 작업자 몫**. |
| C035 | 반영 | 본문 전반 | '고정값' 표기를 `Fixed Values(고정값)` 방향으로 통일(L01과 동일 마킹 `[[고정값\|Fixed Values]]`). |
| C037 | 반영 | `## 정리` 신설 | 5불릿 — PARAMETERS=선택화면 입력칸 / 라벨·F4 출처와 체크 1회 / F4≠검증 / 옵션 3+1 / **3계층 총정리**(기술=Domain · 의미=Data Element · 화면=PARAMETERS). |
| C039 | 반영 | 옵션 불릿 + 흔한 실수 | 'F4≠검증' 4회 반복을 **콜아웃 1회 + 체험**으로 축약 — 옵션 불릿의 재진술 삭제, 흔한 실수는 증상형 1줄("목록 밖 값이 그냥 통과해요 → `VALUE CHECK` 누락")로 교체. |
| C046 | 반영 | 옵션 절 🔠 콜아웃 신설 | 선택화면 기본 대문자 변환이 **ABAP 고유 동작**임을 다른 환경 입력 폼과 대조해 강조(아이디·이메일 주의). |
| C047 | 반영(재량) | `## 유용한 PARAMETERS 옵션` | 코드블록 **2개로 분리** — 화면 편의 3종(`OBLIGATORY`·`DEFAULT`·`LOWER CASE`) / **검증 1종**(`VALUE CHECK`)에 "검증은 따로다" 소제목 문장. |
| C051 | 반영 | F4≠검증 콜아웃 | 꼬리 괄호 문장을 **주어 살린 본문 문장**으로 승격("단, `VALUE CHECK`는 DDIC 타입(Data Element)을 참조하는 입력칸에만 붙일 수 있다") + 코드 예시를 별도 줄로 분리. |

## 위젯 작업자 몫 (5 · 본문 미수정)

| ID | 파일 | 내용 |
|---|---|---|
| C010 | `embeds/_engine/param-screen-lab.js` | `pa_name` 길이 본문 20 ↔ 위젯 maxlength 10 불일치. **본문 `LENGTH 20` 유지**(C002 기각으로 구문 유효 확정) → 위젯을 20으로 맞추는 방향. |
| C015 | `embeds/abap/CH03-L02-S01.html` | L02 위젯 관람형(입력 0) — 생성 폼/라벨 입력 추가 또는 안내문 축소. |
| C031 | `embeds/abap/CH03-L01-S01.html` | `doActivate()` 가드 부재 → 타입 미선택 후 활성화 시 TypeError·버튼 무응답. |
| C033 | `embeds/_engine/param-screen-lab.js` | DEFAULT 삭제 시 복원되는 조건식 2곳 — 사용자 삭제 상태 별도 기록 필요. |
| C048 | `embeds/abap/CH03-L03-S01.html` | 표준 타입 모드 라벨 폴백 소문자 `pa_stat` → 대문자 렌더. |

## 기각 3 (지시대로 무수정 — 원문 보존 확인)

| ID | 확인 |
|---|---|
| C001 | L01 값 테이블 콜아웃의 **사실 관계**(값 테이블=FK 연결 시 검사 · 고정값=그 자체로 F4·검증) 유지. C017로 **용어 노출만** 완화했고 사실 서술은 그대로. |
| C002 | L03 `PARAMETERS pa_name TYPE c LENGTH 20 LOWER CASE.` 및 산문의 `TYPE c LENGTH 8` **원문 유지**. |
| C024 | L01 "**Domain은 항상 Data Element에만 사용된다**" **원문 유지**(C034는 앞 문장 비문만 교정). |

## 무단 수정 0 확인

- 변경 파일 = `content/abap/CH03/` 4개뿐(`git diff --stat` 확인). `embeds/**`·`docs/**` 직접 수정·타 챕터·`.project-docs` 변경 없음. `reference/glossary.json` 미변경.
- 모든 헝크가 위 표의 클러스터 ID에 매핑됨. 클러스터 밖 변경 = **아래 부수 발견 3곳**(전부 대상 4파일 내부, C035 작업 중 발견한 렌더 결함 수리).

### 본선 연동수정 (부기: 2026-08-03, Fable 5)

- C020 부호(Sign) 행 1곳 정밀화 — "숫자 타입(INT4·DEC 등)에서 음수가 필요하면 켠다" → "음수가 필요한 숫자 값이면 켠다(예: 금액의 마이너스, 재고 증감)". 사유: INT4는 본질적으로 부호 있는 타입이라 SE11에서 체크가 "필요"하다는 타입 단위 단정은 과단정 위험(키워드 문서는 SE11 UI 플래그 세부 미기술). 값 관점 예시로 전환해 위젯 칩(재고 증감)과의 연결은 유지.

### 부수 발견 — 표 셀 안 `[[키|표시]]` 마킹이 표를 깨뜨림 (기존 결함)

`[[ ]]` 치환은 marked **인라인** 확장이라 블록 파싱 시점엔 원문 파이프가 살아 있다 → 표 셀 안의 `[[Value Table|값 테이블]]`이 **가짜 열**로 세어져 GFM 표 판정이 실패하고 통째로 `<p>`(파이프 그대로 노출)로 렌더됐다. HEAD 빌드 산출물에서도 동일 확인 = **이번 변경으로 생긴 문제가 아님**.

- 수리 위치(3): L01 `Domain이 아는 것` 표 1셀 · L01 값 제한 비교표 헤더 · L02 `Domain vs Data Element` 표 1셀 → 표 셀은 **평문 표기**로 두고 마킹은 산문으로 이동(escape `\|`는 표는 살리지만 glossary parity 스캐너가 키를 `고정값\`으로 읽어 미정의 1건을 내므로 불채택).
- **남은 동일 결함(타 챕터 · 이번 범위 밖)**: `content/abap/CH22/CH22-L06.md:19` · `content/abap/CH26/CH26-L04.md:26,27`.
