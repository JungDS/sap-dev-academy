# CH04 content 반영 매핑 — 매트릭스 감사 백로그

> 작업일: 2026-08-03 · 실행자: Opus (CH04 본문 보강 실행자)
> 입력: `verdict/CH04.md` · `merged/CH04.json` · 대상 `content/abap/CH04/**`(8파일 = 레슨 7 + `_chapter`)
> 범위: **content .md 8파일만.** `reference/glossary.json` 키 추가 **0건**(새 마킹은 `[[WATCH POINT|Watchpoint]]` 별칭 하나뿐 — 기존 키 재사용). embeds·docs·타 챕터·`.project-docs` 미수정. git commit 미실행(본선 몫).
> 검증: `npm run build:abap` **통과**(glossary parity 미정의 0건 · pages 270) + 생성물 정적 점검 — `<details>` 안 fenced 코드가 `.abap-editor`로 정상 변환(L01·L07) · details 안 표 정상 렌더(L05, 9행) · 미변환 `**`/`[[ ]]` **0** · 소스 반각 `~` 잔존 **0** · 내부 레슨 링크 **22/22 실존** · embed iframe **17/17 실존** · `data-term="WATCH POINT"` 유지 확인(별칭 표기와 parity 양립). 브라우저 검증은 본선 몫이라 미실시.

## 집계

| 처리 | 수 |
|---|---|
| 반영 | **64** |
| 위젯 작업자 몫(미착수) | **10** (C004 · C005 · C006 · C007 · C021 · C028 · C029 · C053 · C054 · C055) |
| 보류 | **0** |
| 기각(지시대로 무수정) | 2 (C009 · C060) |

채택 74 = 반영 64 + 위젯몫 10. 누락 0.
분할 3건(C003 · C013 · C044)은 **본문 쪽만 반영**, 위젯 쪽 잔여는 아래 §좌표 인계에 표기.
지시 목록에 개별 항목이 없던 채택 4건(**C018 · C027 · C031 · C064**)도 verdict 채택분이라 함께 반영 — 각 행에 표시.

---

## `_chapter.md` (2)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C043 | 반영 | front-matter `intro`·`keywords` + 본문 문단 | intro에 **문자열 축**을 넣어 7레슨 전부를 서사에 포함("계산도, 글자 손질도, 판단도, 되풀이도 못 한다" → 디버거 확인 → 구구단 종합). `keywords`에 `문자열` 추가. 본문 문단도 같은 문장으로 동기화. |
| C076 | 반영 | 본문 🧑‍💻 콜아웃 신설 | 챕터 레벨 경력자 안내 — ABAP만 다른 4지점(공백 강제·정수 나눗셈 반올림·`&&`는 문자열 잇기·참/거짓은 `'X'`/공백)만 확인하고 디버깅 레슨은 진입법·단축키 위주로. CH01·CH02 `_chapter` 패턴 그대로. |

## `CH04-L01.md` (12)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C033 | 반영 | 절 순서 재배치 | 불편 절(`## 값을 받았지만, 계산을 못 한다`)을 **레슨 맨 앞으로** 이동하고 `## 대입과 초기화`를 그 뒤로. 내용 손실 0, 마무리 한 문장만 연결어로 조정("값을 넣고 비우는 법부터 짚은 뒤 … 산술 연산자로"). `prevRel: pain-solution` 정합 회복. |
| C047 | 반영 | 불편 절 끝 ⏩ 콜아웃 신설 | 경력자 지름길 — ABAP 고유 3지점(연산자 앞뒤 공백 필수 · `**`는 오른쪽부터 · 정수 나눗셈은 버림이 아니라 반올림)만 확인하고 나머지는 훑어도 된다. |
| C052 | 반영 | 공백 규칙 ⚠️ 콜아웃 | '토큰' 한 줄 풀이 삽입("공백으로 끊어 읽는 한 덩어리") + 왜 붙여 쓰면 오류인지를 그 풀이로 설명. |
| C030 | 반영 | 같은 콜아웃에 문단 추가 | 공백 규칙의 **예외 1건** 명시 — 문자 변수 뒤 `+숫자(길이)`는 오류가 아니라 offset 접근으로 조용히 해석된다([Chapter 2](CH02-L03) 링크), 그래서 더더욱 공백 습관. |
| C023 | **반영(상충 해소 준수)** | `## 계산 순서` 코드 블록 + 1문장 | 본문 서술('`**`끼리는 오른쪽부터')은 **정확하므로 무수정**. 대조 예제 한 줄만 추가 — `gv_n = 2 ** 3 ** 2. " 512`, 뒤에 "왼쪽부터였다면 `( 2 ** 3 ) ** 2` = 64" 대비 문장. |
| C058 | 반영 | 나눗셈 절 코드 블록 아래 | `LENGTH 8 DECIMALS 2`가 [Chapter 2](CH02-L03)에서 정한 **금액용 규격**임을 상기 링크 1문장(8이 어디서 온 수인지 회수). |
| C031 | **반영(지시 밖 채택분)** | `## 정수 몫과 나머지` 절 | `/`와 `DIV`의 정반대 동작을 같은 숫자로 대조하는 코드 3줄 추가(`10 / 4` = 3 반올림 vs `10 DIV 4` = 2 버림). |
| C022 | 반영 | `## 문장형 연산` 도입 문단 | 위상 재프레임 — **새로 쓸 때 표준은 `=` 연산식**, 문장형은 "현업에 남아 있는 옛 코드를 읽기 위해" 눈에 익히는 것. 앞의 `MOVE`(legacy) 프레임과 기준 통일. R6가 명시한 CH04 도입이므로 절 자체는 유지. |
| C034 | 반영 | 문장형 연산 절 `<details>` + C033 재배치 | 부하 완화 — 문장형 4형제 코드 + 등가 설명을 `<details>`(더 보기)로 접음(삭제 0, 빌드에서 `.abap-editor`로 정상 변환 확인). 레슨 상단 재배열(C033)·지름길(C047)과 합쳐 "핵심 vs 옛 코드 해독" 우선순위를 분리. |
| C057 | 반영 | 날짜 절 첫 문장 | '앞 레슨에서 봤다' → **[Chapter 2](CH02-L02.html) 링크**(D 타입 표가 실제로 있는 레슨). |
| C056 | 반영 | 날짜 절 첫 문단 | 마법 서술 제거 — 계산할 때 **기준일로부터 일수로 바꿔** 더하고 다시 `YYYYMMDD`로 되돌린다는 원리 1문장. 날짜−날짜=일수가 왜 성립하는지 연결. |
| C032 | 반영 | 날짜 절 마무리 문단 + 흔한 실수 항목 | '날짜 리터럴' 호칭 정정 — `'20260101'`은 **날짜 전용 표기가 아니라 8자리 문자**이고, D 변수에 넣는 순간 날짜로 해석된다는 모델. 새 리터럴 문법(타입 지정 리터럴) 미도입. 흔한 실수 항목도 '날짜 값 형식'으로 정정. |

> **C009(기각) 준수** — `gv_i = 10 / 4. " 3` · `gv_dec = 10 / 4. " 2.50` 두 주석 **무수정**. C031로 추가한 대조 블록도 `TYPE i` 결과만 다루어 이 교육 포인트와 충돌 없음.

## `CH04-L02.md` (9)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C062 | 반영 | `&&` 예제 블록 | 블록 자기완결 — `DATA gv_short TYPE string.` 선언 포함(앞 블록의 `gv_full` 의존 제거). |
| C059 | 반영 | `&&` 절 🧑‍💻 콜아웃 신설 | 경력자 대조 1줄 — `&&`는 논리 AND가 아니라 **문자열 잇기**이며 ABAP의 논리 AND는 `AND`(다음 레슨에서 사용). |
| C016 | 반영 | 내장함수 문단 뒤 1문단 | `ipow( base = 2 exp = 8 )`의 **이름 붙은 인자 읽는 법**("밑은 2, 지수는 8" = `2 ** 8`). ipow 강등 대신 읽는 법 추가 쪽을 선택(재량 범위). |
| C002 | 반영 | `FIND` 코드 주석 + 💡 콜아웃 | `" 찾으면 sy-subrc = 0` → `" 찾으면 '찾았다'는 성공 표시가 남는다`(용어 미노출). 읽는 법은 [DO/WHILE 레슨](CH04-L05.html)에서 배운다는 **L1 예고 2문장**(코드 없음·"지금은 결과가 어딘가 남는다만"). |
| C046 | 반영 | L02 `SEARCH` 콜아웃 + **L01 `MOVE` 콜아웃** | 괄호 병기에 한글 풀이 — `(obsolete, 즉 더는 쓰지 않기로 한 낡은 문법)` · L01은 `(legacy, 즉 옛 방식이라 읽을 줄만 알면 되는 문법)`. |
| C061 | 반영 | `## 다듬기` 절 제목 | 어순 통일 — '부분 접근(offset)' → **offset 접근**(본문 표기와 일치). 제목은 `CONDENSE · TRANSLATE · offset 접근`. |
| C010 | **반영(본문 쪽 승격)** | `## 다듬기` 절에 TRANSLATE 미니 예제 신설 | `TRANSLATE gv_grade TO UPPER CASE.` 3줄 예제 + `TO LOWER CASE` 한 줄. L04가 참조하는 구문이 실제로 배워지는 자리 확보(라벨 교정은 L04 행 참조). |
| C011 | 반영 | 나열 문단 + front-matter | 나열부에서 TRANSLATE를 빼 예제로 승격하고, 남은 `SHIFT`·`OVERLAY`·`WRITE TO`는 "이름만 눈에 담아 두었다가 필요할 때" 참고로 명시. **`introduces`에서 세 개 제거**(TRANSLATE는 유지), `keywords`에 TRANSLATE 추가. |
| C049 | 반영 | 흔한 실수 `SPLIT` 항목 | 조용한 실동작 명시 — 변수가 모자라면 오류가 아니라 **마지막 변수에 남은 조각이 통째로** 들어간다. (미학습 요소인 `sy-subrc` 값·`INTO TABLE` 대안은 의도적으로 제외 — R15.) |

> **C060(기각) 준수** — `REPLACE 'ABAP' IN gv_text WITH 'HANA'.` **무수정**.

## `CH04-L03.md` (10)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C001 | **반영(높음)** | `## 여러 조건 묶기` 코드 블록 + 1문단 | 활성화 차단 해소 — `PARAMETERS: pa_amt TYPE i, pa_vip TYPE c LENGTH 1, pa_block TYPE c LENGTH 1.` 선언 추가(주석으로 "…이면 `'X'`"). **AS CHECKBOX 미사용**(미학습). 블록 아래에 `'X'`=참 관례 2문장 + "이 레슨 끝에서 제대로 정리한다"로 뒤 abap_bool 절과 연결. |
| C065 | 반영 | 비교 절 제목·표 | 띄어쓰기 — '크거나같다/작거나같다' → **'크거나 같다 / 작거나 같다'**, 절 제목 '비교연산자' → **'비교 연산자'**(L01 '산술 연산자'와 통일). `introduces` 키도 동기화. |
| C035 | 반영 | `IS INITIAL` 예제 + 1문단 | 출력 문구 `'입력값이 없습니다'` → `'금액이 0입니다'` + **빈 입력과 0 입력을 구분하지 못한다**는 주의 1문단. |
| C024 | 반영 | `IS INITIAL` 절 🧑‍💻 콜아웃 신설 | 경력자 대조 — ABAP에는 `null`이 아예 없고([Chapter 2](CH02-L01) 링크) 선언 즉시 타입 초기값이 들어가며, `IS INITIAL`은 "지금 그 초기값이냐"를 묻는다. |
| C018 | **반영(지시 밖 채택분)** | abap_bool 절 첫 두 문단 | 한 문장 3덩어리를 **두 문단으로 분할**(① 전용 타입이 없어 한 글자 문자에 `'X'`/공백 → ② 그래서 표준 타입 `abap_bool`). 표기도 `TYPE c LENGTH 1` **한 가지로 통일**(기존 'C 1'·'c length 1'·'속은 C 1' 혼용 제거). |
| C036 | 반영 | 같은 문단 | '플래그' 한 줄 풀이("켜짐·꺼짐만 나타내는 표시값") — 본문·예제 문자열·흔한 실수에서 쓰이는 말의 정의 확보. |
| C063 | 반영 | abap_bool 예제 뒤 🧑‍💻 콜아웃 신설 | 경력자 매핑 1줄 — 타 언어 `boolean`/`bool` 원시 타입 자리를 ABAP에서는 `abap_bool` 관례가 대신한다. |
| C014 | 반영 | xsdbool 문단 + 📝 참고 콜아웃 신설 | **등가 병기 해체** — 본문은 `xsdbool( )`만, `boolc( )`는 참고 각주로 강등. 반환형이 **문자열**이라 `abap_bool` 비교·`IS INITIAL`이 어긋난다는 함정 1줄 명시. |
| C019 | 반영 | 같은 각주 | bool 5종 부하 완화 — 본문 노출을 `abap_bool`·`abap_true`/`abap_false`·`xsdbool` 4종으로 줄이고 BOOLC는 각주로. `introduces`에서도 BOOLC 제거. |
| C064 | **반영(지시 밖 채택분)** | 같은 각주 괄호 | 릴리스 단서 — `boolc( )`는 7.02, `xsdbool( )`은 7.40 이상에서 사용 가능하며 아주 옛 실습 시스템에선 문법 오류가 날 수 있다. |

## `CH04-L04.md` (5)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C025 | 반영 | 도입 절 제목 | 비문·톤 이탈 교정 — `## 하나의 변수로 IF ~ ELSEIF ~ ELSEIF …처럼 계속 조건 비교할 때` → **`## 한 값만 계속 비교하는데 IF/ELSEIF가 길어진다`**(다른 레슨의 말 거는 서술형 제목과 통일, 반각 `~` 동시 제거). |
| C066 | 반영 | `ENDCASE` 설명 뒤 🧑‍💻 콜아웃 신설 | 경력자 대조 — C 계열 `switch`의 fall-through가 없고 `break`를 쓸 필요도, 그런 문장도 없다. |
| C037 | 반영 | 흔한 실수 `WHEN OTHERS` 항목 | '안전' 단정 조건화 — `WHEN OTHERS`를 **두기만 해서 안전해지지 않고**, 거기서 무엇을 할지(안내·오류 알림)까지 적어야 값이 조용히 새지 않는다. |
| C015 | 반영 | 흔한 실수 소문자 항목 재작성 | 원인 정정 — 선택화면 문자 입력칸은 **기본적으로 대문자로 변환**되어 이 예제로는 재현되지 않음을 명시하고, 소문자가 살아 있는 경우(입력칸을 `LOWER CASE`로 선언 · 파일/DB 유래 값)에 `TRANSLATE`로 통일하는 흐름으로. |
| C010 | **반영(라벨 교정)** | 같은 항목 링크 | `[앞 레슨](CH04-L02.html)` → **`[문자열 레슨](CH04-L02.html)`**(실제 두 레슨 앞이라 '앞 레슨'이 오독을 부름). |

> **C004(위젯몫) 관련** — 본문 `WHEN OTHERS` 출력 `'확인 필요'`는 **정본으로 유지**(무수정). 위젯 S01·S02를 여기에 맞추는 것이 위젯 작업자 몫.

## `CH04-L05.md` (9)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C069 | 반영 | 도입 첫 문장 | 중의 구조 해소 — `"2단을 출력하라"는 WRITE를 9번…` → **`2단을 출력하려면 WRITE를 9번…`**. |
| C012 | 반영 | DO 절·WHILE 절에 완성 코드 블록 각 1개 | DO는 `DO 5 TIMES … ENDDO`(3줄), WHILE은 `DATA gv_n TYPE i VALUE 1.` 포함 자기완결 6줄로 **본문 앵커 확보**. 주의 문구가 가리키던 `gv_n`이 실체화됨. 두 블록 모두 위젯 S02·S03 코드카드와 **동일 코드**로 맞춤(정합 확인). |
| C020 | 반영 | DO 절 💡 콜아웃 신설 | 중첩 시 안쪽 루프가 `sy-index`를 덮어쓴다는 **1줄 예고** + [구구단 실습](CH04-L07.html)에서 확인한다는 링크(L07의 `introduces: []` 전제 회복). |
| C067 | 반영 | 루프 제어 표 `EXIT` 행 + 흔한 실수 | '즉시 완전히' → **"지금 도는 반복을 즉시 빠져나간다(반복이 겹쳐 있으면 가장 안쪽 하나만)"**. 흔한 실수 항목도 동일 문구로 정정. |
| C050 | 반영 | 표 아래 💡 콜아웃 신설 | `CHECK`의 정의는 **반복 안**에서의 뜻이고, 반복 밖에서는 처리 덩어리를 그만두는 다른 뜻이 된다는 1줄(정식은 모듈화 챕터 — L1 예고). |
| C068 | 반영 | 표 아래 🧑‍💻 콜아웃 신설 | 경력자 대응표 — `EXIT`≈`break` · `CONTINUE`≈`continue` · `CHECK 조건`≈"조건이 아니면 continue". |
| C017 | 반영 | `## SY` 절 구조 개편 | 암기 권유 제거("자주 쓰는 것들은 외워 두면 좋다" → **"외워 둘 목록이 아니라 필요할 때 찾아 쓰는 목록"**) + 이 레슨에서 실제 쓰는 `sy-index`를 문장으로 분리하고 **표 전체를 `<details>`로 접음**. |
| C038 | 반영 | 같은 표 `sy-subrc` 행 | 과일반화 축소 — '직전 명령의 성공/실패' → **'이 값을 채우는 명령의 성공/실패'**. |
| C070 | 반영 | SY 콜아웃 분할·재작성 | 한 블록 4주제를 **⚠️(저장 금지 + 좋은 예)와 💡(sy-subrc는 채우는 명령 바로 다음 줄에서 확인 + 뒤 챕터 활용) 두 개로 분리**하고, '채워 주는/세팅하는' 혼용을 **'채워 주는'으로 통일**. L02의 C002 예고를 여기서 회수. |

## `CH04-L06.md` (11)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C072 | 반영 | 도입 절 ⏩ 콜아웃 신설 | 경력자 지름길 — 중단점·한 줄 실행·변수 감시 개념은 동일하니 **SAP만의 진입법·단축키**만 확인. |
| C040 | 반영 | 진입법 ② | 조작 모델 교정 — `/h` 입력 후 "디버깅 켜짐" 안내가 뜨고, 그 자리에서 멈추는 게 아니라 **그다음 조작부터**(실행 버튼·Enter) 디버거가 열린다. |
| C039 | 반영 | 진입법 ③ 신설 + 절 제목 | **에디터 세션 브레이크포인트** 3줄 추가(SE38에서 줄에 커서 → 브레이크포인트 버튼 · 코드 무수정 · 세션 동안만 유효). 절 제목 '두 가지'→'세 가지'. `introduces`에 등재. 심화(외부 브레이크포인트·ADT) 미도입. |
| C013 | **반영(상충 해소 준수·본문 쪽)** | F키 표 아래 콜아웃 | 표 유지 + 통용 명칭 **1줄 병기**("다른 IDE를 써 봤다면 `F6`이 Step Over, `F7`이 Step Out"). 기존 "지금 외울 필요 없다" L1 톤 유지, 모듈 개념 심화 없음. |
| C008 | **반영(높음)** | 변수 값 보기 절 + ⚠️ 콜아웃 신설 | '메모리상 임시값만 바뀐다' 단정 제거 — 바뀐 값을 들고 **뒤 코드가 계속 실행**되므로 저장·갱신이 있으면 실제 데이터가 바뀐다는 경고 1문단(개발·실습 시스템에서만). '가설 확인용'이라는 원래 취지는 유지. |
| C073 | 반영 | 절 제목·본문 마킹·front-matter | 표기 정정 — 제목 `## Watchpoint — 값이 바뀌는 순간 잡기`, 본문은 **`[[WATCH POINT|Watchpoint]]` 별칭**(glossary 키 'WATCH POINT' 유지 → parity 무손상, 빌드 `data-term="WATCH POINT"` 확인). `keywords`·`introduces`도 `Watchpoint`로. |
| C027 | **반영(지시 밖 채택분)** | Watchpoint 절 1문단 | 설정·해제 최소 절차 — 디버거에서 **Watchpoint 만들기 버튼** → 변수 이름 입력 → 저장, 목록에서 확인·삭제, 세션 동안만 유효. |
| C071 | 반영 | Short Dump 항목 | 덤프명 교체 — `BCD_ZERODIVIDE` → **`COMPUTE_INT_ZERODIVIDE`**(링크된 L01 예제가 `TYPE i`), 괄호로 "P 타입이면 `BCD_ZERODIVIDE`" 부연. |
| C074 | 반영 | front-matter | 본문이 L3로 도입 중인 **Short Dump·ST22(Dump Analysis)** 를 `introduces`에 등재하고 `keywords`에도 추가(이후 챕터 기학습 판별 근거 확보). |
| C042 | 반영 | 흔한 실수 이송 항목 | '이송' 한 줄 풀이 + **[Chapter 1](CH01-L06.html)**(개발 패키지와 이송요청 입문 — grep으로 실 레슨 확인) 링크 + "걱정되면 ③번 세션 브레이크포인트" 연결. |
| C041 | 반영 | 흔한 실수 F8 항목 | 전제 명시 — **관심 구간에 브레이크포인트를 걸어 뒀다면** `F8`로 건너뛴다(멈출 지점이 없으면 `F8`은 끝까지 가 버린다). |

## `CH04-L07.md` (7)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C003 | **반영(높음·본문 쪽)** | 2단계 문단·소제목 | 구구단 정본 **2～9단**으로 통일 — '단도 1~9로' → **'단도 2～9로'**, 소제목 `전체(2~9단)` → `전체(2～9단)`. 위젯(`DO 8 TIMES`)·CH25 회수 72행과 정합. |
| C045 | 반영 | 본문 5곳 | 반각 `~` → **전각 `～`** 전수 교체(1단계 `(1～9)` · 2단계 제목 `2～9단` · 2단계 본문 · 3단계 제목 `3～5단` · 3단계 본문 `1～9`). 코드 스팬 밖 프로즈만. 소스 전체 반각 `~` 잔존 0 확인. |
| C051 | 반영 | 도입 절 ⏩ 콜아웃 신설 | 경력자 지름길 — 1·3단계는 훑고, ABAP 고유 지점인 **2단계 중첩 `sy-index` 덮어쓰기**만 확인. |
| C044 | **반영(분할·본문 쪽)** | 도입 절 문단 + 3단계 문단 | ① 도입 선언을 실제 범위로 정직화(1단계=반복, 2단계=중첩, 3단계=반복+입력값 판단, 이후 디버거). ② '판단'을 **3단계 범위 가드로 실체화** — `pa_from > pa_to`면 안내를 `WRITE`로 찍고 `ELSE`에 반복을 넣어 `ENDIF`로 닫는 서술 1문단. **`EXIT` 미사용**(루프 밖 사용 금지 명시). 흔한 실수 '범위 입력 역전' 항목도 이 가드를 가리키도록 정정. |
| C048 | 반영 | 3단계 문단 | `+ 1` 이유를 숫자로 — **3～5단이면 3·4·5로 세 번인데 `5 - 3`은 2뿐**이라 시작 단 자신을 세는 `+ 1`이 필요. |
| C075 | 반영 | 3단계 문단 | 근거 교체 — '효율적이다' → **"하려는 일이 코드에 그대로 드러나 읽기 좋다"**(체감 불가한 성능 대신 의도의 명확성). |
| C026 | 반영 | 2단계 끝 `<details>` 신설 | 완성 코드 블록 1개(`gv_dan`·`gv_mul`·`gv_res` 선언 포함, 위젯 S02 정답과 **동일 코드**: `DO 8 TIMES` / `gv_dan = sy-index + 1` / 안쪽 `DO 9 TIMES` / `SKIP`)로 본문 실체화. 정답 선노출 균형은 **"2단계를 직접 채웠다면 — 완성된 코드 펼쳐 보기"** 접기로 처리. 이어지는 디버거 절·챕터 결론(변수 3개 → Structure)의 전제 회복. |

---

## 본선 연동수정 (부기: 2026-08-03, Fable 5)

- C021 연동 — 위젯 빈칸이 4→9종(REPLACE·CONDENSE·offset·&&·TRANSLATE 포함)으로 확장된 뒤 본문 임베드 캡션이 구식("CONCATENATE · SPLIT · FIND")으로 남아 본선이 교체: 도입 문장 "잇기·쪼개기·길이·찾기·바꾸기·다듬기" + 캡션 "빈칸을 채워 문자열 다루기 완성 — 잇기부터 다듬기까지".

## 위젯 작업자 인계 — 본문이 먼저 바뀐 좌표

| 클러스터 | 본문에서 확정된 것 | 위젯 쪽 해야 할 일 |
|---|---|---|
| **C003** | L07 본문·소제목이 **2～9단**으로 통일(전각 ～). | `CH04-L07-S02`는 이미 `DO 8 TIMES`·주석 `" 2~9단"`으로 정본과 **일치**(수정 불요). 주석 안 반각 `~`는 코드 스팬이라 강제 대상 아님 — 표기 통일 원하면 위젯 재량. |
| **C044** | L07 3단계 본문이 범위 가드를 **`IF … WRITE … ELSE … ENDIF`** 로 서술(EXIT 미사용). | `CH04-L07-S03` 힌트 블록이 루프 밖 `EXIT`를 2회 사용(=C028) → **본문과 어긋나므로 `IF/ELSE/ENDIF` 형태로 교체**해야 정합. 힌트의 '양 끝 관문(시작 ≥ 2·종료 ≤ 9)'은 본문에 없는 추가 규칙이니 유지/축소는 위젯 판단. |
| **C053** | L07 2단계 **완성 코드가 본문 `<details>`에 노출**(S02 정답 5칸 전부 포함, 접힘 상태). | S01 정답 선노출(L06-S01 코드) 해소 작업 시 이 사실을 함께 고려 — S02는 본문 접기와 중복되므로 '정답 보기' 동선 재설계 시 기준점. |
| C004 | L04 본문 `WHEN OTHERS` 출력 `'확인 필요'` 유지(정본). | S01·S02의 `'등급 없음'`을 **`'확인 필요'`로 통일**. |
| C005 | L01에 `DIV` 대조 코드(`10 / 4` vs `10 DIV 4`)와 `2 ** 3 ** 2` 예제가 **신설**됨. | 스텝 확장 시 이 두 예제 + 정수 나눗셈 함정·날짜 산술을 커버 대상으로. 문장형(ADD류)은 본문에서 `<details>`로 강등됐으니 우선순위 낮음. |
| C006 | L03 복합 조건 예제에 **`pa_vip`·`pa_block` 선언이 추가**됨(`TYPE c LENGTH 1`). | S02 인터랙티브화 시 선언 포함 코드로 동기화. abap_bool 절 체험 신설 시 본문은 `xsdbool`만 노출(boolc는 각주)임에 유의. |
| C007 | L06 절 제목·본문이 **Watchpoint**(한 단어) 표기로 변경되고, 거는 절차 1문단이 추가됨. | S01 개조 시 제목·라벨을 `Watchpoint`로 맞추고, 본문 절차(만들기 버튼 → 변수 이름 → 저장)와 어긋나지 않게. |
| C021 | L02에 **TRANSLATE 미니 예제 신설**, `SHIFT`·`OVERLAY`·`WRITE TO`는 참고로 강등(introduces에서 제거). | S01 커버리지 확장 대상 = `REPLACE`·`CONDENSE`·offset·`&&`(+선택적으로 TRANSLATE). 제거된 3개는 체험 대상 아님. |
| C029 | (본문 변경 없음) | L06-S01 스텝-코드 불일치 해소. |
| C054 | (본문 변경 없음) | L07-S02 두 번째 빈칸 `aria-label` 추가. |
| C055 | L04 본문 예제는 A·B·C + OTHERS 그대로. | S04의 D등급('미흡') 제거 또는 본문과 정렬. |
| — | L05 본문에 **DO/WHILE 완성 코드 블록이 신설**(S02·S03 코드카드와 동일 코드). | 위젯 코드 변경 시 본문 블록과 **동기 유지** 필요(둘이 갈라지면 새 불일치). |

## 무단 수정 0 확인

- `git diff --stat` 기준 이 세션이 만든 변경 = **`content/abap/CH04/` 8파일뿐**. 모든 헝크가 위 표의 클러스터 ID에 매핑됨. 클러스터 밖 변경 = **아래 부수 발견 1곳**(대상 파일 내부, 렌더 결함 수리).
- `reference/glossary.json` 키 추가 **0건** — 새 마킹은 기존 키 별칭 `[[WATCH POINT|Watchpoint]]` 하나, 새로 쓴 용어(`플래그`·`토큰`)는 **마킹 없이 인라인 한 줄 풀이**로 처리해 parity 무영향.
- `embeds/**`·`docs/**` 직접 수정 0 · 타 챕터 0 · `.project-docs` 0 · git commit 0.
- 기각 2건 무수정 확인: L01 `gv_i`/`gv_dec` 주석(C009) · L02 `REPLACE … IN … WITH`(C060).
- R15 점검: 새로 들어온 코드·용어 중 미학습 요소 0(`AS CHECKBOX`·외부 브레이크포인트·타입 지정 리터럴·`INTO TABLE`·`sy-subrc` 값 노출 모두 회피). `SKIP`은 CH01-L05 기학습, 콜론 체인은 CH01-L04 기학습으로 확인 후 사용.
- ※ 참고: `npm run build:abap`은 전 챕터를 재생성하므로 `docs/**`에는 **동시 진행 중인 CH02·CH03 작업분도 함께 반영**됐다(그 챕터 담당자가 재빌드하면 그대로 갱신됨).

### 부수 발견 — 코드 스팬으로 끝나는 굵게가 한글 조사 앞에서 안 닫힘 (기존 결함)

CommonMark의 닫는 구분자 규칙 때문에, `**…`코드`**` 처럼 **닫는 `**` 바로 앞이 구두점**(백틱·닫는 괄호)이고 **바로 뒤가 한글/영문 글자**면 그 `**`는 닫는 구분자로 인정되지 않는다 → 굵게가 적용되지 않고 **`**`가 화면에 그대로 노출**된다. (뒤가 공백이나 구두점이면 정상 렌더되므로 대부분의 `**`코드`**(부연)` 형태는 문제없다.)

- 수리 위치(1): `CH04-L01.md` 임베드 도입 문단 — `**사칙연산·제곱·`DIV`·`MOD`**가` → `**사칙연산·제곱**과 `DIV`·`MOD`가`. HEAD 산출물에서도 동일하게 깨져 있었음 = **이번 변경으로 생긴 문제가 아님**. 수리 후 CH04 7페이지 전부 미변환 `**` 0건.
- **남은 동일 결함(타 챕터 · 이번 범위 밖)**: 생성물 전수 스캔(270페이지, `<pre>`·`<code>` 제외) 결과 **54페이지**에 잔존 — CH09·CH10·CH12·CH13·CH15·CH16·CH18～CH26·CH28·CH30·CH33·CH34·CH36～CH39. 대표: `CH10-L01`(`**레거시(구식)**다`) · `CH12-L06`(`**`TYPE RANGE OF`**를`) · `CH24-L02`(`**뿌리(root)**다`). 재현 명령은 이 문서 §검증의 스캔 스크립트와 동일(생성물 기준이 정본 — 소스 정규식만으로는 오탐이 많다).
