# CH06 위젯 반영 매핑 — 매트릭스 감사 배치 2

> 작업일: 2026-08-09 · 실행자: Opus (CH06 위젯 보강 작업자, 브라우저 실측 직렬 슬롯)
> 입력: `verdict/CH06.md` "위젯 작업(직렬)" 9건 · `fixes/CH06-content-map.md` E절(인계 좌표, ★ 6건) · `merged/CH06.json` · `raw/CH06/AG02-*.json`
> 범위: `embeds/abap/CH06-*.html` + 공통 엔진 `embeds/_engine/step-debugger.{js,css}` + `embeds/abap/_index.md`.
> **미수정(지시대로)**: `content/**` · `docs/**` · 타 챕터 위젯 · `.project-docs`. git commit 미실행(본선 몫).
> 실측: `http://localhost:8143`(기동·중지 안 함) · 스텝 디버거는 **전 스텝 주파** · 콘솔 오류 0 · 라이트/다크 양쪽 · 디스크 진위는 `fetch no-store` 대조.

## 집계

| 처리 | 수 |
|---|---|
| 위젯 수정 | **5** (L01-S01 · L03-S01 · L04-S01 · L04-S02 · L06-S01) |
| 위젯 신설 | **1** (L04-S04) |
| 지시 클러스터 해소 | **9 / 9** (C006 · C041 · C042 · C043 · C044 · C045 · C058 · C059 · C060) |
| 공통 엔진 수정 | **1** (step-debugger — 가산적 `itab.max`) |

---

## A. 클러스터별 반영 · 실측 (수정 전 → 후)

| 클러스터 | 위젯 파일 | 변경 요지 | 실측 결과 (전 → 후) |
|---|---|---|---|
| **C006** 본문-위젯 불일치(높음, AG02 4모델 전원) | `CH06-L06-S01.html` | 행 수 81 → **72** 전면 교정. 리드문·라벨·마지막 idx를 전부 재작성하고 근거를 본문 코드(`DO 8 TIMES` × `DO 9 TIMES`)에 맞춤. `gs_line-result` 최종값 81(9×9)과 **행 수 72**를 콘솔에서 명시적으로 구분("행 수 72와 헷갈리지 마세요") | 본문 텍스트 `81` 7회(전부 행 수 오기) → **`81[행\|개]` 정규식 매치 0**. 워치 마지막 행 `72 / 9 / 9 / 81`, `gv_cnt = 72` |
| **C042** 헛체험(★) | `CH06-L06-S01.html` | 정적 `state-change-grid` 스냅샷 → **`step-debugger` 전환**(엔진 재사용). 13스텝으로 `APPEND`마다 **1→2→3→4행 하나씩** 증가를 관찰 — 본문 C018 개정("1개, 2개, 3개 … 72개로 하나씩")과 정합. 이후 9→18→72행, `DESCRIBE`, `SORT result DESCENDING` | **버튼 0개(클릭 요소 없음)** → 버튼 4개·**13/13 스텝 완주**. 성장 트레이스 실측 `1→2→3→4→9→9→18→72→72→72행` |
| **C041** 위젯 결함(★) | `CH06-L03-S01.html` | READ 실패 스텝에 `sy-tabix: "0"` 추가. 워치 sub도 "다룬 행 번호" → **"찾은 행 번호 · 실패하면 0"**. `sy-subrc` sub은 본문 C049에 맞춰 "0이면 성공 (실패는 4, 때로 8)". note에 실패 시 `sy-tabix=0` 명시 | 실패 스텝 `subrc=4 **tabix=4**`(직전 성공값 잔존) → `subrc=4 **tabix=0**`. Work Area 잔존(홍길동/26)은 의도대로 유지 |
| **C043** 체험 공백(★) | `CH06-L03-S01.html` | `INSERT gs_person INTO TABLE gt_person.` 스텝 신설(심청/17). 본문 신설 **종류별 삽입 위치 표**(C003)와 정합 — STANDARD라 맨 끝에 붙고 결과가 `APPEND`와 같음을 콘솔이 설명하고 SORTED/HASHED 차이도 한 줄. 코드 1행 주석에 `gt_person(STANDARD)` 명시 | 문장 부재 → 4번 스텝에서 **5번 행에 심청 추가**(focus 강조) 확인 |
| **C060** 체험 공백 | `CH06-L03-S01.html` | `READ TABLE gt_person TRANSPORTING NO FIELDS WITH KEY name = '유재석'.` 스텝 신설. 어순은 판정 확정(C012 기각 = `READ TABLE itab [result] [key]`)대로 result 선행 | 문장 부재 → 6번 스텝 `subrc=0 tabix=3`인데 **Work Area는 정훈영/30 그대로**(한 필드도 미복사) 확인 |
| **C044** 체험 공백(★) | `CH06-L04-S01.html` | **2막 FROM/TO 신설** + 막 순서를 본문 서술 순서(WHERE·FROM/TO → ASSIGNING)로 재배치. 코드 문자열은 지시대로 **`LOOP AT gt_person INTO gs_person FROM 2 TO 5.`**(C007 교정 어순). 2막이 원본 테이블에서 돌도록 ASSIGNING(+1 변형)을 3막으로 미룸 | 14스텝(2막) → **19/19 스텝**(3막). 2막 실측 회차 1·2·4 ↔ `sy-tabix` 2·3·5, **1번 행 미순회** 확인 |
| **C058** 위젯 결함(★) | `CH06-L04-S02.html` | ① 경계 문장을 스텝으로 짚음 ② 본문 신설 **AT 마스킹 경고**(C017)를 동작으로 구현 — `AT` 진입 시 `gs_sales-amount` → 0(초기값), `ENDAT` 이탈 시 행 값 복원, `AT END OF`에서 `SUM`이 그 빈자리에 그룹 합계 충전. 부산 그룹은 전 과정 정밀, 서울 그룹은 압축 | 미강조 줄 `1,8,9,**11,12,14,16,19**` → **`1,8,9`(주석·빈 줄)만**. 즉 `LOOP AT`·`AT NEW`·`ENDAT`·`AT END OF`·`ENDLOOP` 전부 강조됨. amount 궤적 실측 `20→0→20 / 30→0→50→30 / 100→0→100 / 30→0→130→30`. 18→**28/28 스텝** |
| **C059** 헛체험(★) | `CH06-L01-S01.html` | `gt_person 메모리` 워치 신설 — `CLEAR`에서 "초기 메모리 확보됨 (행만 비움)", `FREE`에서 "반환됨 · 0". 콘솔 문구도 본문 C021 단일 표기 **'초기 메모리'**로 통일 | 마지막 FREE 스텝 **`visualDelta=NO flashCells=0`** → **`YES flash=1`**. 11스텝 전부 시각 변화 있음. 본문 대조: '초기 할당' **0회** / '초기 메모리' 2회 |
| **C045** 체험 공백(재량) | **`CH06-L04-S04.html` (신설)** | `APPEND LINES OF` 소형 체험 4스텝. 본문 신설 선언(`gt_a` 지점 명단 · `gt_b` 전체 명단)의 이름·의미를 그대로 재사용. ① 끝에 붙음(기존 행 뒤, 원래 순서 유지) ② `DESCRIBE`로 합친 쪽 3행 ③ **원본 `gt_a`는 2행 그대로(옮기기 아닌 복사)** ④ 한 번 더 실행하면 중복 5행 | 신규 → **4/4 스텝**. 실측 `gt_b: [손흥민/28] → [손흥민, 정훈영, 유재석] → (재실행) 5행`, `gt_a` 시종 2행 불변 |

### 사실 기준 준수
- **72행** — 캡스톤 위젯의 리드문·콘솔·워치·`gv_cnt` 전부 72. 생성 스크립트가 `all.length !== 72`면 빌드 실패하도록 자체 검증.
- **READ 실패 시 `sy-tabix`는 유효값 아님 → 0** — L03-S01 반영(C041), note·워치 sub까지 일관.
- **SORT 대상 = STANDARD·HASHED(본문 유지 확정)** — CH06 위젯 어디에도 이와 어긋나는 서술 없음(전수 확인). 캡스톤은 STANDARD 테이블 정렬이라 무관.
- **AT 마스킹·SUM** — 오프라인 코퍼스 재확인 후 구현: `ABAPAT_ITAB.md` 105～107행(그룹 키는 불변 · 오른쪽 문자류는 `*` · 그 외는 초기값)·62/76/84행(이탈 시 행 값 재할당), `ABAPSUM.md` 30행(그룹 키 오른쪽 숫자 성분 합산). 이 위젯의 `amount`는 `i`라 초기값 0이 맞다.

---

## B. 공통 엔진 · 자산 회귀 확인

### 변경 내용 (가산적 — 기존 동작 무변경)

| 파일 | 변경 | 하위호환 근거 |
|---|---|---|
| `embeds/_engine/step-debugger.js` | `itabHTML()`에 **`itab.max`** 지원 — 행이 `max`보다 많으면 앞/뒤만 렌더하고 가운데를 `⋮ n행 생략`(`tr.itw__more`)으로 접는다. `focus` 행은 항상 포함. 인덱스 주석(R8) 갱신 | `max` 미지정 시 `d.itab.max|0 === 0` → 렌더 인덱스 = 전 행 → **마크업이 종전과 동일**(생략 행은 인덱스가 연속이라 삽입되지 않음) |
| `embeds/_engine/step-debugger.css` | `.itw__t .itw__more td` 1규칙 추가(`var(--ink-soft)`·중앙정렬·소형) | 신규 클래스라 기존 셀렉터와 충돌 없음. **하드코딩 hex 0** → 다크 자동 적응 |

### 전수 회귀 — step-debugger 소비 **16/16**

`CH04-L01-S01` · `CH04-L05-S01` · `CH04-L06-S01` · `CH05-L01-S01` · `CH05-L01-S05` · `CH05-L04-S02` · `CH05-L05-S01` · `CH05-L05-S02` · `CH06-L01-S01` · `CH06-L03-S01` · `CH06-L03-S02` · `CH06-L04-S01` · `CH06-L04-S02` · `CH06-L04-S04` · `CH06-L06-S01` · `CH10-L07-S01`

- 방법: 동일 오리진 iframe 16개에 **캐시 우회 최신 엔진을 강제 주입**한 뒤 각각 전 스텝 주파(브라우저가 `_engine/*.js|css`를 메모리 캐시에서 재사용해 구엔진으로 측정되는 함정을 확인하고 우회 — `fetch no-store`로 서빙본과 대조).
- 결과: **16/16 스텝 완주 · 콘솔 오류 0 · `data-counter` 일치**.
- 무변경 증명: 신엔진/구엔진 두 차례 측정에서 **`max` 미사용 15개의 itab 행 수·`body.scrollHeight`가 전부 동일**(예: CH06-L04-S02 921 = 921, CH05-L05-S02 1032 = 1032). 달라진 건 `max`를 쓰는 캡스톤뿐: `72행 / h=2781` → `6행+⋮66행 생략 / h=963`.
- **CH04-L06 Watchpoint 레이어**(`[data-wp]` 선택 배선)도 정상 — 패널 생성·`wp__none` 안내 표시 확인. 나머지 15개는 `[data-wp]`가 없어 해당 코드 경로 미진입(종전 계약 유지).

### 다크모드 계약 (AUTHORING §4)

- `node tools/gen-embed-dark.mjs` 재실행 완료 → 출력: `엔진 190종 · 수동 html.dark 보유로 제외: command-field-sim.css, mermaid.css, **step-debugger.css**`.
- **`_dark.css` diff 0줄**(HEAD와 바이트 동일). step-debugger는 자체 `html.dark` 블록 보유 엔진이라 생성기가 스킵하며, 추가 규칙이 토큰 전용이라 짝 규칙도 불필요 — 병행 세션의 `_dark.css`를 건드리지 않았다.
- 라이트/다크 실측: `.itw__more` 글자색이 라이트 `#5b6478` / 다크 `#a2a9ba`(= `--ink-soft`)로 추종, 두 모드 모두 데이터 행 잉크보다 흐림. 수정 위젯 6종 전부 다크 토큰 정상 적용.

### 반응형·높이
- 뷰포트 820 / 700 / 560px 전부 **페이지 가로 스크롤 없음**. 캡스톤 워치 표도 `clientW == scrollW`(가로 스크롤 없음).
  - 중간에 4열 워치(`#·dan·mul·result`)가 워치 칸을 넘겨 가로 스크롤이 생겼는데, **공통 레이아웃을 건드리지 않고** 위젯 config의 워치 sub 문구를 줄여 해소(151px = 151px).
- 전 위젯 `_autoheight.js` 유지 — 본문 `::embed`가 높이 인자를 쓰지 않으므로 셸 자동 높이에 의존(계약 불변).

### CH06 위젯 전수 최종 실측 (12개, 엔진 무관)

| 항목 | 결과 |
|---|---|
| 콘솔 오류 | **0 / 12** |
| 페이지 가로 스크롤 | **0 / 12** |
| 스텝 디버거 완주 | 6/6 (11·13·3·19·28·4·13 → 카운터 전부 일치) |
| config JSON 파싱 | 6/6 OK · **범위 밖 line 참조 0 · 미정의 vars 키 0** |
| R6 게이팅(modern 문법) | 인라인 `DATA(`·`VALUE #(`·`NEW #(`·`+=`·문자열 템플릿 **0건** |

---

## C. 본선 인계 (본문 몫 — 위젯 작업자가 손대지 않음)

1. **`CH06-L04-S04` `::embed` 라인 삽입 필요** — 신설 위젯이라 본문에 아직 연결되지 않았다(`_index.md` 상태 🔁).
   - 위치: `content/abap/CH06/CH06-L04.md`의 **`APPEND LINES OF` 설명 문단 바로 뒤**(현재 파일 기준 `` `gt_b`에 이미 행이 있으면 그 **뒤에** 붙는다… `` 문단과 `::embed CH06-L04-S03` 사이).
   - 제안 문구: `::embed CH06-L04-S04 | 두 명단 합치기 — APPEND LINES OF를 직접 확인::`
   - 참고: 이 절에는 코드 블록이 있으므로 R2(코드=체험) 충족에도 기여한다.
2. **`CH06-L04-S01`의 `::embed` 제목 갱신 검토(선택)** — 현재 본문 제목이 "반복 회차 vs sy-tabix — WHERE와 ASSIGNING을 직접 확인"인데 위젯에 **FROM/TO 막이 추가**됐다. 사실 오류는 아니나 과소 서술 → 예: "… — WHERE·FROM/TO·ASSIGNING을 직접 확인". **본문 수정 금지 지시에 따라 미반영.**
3. **`CH06-L06-S01` `::embed` 제목은 그대로 유효** — "행이 APPEND될 때마다 내부 테이블이 늘어나는 모습 보기"가 오히려 전환 후 위젯과 더 잘 맞는다(정적→관찰 가능).

## D. 부기 — 발견했으나 고치지 않은 것

- **`step-debugger.css`의 죽은 규칙(잠재 결함, 이번 감사 범위 밖)**: 47행 `.dbg .var--itab{display:block;}`이 88행 `.dbg .var{display:flex;…}`와 **특이도 동률(0,2,0)인데 소스 순서상 뒤에 오는 flex가 이겨** 무효화돼 있다. 저자 의도(itab 워치를 전폭 블록으로)가 실현되지 않아 itab 워치 7종이 전부 좁은 우측 칸에 갇힌다. 고치면 CH06 itab 위젯 7종의 레이아웃이 한꺼번에 바뀌므로 **어떤 클러스터도 요구하지 않은 시각 변경**이라 판단해 미이행 — 본선 판단용으로 기록만 한다(고칠 경우 `.dbg .var--itab` 규칙을 88행 뒤로 옮기거나 특이도를 올리면 되고, 소비 7종 전수 회귀가 필요하다).
- **`_index.md`는 병행 세션(CH05 위젯 작업자)과 공유 중**이라 CH06 행 5개와 표 B의 `step-debugger`(14→16)·`state-change-grid`(2→1) 행만 타깃 수정했다. 타 챕터 행은 건드리지 않았다.
- 워킹트리에 CH05·CH07·CH08 병행 작업분이 함께 있다. **본 작업의 diff는** `embeds/_engine/step-debugger.{js,css}` · `embeds/abap/CH06-L01-S01|L03-S01|L04-S01|L04-S02|L06-S01.html` · 신규 `embeds/abap/CH06-L04-S04.html` · `embeds/abap/_index.md`(CH06 부분) **뿐이다.**
