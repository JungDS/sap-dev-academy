# CH05 content 반영 매핑 — 매트릭스 감사 배치 2

> 작업일: 2026-08-09 · 실행자: Opus (CH05 본문 보강 작업자)
> 입력: `verdict/CH05.md`(정본) · `merged/CH05.json`(58클러스터) · 대상 `content/abap/CH05/**`
> 범위: **content .md 5파일**(`CH05-L01`～`CH05-L05`). `_chapter.md` 미수정(C055 기각) · `embeds/**` 미수정(위젯 작업자 몫) · `docs/**`·타 챕터·`.project-docs` 미수정 · `reference/glossary.json` 키 추가 **0건** · git commit 미실행(본선 몫).
> 빌드: 지시대로 `npm run build:abap` **미실행**(본선 일괄 점검).

## 집계

| 처리 | 수 |
|---|---|
| 반영 | **39** |
| 부분(지시가 정한 범위만) | **2** (C019 · C020) |
| 보류 = 위젯 작업자 몫 | **9** (C015 · C021 · C027 · C031 · C037 · C038 · C039 · C040 · C041) |
| 기각(지시대로 무수정) | **8** (C007 · C011 · C018 · C023 · C025 · C026 · C029 · C055) |

채택 50 = 반영 39 + 부분 2 + 위젯몫 9. 누락 0. 축 항목 **AX-1**은 별도 행으로 이행.

---

## 채택 본문 클러스터 전수 (41)

| ID | 파일 | 변경 요지 | 상태 |
|---|---|---|---|
| C001 | L03 | `ls-` 표기 붕괴 해소 — 접근 경로를 **선언 변수 `gs_order-` 기준으로 통일**(설명 문단 · 비교표 · 흔한 실수 3곳 전부). | 반영 |
| C002 | L05 | 72줄↔9줄 정합 — "방금 지나간 **9줄**"이 기본 서술이 되고, 72줄은 "2～9단 전체로 넓히면"으로 **조건화**. 정리 문장도 "9줄이든 72줄이든"으로. | 반영 |
| C003 | L03 | `.INCLUDE`·`.APPEND` 절차 공백 메움 — 각각 **SE11 4단계**(어느 화면·어느 칸에 무엇) + 활성화 후 **최종 모양과 접근 경로**를 제시. L02가 세운 조작 서술 패턴 유지. | 반영 |
| C004 | L02 | "그 외에는 전부 DDIC" 무기준 단정 제거 → **판단 기준 제시형**으로 재서술(임시 묶음=Local / 공유·화면·테이블 짝·일괄 수정 중 하나라도면 DDIC / 대가는 이름·이송·영향 확인). "남은 레슨 예제가 대부분 Local"이라는 사실도 명시해 L03～L05와의 충돌 해소. | 반영 |
| C005 | L05 | 캡스톤 무회수 해소 — **`## 배운 것을 한 번에` 절 신설**(LIKE 그릇 하나 더 → 통째 대입 → MOVE-CORRESPONDING → CLEAR, 4항목 해설). 이어서 **L02 회수**(같은 모양을 공유하려면 `ZST_GUGUDAN`으로 등록)와 **L03 회수**(공통 묶음은 `.INCLUDE`) 문단. front-matter `prereq`를 L01·L02·L03·L04로 정정, `keywords`에 MOVE-CORRESPONDING·CLEAR 추가. | 반영 |
| C006 | L01 + L02 | global 두 층위 충돌 — L01 접두어 표 뒤 ⚠️("`g`=한 프로그램 안 어디서나"), L02 본문("여기서의 Global=여러 프로그램이 함께 쓴다, `gs_`의 global보다 넓은 범위")로 **양쪽에 구분 문장**. | 반영 |
| C008 | L02 | `age`가 `i`→NUMC 3으로 바뀌는 다리 문단 + NUMC 한 줄 풀이(숫자만 담는 문자 · 앞 0 유지 `007`) + 선택 기준(계산이면 `i`, 자릿수 표시면 NUMC). 정의는 기수록 레슨 링크로 회수. | 반영 |
| C009 | L02 | 타입 후보 표에서 **미학습 행(Transparent Table·View·Table Type) 제거** → 배운 3행(Data Element·Structure·Domain)만 남기고, "아직 안 배운 DDIC 객체도 대부분 타입으로 쓸 수 있다"는 **새 용어 0개짜리 한 줄**로 대체. View 정의 선노출 없음(R15). | 반영 |
| C010 | L03 | `zst_order_head` 실물화 — "앞 레슨에서 `ZST_PERSON`을 만든 것과 같은 방법으로 만들어 둔 Structure, Component는 `customer`·`order_date` 둘" 리드인 추가. 예제에 `order_id` 칸을 넣어 **자기 필드 vs 중첩 필드** 대비가 코드에서 보이게. | 반영 |
| C012 | L01 | 첫 레슨 부하 재정돈 — `## 잠깐 정리` **중간 정리 절 신설**(만들기·넣고 읽기·이름 짓기 3줄) + 임베드 배치 조정(아래 C028). | 반영 |
| C013 | L01 | 흔한 실수 1개 → **4개**로 확충(END OF 이름 + 선언 체인 구두점 + 점(`.`) 대신 하이픈 + LIKE 값 미복사). 본문이 강조한 내용만 회수. | 반영 |
| C014 | L04 | 통째 대입 조건 정밀화 — "타입 다른데"→**"모양이 다른데"**, '다르면'의 지시 대상을 **Component 구성**으로 명시. 문자 계열끼리는 이름과 무관하게 자리로 밀려 들어간다는 실동작 추가. | 반영 |
| C016 | L04 | 값 복사 명시 — "`gs_b = gs_a.`는 값을 통째로 베껴 담는다, 다음 줄에서 `gs_a`를 비워도 `gs_b`는 그대로" 1문단 + 🧑‍💻 참조 언어 대조 1줄. | 반영 |
| C017 | L04 | 동명 이타입 위험 ⚠️ 신설 — 이름만 보고 옮기므로 30자→20자는 **조용히 잘리고**, 글자 칸→숫자 칸은 실행 중 오류. "길이·타입도 같은지 확인". | 반영 |
| C019 | L03 | **부분(지시 범위만)** — `.APPEND` 안전 서술은 무수정 유지하고, "덧붙이는 **필드 이름도 고객 네임스페이스**(`ZZ`/`YY`)로" 권고 한 줄만 추가. SPAU·호환성 검토 등 확대 서술은 하지 않음. | 부분 |
| C020 | L02 | **부분(지시 범위만)** — "`AMOUNT` 길이 8 = 자릿수 8 vs Local `p LENGTH 8` = 바이트" 한 줄 + 기수록 레슨 링크. 15자리 유도는 기수록이라 재설명 없음. | 부분 |
| C022 | L02 + L04 | Internal Table 표기 통일 — L02는 '내부 테이블'이 있던 Table Type 행이 C009로 사라져 해소, L04 본문은 `[[Internal Table]]`(기존 glossary 키) + front-matter `foreshadow`도 영어 원문으로. | 반영 |
| C024 | L02 | 전역 변경 파급 ⚠️ 신설 — "한 곳만 고치면 전부 반영"의 반대편(길이 축소 시 값 잘림·이름 변경 시 코드 붕괴)과 **사용처 조회(Where-Used List) 선행** 규율. | 반영 |
| C028 | L01 | 불편 3회 반복 축약 + 섹션↔체험 축 정렬 — 네 번째 절에서 "첫 섹션의 번호 참사" 재언급 삭제, **S06 임베드를 TYPES 절 뒤로 이동**(위젯 내용이 실제로 `TYPES`+Structure 대비라 그 절의 논지와 일치). 캡션도 `번호 변수 참사 vs TYPES + Structure`로. | 반영 |
| C030 | L01 | Work Area 정밀화 — "부르기도 하며"(동의어 뉘앙스) → **"한 건을 담아 처리하는 자리에 놓이면 그 변수를 Work Area라 부른다 · 새 타입이 아니라 쓰임새에 붙은 이름"**. | 반영 |
| C032 | L01 | `DATA gs_p4` 선언 위치 — verdict가 준 두 선택지 중 **한 줄 해설** 채택("선언문은 실행 순서와 무관 · 디버거가 선언문에 멈추지 않는 이유"). 코드 순서를 그대로 둔 이유는 위젯 인계 표 참조. | 반영 |
| C033 | L02 | 심화 콜아웃 풀이 보강 — `C`(글자)·`N`(숫자만 담는 문자) 한 줄 풀이, `sy-datum`(시스템이 채워 주는 오늘 날짜) 풀이, "앞에서부터 4·2·2자리로 나뉘어 담긴다"는 결과 서술. | 반영 |
| C034 | L02 | SE11 생성 절차 실물 순서로 교정 — **Data type 라디오 → 이름 입력 → Create → 팝업에서 Structure**(CH03-L02와 동일 패턴) + 🖱️ 주의 콜아웃 + **저장 시 패키지 단계**(학습 중엔 Local Object/`$TMP`) 추가. | 반영 |
| C035 | L02 | 금액=CURR+통화 키 📝 참고 1건("예제라서 DEC, 실무는 CURR+통화 키, 짝 맺는 법은 테이블 챕터") + front-matter `foreshadow` 등재. | 반영 |
| C036 | L02 | `name`이 `string`→CHAR 20으로 바뀌는 다리 문장(화면·테이블 칸은 폭이 정해져야 한다). | 반영 |
| C042 | L02 | 축 이름 정렬 — 도입부에 **"Local Structure ↔ DDIC Structure"로 부른다** 선언 + "ABAP Dictionary·DDIC·SE11은 같은 곳(이름·줄임말·T-code)" 1문장. 본문 잔여 '로컬' 4곳도 Local로 통일, 절 제목도 `## 언제 Local, 언제 DDIC?`. | 반영 |
| C043 | L05 | 조사 누락 교정 — `gs_line` **메모리에는** → `gs_line`**의** 메모리에는. | 반영 |
| C044 | L03 | 용어 통일 — `.INCLUDE` 설명의 **'평평하게(직접 필드처럼)'** → **'펼쳐져서(내 필드처럼)'**(같은 레슨의 '펼쳐 담기/펼쳐짐'과 일치). | 반영 |
| C045 | L03 | 요약 카드 문체 완화 — 등호로 항을 잇던 콜아웃을 **서술어 있는 두 문장**으로 재작성. | 반영 |
| C046 | L02 | Component 풀이 통일 — `[[Component]](필드)` → `[[Component]](구성 요소)`(L01과 동일). | 반영 |
| C047 | L01 | 주어 보강 — "얼마나 나아지는지는 규모로 체감된다" → **"타입으로 묶으면 얼마나 나아지는지는 규모가 커질수록 또렷해진다"**. | 반영 |
| C048 | L02 | 문장 재작성 — "Component를 Data Element로 준 덕에 라벨까지 얹혀 있는" → **"각 Component의 타입을 Data Element로 지정한 덕분에 라벨(필드 설명)까지 함께 따라온"**. | 반영 |
| C049 | L01 | 접두어 관례 배경 🧑‍💻 1건 — 헝가리안 표기법을 안티패턴으로 배운 경력자용 설명(선언이 멀리 떨어진 긴 프로그램에서 접두어가 사실상 유일한 단서 · 표준 교재 관례). | 반영 |
| C050 | L01 | SQL `LIKE` 무관 🧑‍💻 1줄. | 반영 |
| C051 | L01 | struct 대응 🧑‍💻 1건 — "값만 묶는 그릇, 메서드는 붙이지 않는다"(동작까지 묶는 도구는 뒤 챕터, 정의 선노출 없음). | 반영 |
| C052 | L01 | 제목 교정 — `## 여러 명 담기 — 이번엔 모양 선언이 반복된다` → **`## 이번엔 모양 선언이 반복된다`**(Structure가 컬렉션이라는 오해 제거). | 반영 |
| C053 | L04 | 매퍼 대응 🧑‍💻 1줄(ModelMapper·AutoMapper 자리 · 다른 점은 라이브러리가 아니라 문법 내장). | 반영 |
| C054 | L04 | `zst_person` 출처 — 본문에 **[DDIC Structure](CH05-L02.html) 링크 1문장** + front-matter `prereq`에 `CH05-L02` 추가. | 반영 |
| C056 | L01 | `gs_p2` amount 공란 의도 주석 2줄("일부러 비워 둔다" · "안 넣은 칸은 초기값 0.00으로 남는다") — 뒤의 초기값 콜아웃과 연결. | 반영 |
| C057 | L01 | LIKE 절이 불편 없이 정의로 들어가던 문제 — **재방문 프레임**(아래 AX-1)과 함께 **TYPES↔LIKE 선택 기준** 1문단 추가("모양에 이름 붙여 여러 곳에 쓸 거면 TYPES, 잘 만든 변수의 짝 하나면 LIKE"). | 반영 |
| C058 | L01 · L02 · L03 | 말미 예고 링크화(R5) — 평문 "다음 레슨"을 각각 [DDIC Structure](CH05-L02.html) · [Structure 재사용 — 중첩 · .INCLUDE · .APPEND](CH05-L03.html) · [Structure 다루기](CH05-L04.html) 링크로. 내부 ID 노출 0. | 반영 |

## 축 라우팅 이행

| ID | 파일 | 변경 요지 | 상태 |
|---|---|---|---|
| **AX-1** | L01 | LIKE 원장 중복 해소 — front-matter `introduces`에서 **"LIKE(동일 모양 선언)" 제거**(L3 정식 도입은 CH02-L01), 절 제목을 `## LIKE 다시 만나기 — 변수를 보고 똑같은 모양으로`로 바꾸고 **"[Chapter 2 · 변수 선언(DATA)](CH02-L01.html)에서 이미 배운 키워드 · Structure에도 그대로 쓴다 · 새 문법이 아니다"** 재방문 프레임 2문장. `keywords`의 LIKE는 검색어라 유지. | 반영 |

## 보류 — 위젯 작업자 몫 (9) · 본문이 먼저 확정한 좌표

| ID | 위젯 | 본문에서 확정된 것 | 위젯 쪽 해야 할 일 |
|---|---|---|---|
| C015 | L04-S01 | (본문 변경 없음) 통째 대입·CLEAR는 본문 문단·🧑‍💻로만 보강됨. | 통째 대입/CLEAR 체험 추가(모드 추가 또는 소형 위젯). |
| C021 | L05-S01 | 본문 메인 코드 블록 **무수정**(줄 번호 그대로). 단 아래 "새 코드 블록" 항목 참조. | 스텝↔줄 1:1 복구. |
| C027 | L03-S01 | 본문 ① 중첩 예제가 **`order_id`·`head`·`qty`** 로 바뀌어 위젯 `own: ['order_id','qty']`와 **정합**. | 사실상 해소 — 위젯 수정 없이 본문과 일치하는지 확인만. |
| C031·C039 | L04-S01 | 본문 타입 표기(`c LENGTH 20`)·따옴표 규약 **무수정**(본문이 정본). | 위젯의 `string` 표기·칸 값 따옴표를 본문 규약에 맞춤. |
| C037 | L02-S01 | 본문 Component 표(NUMC 3 · DEC 8,2) 유지. | 초기값 표기를 `000`·`0.00`으로. |
| C038 | L03-S01 | 퀴즈 문항 내용은 본문과 정합(경로 3종). | 정답 위치 분산. |
| C040 | L01-S02 | 본문 표기 `p LENGTH 8 DECIMALS 2`가 정본. | 위젯의 `p 8,2` 표기 교정. |
| C041 | L01-S06 | 본문에서 이 임베드가 **TYPES 절 뒤로 이동**(C028) — 캡션도 `번호 변수 참사 vs TYPES + Structure`. | 좁은 폭 2단 고정 해소(캡션 변경분도 함께 확인). |

**추가 인계 좌표(본문이 먼저 바뀐 곳)**

- **L05에 새 코드 블록 신설**(C005: `gs_keep`·`gs_report` + MOVE-CORRESPONDING + CLEAR). 현재 이 블록에는 전용 체험이 없다 — L05-S01 개조(C021) 때 스텝을 여기까지 넓힐지, 소형 위젯을 붙일지는 위젯 작업자 판단.
- **L01 임베드 순서 변경**: S07 → S02 → S03 → S01 → **(중간 정리)** → **S04 → S06** → S05. S06이 TYPES 절 뒤로 내려갔다.
- **L01 LIKE 예제 코드는 순서 무수정**: `DATA gs_p4`를 위로 올리지 않고 해설로 처리(C032). L01-S05 위젯이 "선언문은 실행 스텝이 아니다"를 **이 줄 순서 그대로** 가르치고 있어, 코드를 옮기면 위젯과 어긋나기 때문이다.
- **L03 ② `.INCLUDE` 본문이 위젯 config를 따름**: 담는 대상 `ZST_AUDIT`(`created_by`·`created_on`), 접근 경로 `gs_order-created_by`. 비교표도 이 경로로 맞췄으니 위젯 ②는 수정 불요.
- **L03 ③ `.APPEND` 접근 경로 `gs_mat-zzgrade`** 를 본문에 명시(위젯 path와 동일).

## 기각 8건 — 무수정 확인

`git diff` 기준 아래 문장·구간은 **한 글자도 바뀌지 않았다**(삭제 라인 grep으로 확인).

- **C007** L02 밑줄 위치 제한 항목 전체 · **C011** `'1500.50'` 등 소수 리터럴 표기 · **C018**·**C023** `.APPEND` 서술 골격 두 문장("별도 Structure로 덧붙인다" · "업그레이드에 안전하다(확장 주제, 자세히는 한참 뒤 챕터에서)") · **C025** L05 `BREAK-POINT` 시연 문단 · **C026** 위젯 · **C029** 건너뛰기 이정표 미도입(⏩ 지름길 콜아웃 0건, 타 언어 대응은 C049·C050·C051·C053의 🧑‍💻 한 줄로만) · **C055** `_chapter.md` 미수정(diff 0).

## 검증 (빌드 제외)

- **내부 레슨 링크 25/25 실존**(전 링크의 `CHnn-Lmm` 소스 파일 확인) · 챕터 직접 링크 0 · 본문 프로즈에 내부 ID 노출 0(`::embed` 지시자 제외).
- **glossary 패리티**: 본문 `[[마킹]]` 전수가 `reference/glossary.json`에 존재(미정의 0). **키 추가 0건** — 새 마킹은 기존 키 `[[Internal Table]]` 1건뿐, 새로 쓴 용어(NUMC·CURR·헝가리안 표기법)는 마킹 없이 인라인 풀이로 처리.
- **embed 12/12 실존**(`embeds/abap/*.html`).
- **렌더 함정 점검**: 닫는 `**` 앞이 백틱·괄호이고 뒤가 한글인 패턴(CH04 세션이 찾은 CommonMark 결함) **0건**.
- 프로즈 반각 `~` 잔존 3건은 **전부 기존 문자열** `BEGIN OF ~ END OF`(레슨 제목·direction·절 제목) — 범위 표기가 아니라 구문 표기이고 클러스터 대상도 아니라 무수정.
- R15 점검: 새로 들어온 문법·용어 중 미학습 요소 0. New Syntax·New Open SQL 0(R6). 사람 이름은 전부 풀 안(정훈영·손흥민·유재석·강호동), 1번 정훈영 유지(R9).
- 빌드·브라우저 검증은 지시대로 **미실시**(본선 몫).

## 무단 수정 0 확인

- 이 세션이 만든 변경 = **`content/abap/CH05/` 5파일뿐**. 모든 diff 헝크가 위 표의 클러스터 ID에 매핑됨(헝크↔ID 대조 완료).
- ⚠️ **`.claude/launch.json`이 working tree에 수정 상태로 존재하나 이 세션의 변경이 아니다**(python 서버 실행 설정 → `url` 부착형으로 바뀐 diff). 본문 작업 중 이 파일을 연 적이 없다. 동시 진행 중인 다른 세션(위젯 작업자의 preview 기동 등)의 산물로 보이며, 범위 밖이라 **되돌리지 않고 그대로 두었다** — 커밋 시 본선이 포함 여부를 판단할 것.
