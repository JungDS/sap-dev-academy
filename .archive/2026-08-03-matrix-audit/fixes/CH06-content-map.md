# CH06 content 반영 매핑 — 매트릭스 감사 배치 2

> 작업일: 2026-08-09 · 실행자: Opus (CH06 본문 보강 작업자)
> 입력: `verdict/CH06.md`(정본, 채택 테마 T1～T5 + 축 항목 AX-2) · `merged/CH06.json`(69클러스터) · 대상 `content/abap/CH06/**`
> 범위: **content .md 6파일**(`CH06-L01`～`CH06-L06`). `_chapter.md` 미수정(C020 기각) · `embeds/**` 미수정(위젯 작업자 몫) · `docs/**`·타 챕터·`.project-docs` 미수정 · `reference/glossary.json` 키 추가 **0건** · git commit 미실행(본선 몫).
> 빌드: 지시대로 `npm run build:abap` **미실행**(본선 일괄 점검).

## 집계

| 처리 | 수 |
|---|---|
| 반영 | **48** (+ 축 항목 AX-1 대응인 **AX-2** 별도 1행) |
| 부분(지시가 정한 범위만) | **6** (C005 · C011 · C013 · C024 · C040 · C054) |
| 보류 = 위젯 작업자 몫 | **9** (C006 · C041 · C042 · C043 · C044 · C045 · C058 · C059 · C060) |
| 기각(지시대로 무수정) | **5** (C012 · C020 · C039 · C047 · C057) + C002 내 5벌 |
| 보류 = 판정문 미등재 | **1** (C055) |

69클러스터 = 반영 48 + 부분 6 + 위젯몫 9 + 기각 5 + 판정 미등재 1. 누락 0.
**diff 대응**: `git diff -- content/abap/CH06/`의 54개 hunk가 모두 아래 표의 어느 행에 대응한다(무단 수정 0).

---

## A. 채택 — 본문 반영

| ID | 파일 | 변경 요지 | 상태 |
|---|---|---|---|
| C016 | CH06-L01 | 접두어 불릿에서 Header Line 선노출 제거. `gt_` 설명만 남기고 `it_`는 레슨 끝 "옛 코드에서 보이는 것들" 콜아웃으로 이관(콜아웃 제목도 "두 가지"→"것들") | 반영 |
| C021 | CH06-L01 | '초기 할당 메모리/초기 할당분/초기 메모리' 3표기를 **초기 메모리**로 통일(코드 주석 2줄 포함) + "선언 시 행 0이어도 미리 잡아 두는 공간" 한 줄 풀이 추가 | 반영 |
| C022 | CH06-L01 | 점검 목록의 '헤더라인·작업영역' → **Header Line·Work Area** 영어 원문으로 통일 | 반영 |
| C026 | CH06-L01 | APPEND 예제의 선등장 `CLEAR gs_person.`에 인라인 주석("다음 행을 채우기 전에 값 비우기, 아래 '비우기'에서 자세히") | 반영 |
| C051 | CH06-L01 | APPEND 값 복사 문단 뒤 `🧑‍💻 다른 언어를 해 봤다면` 콜아웃 1개(Java·C# 참조 시맨틱과 정반대임을 대조) | 반영 |
| **AX-2** | CH06-L01 | 선언 불릿에 `ts_person`이 [Chapter 5 · Local Structure]의 3필드(name·age·amount)에서 `amount`를 뺀 **단순화 버전**임을 반 줄 명시(드리프트 선언) | 반영 |
| C031 | CH06-L02 | `title`·`direction`·첫 섹션 제목의 '내부 테이블' → **Internal Table** 통일. L01 말미 링크 텍스트도 새 제목으로 동기화 | 반영 |
| C004 | CH06-L02 | Table Kind 표 아래 **이진 탐색**(전화번호부 반 가르기)·**해시**(키로 자리 계산) 원리 한 줄씩 신설 | 반영 |
| C010 | CH06-L02 | ③에 종류별 UNIQUE 제약 불릿 추가 — STANDARD=`NON-UNIQUE`만·SORTED=둘 다·HASHED=`UNIQUE`만(기존 "HASHED는 UNIQUE여야" 불릿 흡수). 코퍼스 ABAPTYPES_KEYDEF 확인 | 반영 |
| C033 | CH06-L02 | ② 콜아웃 수정 — 키가 "찾는 기준일 뿐"에서 "`UNIQUE`면 메모리 안 중복 삽입까지 차단, 다만 DB Primary Key와는 다름"으로 기능 보강 | 반영 |
| C014 | CH06-L02 | standard key 풀이 확장 — 문자 계열에 `string` 포함 명시 + `ts_person` 적용 예(`name`만 키, `age` 제외) + '느슨하다'의 뜻(동명이인 미구분) 풀이. 코퍼스 ABENITAB_STANDARD_KEY 확인 | 반영 |
| C034 | CH06-L02 | Index Table 불릿에 "행 번호는 **1번부터**(0번 행은 없다)" 명시 | 반영 |
| C025 | CH06-L02 | 전역 Table Type 생성 절차를 CH05-L02와 같은 형식으로 단계화(Data type 라디오→Create→Table type 팝업 / Line Type 칸 / Short Description·활성화 Ctrl+F3 / `$TMP`) | 반영 |
| C062 | CH06-L02 | 점검 목록의 "측정 후 바꾼다" → "접근 방식이 분명해지고 속도가 실제로 문제가 될 때 바꾼다"로 톤 완화 | 반영 |
| C027 | CH06-L02 | ② 콜아웃의 중간 예고 "(그쪽은 다음 챕터에서 만난다)" 삭제 + L05 점검 항목의 CH07 직링크 제거 → 본문 CH07 예고 4회에서 3회(L01 도입·L02 약속·L06 마무리)로 축소. **L02의 "다음 챕터에서 직접 해 본다" 약속 문장은 지시대로 유지** | 반영 |
| C003 | CH06-L03 | `INSERT ... INTO TABLE`의 '키에 맞는 자리' 일반화 폐기 → "**테이블 종류가 정하는 자리**"로 바꾸고 종류별 표 신설(STANDARD=맨 끝·`APPEND`와 동일 / SORTED=키 자리 / HASHED=해시 관리 자리). 코드 주석·후속 콜아웃도 정합화 | 반영 |
| C049 | CH06-L03 | "못 찾으면 `sy-subrc = 4`" → "성공 판정은 언제나 **`= 0`인지**로. BINARY SEARCH가 끝까지 가면 8도 나오므로 `= 4` 비교는 어긋난다" | 반영 |
| C023 | CH06-L03 | READ 성공 시 `sy-tabix` 문장에 "번호가 있는 Index Table(STANDARD·SORTED) 기준, HASHED는 0" 제한 명시 | 반영 |
| C029 | CH06-L03 | `TRANSPORTING`의 공통 의미("어떤 필드를 옮길지 지정") 문단을 `NO FIELDS` 코드 앞에 신설하고, MODIFY의 `TRANSPORTING f1 f2` 불릿에서 그 낱말을 회수 | 반영 |
| C063 | CH06-L03 | '최빈 패턴' → '가장 자주 쓰는 패턴' | 반영 |
| C068 | CH06-L03 | `"몇 번 행에서 찾았나"다` → "몇 번 행에서 찾았는지를 시스템이 알려 주는 것이다"로 서술문화 | 반영 |
| C002 | CH06-L03 | **기존 문장 유지**(코퍼스 ABAPSORT_ITAB: itab expects standard or hashed). 채택된 소수 의견만 반영 — HASHED 정렬의 의미(그 뒤 `LOOP`가 행을 꺼내는 순서가 바뀐다) 2줄 추가 | 반영 |
| C001 | CH06-L04 | 집계 절 첫머리에 `ts_sales`(region `c LENGTH 10` / amount `i`)·`gs_sales`·`gt_sales` 선언 코드 블록 신설, 합치기 절에 `gt_a`·`gt_b` 선언 추가 → 예제 4개가 자기완결. 값 예시(서울 100→30→130)는 위젯 L04-S02 데이터와 일치 | 반영 |
| C009 | CH06-L04 | "왼쪽·오른쪽 = 화면 자리가 아니라 **Structure 선언 시 필드 순서**" 문단 신설 + `ts_sales` 기준 예시 | 반영 |
| C019 | CH06-L04 | '주의 셋' 한 문단을 독립 불릿 3개로 분리하고 "경계가 실행되므로" 등 주술 손질 | 반영 |
| C017 | CH06-L04 | `AT ... ENDAT` 진입 시 Work Area 마스킹 경고 콜아웃 신설 — 그룹 키는 유지, 오른쪽 **고정 길이 문자 필드는 `*`**, 나머지는 초기값, 블록 탈출 시 복원, `SUM`이 그 자리를 채움. 코퍼스 ABAPAT_ITAB 확인 | 반영 |
| C035 | CH06-L04 | COLLECT 제약을 코퍼스 ABAPCOLLECT.md 문구 범위로 정밀화 — ① 키(primary table key) 아닌 필드는 모두 숫자 ② Work Area는 행 타입과 compatible ③ 키가 비면 전 필드 숫자 + 첫 행에만 합산. **문서에 없는 'flat 행 타입' 조건은 쓰지 않음** | 반영 |
| C036 | CH06-L04 | "한 테이블에 `COLLECT`와 `APPEND`를 섞지 말자(같은 키 행이 갈라져 합계가 쪼개짐)" 경고 불릿 | 반영 |
| C037 | CH06-L04 | "대량이면 SORTED·HASHED와 궁합, STANDARD는 다른 문장이 건드리는 순간 처음부터 훑게 되어 느려짐" 불릿(코퍼스 성능·가이드라인 절 근거) | 반영 |
| C038 | CH06-L04 | ASSIGNING 절에 "SORTED·HASHED의 **키 필드**를 `<fs>`로 고치면 런타임 오류" 경고 콜아웃 | 반영 |
| C028 | CH06-L04 | `APPEND LINES OF`에 왜/언제 3문장(두 명단 합치기 상황) + 동작 설명(뒤에 붙음·원본 유지·행 모양 일치) 추가, 코드도 독립 블록으로 분리 | 반영 |
| C061 | CH06-L04 | `::embed CH06-L04-S01`을 ASSIGNING 절 **뒤로 이동**(위젯 제목의 ASSIGNING 선노출 해소). 위젯 파일 자체는 미수정 | 반영 |
| C064 | CH06-L04 | "이번 레슨은 …도구들을 배운다" → "이번 레슨에서는 …도구를 하나씩 익힌다" | 반영 |
| C032 | CH06-L04 | '컨트롤레벨' 첫 등장에 **(Control Level)** 영어 병기 | 반영 |
| C007 | CH06-L04 | `LOOP AT gt_person FROM 2 TO 5 INTO gs_person.` → **`LOOP AT gt_person INTO gs_person FROM 2 TO 5.`**(코퍼스 ABAPLOOP_AT_ITAB: result가 cond보다 앞). 같은 절 WHERE 예제와도 형태 일치 | 반영 |
| C015 | CH06-L05 | '참조(reference)' 한 줄 풀이(값이 아니라 값이 있는 자리를 가리킴, 앞 레슨 Field Symbol과 같은 종류) + "뒤에서 따로 배운다" L1 예고화 | 반영 |
| C030 | CH06-L05 | 'deep 요소'↔'가변 요소' 혼용 정리 — 전부 **가변 요소**로 통일(Flat 정의 문구·코드 주석·본문 포함)하고 그 뜻을 한 문단으로 풀이 | 반영 |
| C052 | CH06-L05 | Flat/Deep을 나누는 배경 문단 추가(고정 길이만 모이면 메모리에서 한 덩어리, 가변 요소는 따로 놓이고 자리만 가리킴) | 반영 |
| C056 | CH06-L05 | 'Deep은 DB 컬럼 불가' 단정 → 불가 범위를 **Internal Table·참조**로 좁히고 `string`은 전용 컬럼 타입이 있어 가능함을 명시 | 반영 |
| C069 | CH06-L05 | Flat/Deep 절 첫머리에 "여기서 말하는 Component는 앞 챕터에서 '필드'라 부르던 그것" 정렬 문장 + CH05-L01 링크 | 반영 |
| C065 | CH06-L05 | 집필 용어 '나선형으로 깊어진다' → "뒤에서 차근차근 넓혀 간다" | 반영 |
| C018 | CH06-L06 | 디버거 관찰 서술 '2개, 4개 … 72개' → "안쪽 루프가 한 번 돌 때마다 **1개, 2개, 3개 … 72개**로 하나씩" | 반영 |
| C066 | CH06-L06 | 중의어 '바깥 단수' → "바깥쪽에서 정한 **단 값**" | 반영 |
| C067 | CH06-L06 | '정렬 전에 기준이 되는 필드를 보존' → "쌓을 때 **순번을 담는 필드**를 하나 더 두고 1, 2, 3 …을 같이 넣어 두라, 그 필드로 SORT하면 입력 순서로 복귀"로 재작성 | 반영 |
| C008 | CH06-L06 | ① '이번 챕터 전체의 결론' → "배운 것들을 한 프로그램에 모으는 자리"로 완화 ② "더 해 보기 — 미션 둘" 절 신설: 미션1 `READ TABLE ... WITH KEY dan/mul` + `sy-subrc = 0` 실패 체험(L03 회수), 미션2 `SORT BY dan` + `AT END OF dan`·`SUM` 단별 소계(L04 회수, AT 마스킹까지 확인). 새 fenced 코드 블록 없이 인라인 지시문으로만 구성(R2 회피) | 반영 |
| C046 | CH06-L06 | 경력자 접기 박스 1개로 통합 처리 — Table Kind 3종 ↔ 리스트/키 순서 맵/해시 맵 대응 | 반영 |
| C053 | CH06-L06 | 같은 박스 — `COLLECT` ↔ 그룹 합산(SQL `GROUP BY` 합계·맵 누적) | 반영 |
| C048 | CH06-L06 | 같은 박스 — `ASSIGNING <fs>` ↔ 참조. "타 언어에선 이쪽이 기본" | 반영 |
| C050 | CH06-L06 | 같은 박스 — `sy-subrc` ↔ 예외 대신 반환 코드 패러다임(미확인 시 조용히 진행) | 반영 |

## B. 부분 반영 (판정문이 부분 채택으로 지시)

| ID | 파일 | 변경 요지 | 상태 |
|---|---|---|---|
| C005 | CH06-L04 | **레슨 분할 미이행**(리넘버급 구조 변경 → 판정문대로 관찰 기록). 대신 부하 완화 — 도입에 "순회 / 집계 두 갈래" 안내 2줄, '중복·개수·합치기' 앞에 "큰 두 갈래는 여기까지" 중간 정리 1줄 | 부분(분할 미이행) |
| C011 | CH06-L03 | **분할 미이행**. 대신 "## 여기까지 한눈에" 정리 표 신설(넣기·찾기·고치기·지우기 × 문장 × 언제) + "네 갈래에 번호냐 키냐가 붙는 구조" 요약 1줄 | 부분(분할 미이행) |
| C013 | CH06-L01 | **R11 `it_` 관례 자체는 유지**(판정문 지시). "이 과정에서는 Header Line 딸린 옛 테이블에만" 스코프 명시 + "현업엔 일반 테이블에도 `it_`를 쓰는 팀이 있으니 이름만 보고 단정 말 것" 반 줄 | 부분(관례 유지) |
| C024 | CH06-L05 | **레슨 이동 미이행**(관찰). 대신 도입에 위치 사유 2줄 추가 — "새 문장을 배우는 자리가 아니라, 캡스톤 전에 구조를 분류하는 눈을 정리하는 짧은 레슨" | 부분(이동 미이행) |
| C040 | CH06-L02 | **순서 재배치 미이행**. 대신 도입 동기 3줄 추가 — "한 줄이 세 가지 결정을 한꺼번에 내린다. 막히는 순간 답은 이 셋 중 하나" | 부분(동기 문장만) |
| C054 | CH06-L06 | 점검 항목 "담았으니 저장됐다는 착각" 1곳 삭제(마무리 문단이 역할 승계). 나머지 반복(L01 콜아웃·L01 점검·L06 마무리)은 판정 범위 밖이라 유지 | 부분(1곳만 축소) |

## C. 기각 — 무수정 (판정문 사전 등재)

| ID | 파일 | 사유 | 상태 |
|---|---|---|---|
| C002(5벌) | CH06-L03 | 'SORT 대상은 STANDARD·HASHED' **본문이 정확**(코퍼스 확인). 문장 그대로 유지, 의미 보강만 A표 C002 행 | 무수정 |
| C012 | CH06-L03 | `READ TABLE`의 `TRANSPORTING NO FIELDS` 선행 배치가 문서 표준 순서(`READ TABLE itab [result] [key]`) | 무수정 |
| C020 | _chapter.md | 챕터 intro 한 줄 형식은 R10 전 챕터 공통 — 정책 관찰 | 무수정 |
| C039 | CH06-L04 | LOOP 중첩 READ 성능 경고는 이 챕터에서 안 가르치는 패턴 → 오히려 선노출 | 무수정 |
| C047 | CH06-L01 | 경력자 건너뛰기 이정표는 전 챕터 미채택 관례 | 무수정 |
| C057 | CH06-L06 | BREAK-POINT 이송 경고는 CH04-L06에 기수록 + 본문이 그 레슨 직링크 | 무수정 |

## D. 판정문 미등재 — 보류

| ID | 파일 | 내용 | 상태 |
|---|---|---|---|
| C055 | CH06-L04 | "LOOP 순회 중 그 테이블을 삭제·삽입으로 바꾸면 행 건너뜀·덤프" 경고 부재. **판정문 CH06.md의 채택(T1～T5)·기각 어느 목록에도 등장하지 않는다**(69클러스터 중 유일). 코퍼스 ABAPAT_ITAB에는 "The internal table must not be modified within the LOOP loop" 근거가 있어 채택 여지가 있으나, 무단 반영 금지 원칙에 따라 미반영 | 보류(판정 누락 → 본선 확인 필요) |

## E. 위젯 몫 — 위젯 작업자 인계 좌표

본문은 손대지 않았다(C061의 임베드 줄 이동만 본문 몫으로 처리). **이번 본문 보강으로 위젯이 맞춰야 할 기준이 바뀐 항목에 ★ 표시.**

| ID | 위젯 파일 | 요지 · 본문 정합 좌표 |
|---|---|---|
| C006 | `embeds/abap/CH06-L06-S01.html` | 행 수 81 → **72** 전면 교정(리드문·③④ 라벨·마지막 idx). 기준 = L06 "쌓기" 코드 `DO 8 TIMES` × `DO 9 TIMES` |
| C042 ★ | `embeds/abap/CH06-L06-S01.html` | 정적 스냅샷 → 관찰 가능한 형태. 본문 L06 "디버거로 행들 보기"가 **"1개, 2개, 3개 … 72개로 하나씩"**(C018 반영)으로 바뀌었으므로 위젯도 1씩 증가를 보여야 정합 |
| C041 ★ | `embeds/abap/CH06-L03-S01.html` | READ 실패 스텝에서 `sy-tabix`를 0으로. 본문 L03이 "성공 판정은 `sy-subrc = 0`"(C049)·"HASHED는 `sy-tabix` 0"(C023)으로 바뀌어 위젯 문구도 이 기준에 맞출 것 |
| C058 ★ | `embeds/abap/CH06-L04-S02.html` | `LOOP AT`·`AT NEW`·`AT END OF`·`ENDAT` 줄 강조 추가. 본문에 **AT 마스킹 경고**(C017)가 새로 들어갔으니, AT 블록 진입 시 `gs_sales-amount`가 0으로 비고 `SUM` 후 합계가 채워지는 흐름까지 보이면 정합↑. 위젯 데이터(서울 100/30, 부산 50/20)는 본문 새 예제와 이미 일치 |
| C059 ★ | `embeds/abap/CH06-L01-S01.html` | 마지막 `FREE` 스텝에 시각 변화 부여. 본문 L01이 '초기 할당 메모리'→**'초기 메모리'** 단일 표기(C021)로 정리됐으니 위젯 콘솔 문구도 동일 표기로 |
| C043 ★ | `embeds/abap/CH06-L03-S01.html`(또는 신규) | `INSERT ... INTO TABLE` 스텝 보강. 본문에 **종류별 삽입 위치 표**(C003)가 신설됐으므로 STANDARD에서 맨 끝에 붙는 동작을 보여야 함 |
| C060 | `embeds/abap/CH06-L03-S01.html` | `TRANSPORTING NO FIELDS`(존재 확인) 스텝 추가 |
| C044 ★ | `embeds/abap/CH06-L04-S01.html` | LOOP `FROM/TO` 모드 추가. **위젯 코드 문자열은 반드시 `LOOP AT gt_person INTO gs_person FROM 2 TO 5.` 순서**(C007 교정 반영) |
| C045 | `embeds/abap/CH06-L04-S03.html`(또는 신규) | `APPEND LINES OF` 소형 체험(재량). 본문에 `gt_a`(지점 명단)·`gt_b`(전체 명단) 선언이 생겼으므로 그 이름·의미를 재사용 |

### 위젯 작업자가 알아야 할 본문 변경(위젯 수정 대상은 아님)
- `CH06-L04-S01` 임베드 줄이 **ASSIGNING 절 뒤로 이동**했다(C061). 위젯 파일은 무변경.
- L04 집계 절에 `ts_sales`(`region TYPE c LENGTH 10` · `amount TYPE i`) 선언이 본문에 생겼다. 위젯이 선언 줄을 노출한다면 이 타입·이름을 그대로 쓸 것.
- L06에 미션 2개(READ / `AT END OF`+`SUM`)가 추가됐다. 별도 위젯 요구는 아니며 SE38 실습 과제다.

## F. 검증 (빌드 미실행 — 정적 점검만)

- `[[마킹]]` 전수 → `reference/glossary.json` 미정의 **0**(신규 키 추가 없음. 새로 쓴 `[[Component]]`는 기존 키).
- 레슨 링크 `(CHnn-Lmm.html)` 전수 → 소스 실존 **전부 통과**(L02 제목 변경에 따른 L01 링크 텍스트 동기화 완료, 구 제목 잔존 참조 0).
- `::embed` 11건 → `embeds/abap/*.html` 실존 **전부 통과**.
- R6 게이팅 → 인라인 `DATA(`·`VALUE #(`·`NEW #(`·`+=`·`@` 등 modern 문법 **0건**.
- 코드 펜스 전수 ` ```abap ` 태깅 유지, 본문 직접 HTML 없음(`<details>`는 CH02·CH04 기존 패턴. 파서 리스크를 피해 접기 박스 안은 표 대신 불릿 사용).
- 작업 중 워킹트리에 **CH05 6파일·`.claude/launch.json` 변경이 병렬 세션에서 발생**했다. 본 작업의 diff는 `content/abap/CH06/` 6파일뿐이며 타 경로는 건드리지 않았다.

## 본선 부기 (판정 누락 정정)

| C055 | CH06-L04.md | 점검 목록에 "LOOP 도는 중 행 추가·삭제" 경고 1건 추가(루프 밖 DELETE WHERE 안내, `<fs>` 값 수정은 안전 구분) | 본선 직접 반영 — 작업자 보류 보고를 받아 코퍼스(ABAPLOOP_AT_ITAB Caution) 근거로 채택 확정 |
