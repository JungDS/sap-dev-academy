# CH04 위젯 반영 매핑 — 매트릭스 감사 백로그

> 작업일: 2026-08-03 · 실행자: Opus (CH04 위젯 보강 실행자)
> 입력: `verdict/CH04.md` · `merged/CH04.json`(위젯 몫 10) · `fixes/CH04-content-map.md` §위젯 작업자 인계 표 · `embeds/_engine/AUTHORING.md`
> 범위: `embeds/abap/CH04-*.html` · `embeds/_engine/step-debugger.{js,css}` · `embeds/abap/_index.md` · 신규 위젯용 `::embed::` 선언 **1줄**(`content/abap/CH04/CH04-L03.md`)
> 검증: `npm run build:abap` **통과**(glossary parity 0 · pages 270) · `node tools/gen-embed-dark.mjs` 재생성 후 `_dark.css` **diff 0** · 브라우저 실측 = **http://localhost:8143**(기동/종료 미실시) · **콘솔 오류 0**

## 집계

| 처리 | 수 |
|---|---|
| 반영 | **10 / 10** (C004 · C005 · C006 · C007 · C021 · C028 · C029 · C053 · C054 · C055) |
| 보류 | **0** |
| 신규 위젯 | **1** (`CH04-L03-S04`) |
| 엔진 수정 | **1** (`step-debugger` — 선택 레이어 추가, 소비자 11/11 회귀 통과) |

---

## 클러스터별 처리

| ID | 파일 | 처리 | 한 줄 설명 |
|---|---|---|---|
| **C004** | `CH04-L04-S01.html` · `CH04-L04-S02.html` | 반영 | `WHEN OTHERS` 출력을 본문 정본 **`'확인 필요'`** 로 통일 — S01은 `CASE_CFG` variant 1의 `code`·`out` 둘 다, S02는 mermaid 노드 라벨과 코드카드 문자열 둘 다. 실측 결과 두 파일에서 `'등급 없음'` **잔존 0**, S04까지 세 위젯이 같은 문자열. |
| **C055** | `CH04-L04-S04.html` | 반영 | 본문에 없는 **D등급('미흡') 제거** — IF 칼럼의 해당 `<p>` 한 줄과 CASE 칼럼 문장의 `'D'일 때 '미흡'을` 구절 삭제. 이제 양쪽 다 **A·B·C + 그 외('확인 필요')** 로 본문 예제와 등급 수가 일치(나란히 비교 취지 복원). |
| **C005** | `CH04-L01-S01.html` | 반영 | 스텝 **4 → 10**으로 확장. 커버: 사칙연산 4종 · **`2 ** 3 ** 2` = 512**(우결합, 콘솔에 "왼쪽부터였다면 64" 대비) · **정수 나눗셈 함정**(`gv_i = 10 / 4` → 3 과 `gv_q = 10 DIV 4` → 2 를 워치에 **동시 표시**해 정반대임을 눈으로, `gv_dec` → 2.50로 타입 대조) · `17 MOD 5` · **날짜 산술**(`'20260131' + 1` → `20260201` 달 넘김). 문장형(ADD류)은 본문에서 `<details>` 강등이라 제외(지시대로). |
| **C006** | `CH04-L03-S01/S02/S03.html` + **신규 `S04`** | 반영 | 정적 그림 3종에 **최소 인터랙션** 부여(재제작 아님 — SVG 골격·문안 유지, 상태 클래스와 조작 패널만 추가). ① **S01**: `pa_amt` 입력·슬라이더·프리셋 → **참이 되는 가지만 점등, 나머지 흐림** + 판정 문장. ② **S02**: `pa_amt` 슬라이더 + `pa_vip`·`pa_block` 스위치 → 조건 칩이 참(초록)/거짓(분홍)으로 물들고 **판정 보드가 괄호→NOT→AND 순서로 실시간 계산** + **선언 포함 코드 스트립**(인계 표 요구). ③ **S03**: 값 입력 시 해당 카드 점등 + 프리셋 **[아무것도 입력 안 함]/[0 직접 입력]이 둘 다 참**으로 나와 본문 주의를 손으로 체감(둘 다 눌러야 경고 노출). ④ **S04 신설**: '(중요)' abap_bool 절의 R2 공백 해소. |
| **C007** | `CH04-L06-S01.html` (+엔진) | 반영 | **Watchpoint 체험 포함형으로 개조** — F5 재탕 해소. 본문 절차와 **같은 순서**(＋Watchpoint 만들기 → 변수 이름 입력 → 저장 → 목록에서 확인·✕삭제)로 걸고, **계속(F8)** 이 그 값이 바뀌는 줄에서 자동 정지. 제목·라벨 전부 **`Watchpoint`** 한 단어 표기(본문 C073과 일치). 세션 한정 성격도 재현(↻ 다시를 눌러도 걸어 둔 것은 유지). |
| **C053** | `CH04-L06-S01.html` | 반영 | C007 개조에서 **코드를 구구단 → 합계 누적으로 교체**해 동시 해소. 다음 레슨 `CH04-L07-S01`의 정답 4개(`TIMES`·`sy-index`·`*`·`ENDDO`)가 완성형 구구단으로 선노출되던 문제가 사라짐(교체 코드에 곱셈·구구단 출력 없음). |
| **C029** | `CH04-L06-S01.html` | 반영 | 스텝-코드 불일치 해소 — 새 config는 **마지막 줄(11번 `WRITE`)까지 1:1**. 실측: F5 10스텝 완주 후 `.done` 줄 = **11 = 총 코드 줄 수**, 3회차 조기 '완료' 없음. |
| **C021** | `CH04-L02-S01.html` | 반영 | 빈칸 **4 → 9**로 확장: `INTO` · **`&&`** · `AT` · `strlen` · `FIND` · **`REPLACE`** · **`CONDENSE`** · **offset `+4(2)`** · **`TRANSLATE`**(승격분 포함). 본문에서 참고로 강등된 `SHIFT`·`OVERLAY`·`WRITE TO`는 **제외**(introduces에서도 빠짐). 9칸 전부 aria-label 부여 + `👁 잠깐 보기` 버튼 추가. |
| **C028** | `CH04-L07-S03.html` | 반영 | 힌트 코드의 **루프 밖 `EXIT` 2회를 본문 확정형으로 교체** — `IF … WRITE 안내 … ELSEIF … WRITE 안내 … ELSE(여기에 반복) … ENDIF`, **EXIT 미사용**. 딸린 설명도 정합화: '프로그램 종료' ×3 → "안내를 찍고 **반복은 건너뜀**", 'ENDIF 다음 문장 실행' → "**ELSE 안의 반복** 실행". `EXIT`는 반복 탈출 전용이라 이 자리에 안 쓴다는 주석 1줄 추가. **양 끝 관문(2～9)은 유지하되 '본문에 없는 추가 규칙'으로 명시**(②번만 만들어도 충분하다고 안내). |
| **C054** | `CH04-L07-S02.html` | 반영 | 두 번째 빈칸(`data-answer="DO"`)에 `aria-label="빈칸2"` 추가 → 5칸 전부 `빈칸1`～`빈칸5`로 형식 통일. 실측 `missingAria = 0`. |

### 재량 판단 기록

- **C006 신규 위젯 = 만듦**(재량 항목). 근거: 본문이 '(중요)'로 표시한 절에 코드가 있는데 체험이 없어 **R2 위반**이 남는다. ID `CH04-L03-S04`, **`xsdbool( )`만 노출**(`boolc( )`는 본문에서 각주 강등이라 제외 — 실측 `boolc` 문자열 0), 코드는 `DATA gv_flag TYPE abap_bool.` 선언 포함형.
- **C028 '양 끝 관문' = 유지 + 라벨링**(재량 항목). 통째로 지우면 잘 만들어진 인포그래픽(IF 좌/우 조건 → ELSEIF → 2～9 가둠 증명)이 함께 죽는다. 대신 **본문이 요구한 것은 ②번(시작 ≤ 종료) 하나**임을 명시하고 ①번은 '추가 규칙'으로 정직하게 표기.
- **반각 `~` → 전각 `～`**(인계 표에서 "코드 스팬이라 강제 아님·재량"): L07-S02·S03의 **프로즈·주석 6곳 전부 전각으로 통일**(본문 C045와 같은 기준). 실측 두 파일 반각 `~` **잔존 0**.
- **부수 정합 1건** — `CH04-L03-S03.html`의 출력 문구가 본문 C035 반영 전 문자열 `'입력값이 없습니다'`로 남아 있어, C006으로 이 파일을 손보는 김에 **본문 정본 `'금액이 0입니다'`로 교체**(새 불일치 예방).

---

## 엔진 영향 — `step-debugger` (+ Watchpoint 선택 레이어)

**수정 전 소비자 전수 grep** (`grep -l '_engine/step-debugger\.js' embeds/abap/*.html`) = **12개**:
`CH04-L01-S01` · `CH04-L05-S01` · `CH04-L06-S01` · `CH05-L01-S01` · `CH05-L01-S05` · `CH05-L05-S01` · `CH06-L01-S01` · `CH06-L03-S01` · `CH06-L03-S02` · `CH06-L04-S01` · `CH06-L04-S02` · `CH10-L07-S01`
> ※ `CH10-L07-S02`는 주석에 'step-debugger' 문자열만 있고 실제 엔진은 `can-book-toggle` — 소비자 아님(초기 grep 오탐을 실측으로 정정, `_index.md` 집계 **12** 유지).

**설계 = 순수 추가 · 마크업 게이트**
- 엔진은 `root.querySelector("[data-wp]")`가 **있을 때만** Watchpoint 블록에 진입한다. **config 스키마는 무변경**(`.stepper-config`에 새 키 없음) → 마크업이 없는 나머지 11개는 코드 경로 자체에 도달하지 않는다.
- 감시 후보 목록(datalist)은 `cfg.vars`에서 **자동 파생** — 엔진에 레슨 데이터 하드코딩 없음(AUTHORING §2 준수). 로직=엔진, 마크업·데이터=인스턴스.
- CSS `.wp*`는 **전부 `var()` 토큰**. `step-debugger.css`는 `gen-embed-dark`가 "수동 `html.dark` 보유"로 **스킵**하는 파일이라 하드코딩 라이트색을 넣으면 다크가 깨지는데, 토큰만 써서 짝 규칙이 불필요하다. 재생성 후 `_dark.css` **diff 0**으로 확인.
- 인덱스 주석(04 R8): `step-debugger.js` 상단에 마크업 훅·동작·"기존 소비자 미진입" 경고를, `step-debugger.css` 상단에 섹션 목록과 "이 파일은 gen-embed-dark 스킵 대상" 주의를 추가.

**회귀 — 소비자 11/11 통과**(브라우저에서 iframe으로 실로드 후 `▶ 시작` → `다음 ⏭` 완주)

| 위젯 | 코드 줄 | wp 패널 | 스텝 완주 | 마지막 `.done` |
|---|---|---|---|---|
| CH04-L01-S01 | 20 | 없음 | 10 / 10 | 20 |
| CH04-L05-S01 | 5 | 없음 | 13 / 13 | 5 |
| CH05-L01-S01 | 13 | 없음 | 6 / 6 | 13 |
| CH05-L01-S05 | 9 | 없음 | 4 / 4 | 9 |
| CH05-L05-S01 | 13 | 없음 | 10 / 10 | 11 |
| CH06-L01-S01 | 23 | 없음 | 11 / 11 | 23 |
| CH06-L03-S01 | 19 | 없음 | 10 / 10 | 19 |
| CH06-L03-S02 | 8 | 없음 | 3 / 3 | 7 |
| CH06-L04-S01 | 9 | 없음 | 14 / 14 | 9 |
| CH06-L04-S02 | 20 | 없음 | 18 / 18 | 20 |
| CH10-L07-S01 | 23 | 없음 | 6 / 6 | 22 |

→ 전 소비자가 수정 전과 동일하게 동작하고 **Watchpoint UI가 생성되지 않음**(게이트 정상). 콘솔 오류 0.

---

## 브라우저 실측 (http://localhost:8143 · 콘솔 오류 **0**)

> ⚠️ 실측 중 확인한 함정: 로컬 서버가 정적 캐시를 그대로 물려 **수정 전 위젯/엔진이 로드**된다(초기 측정이 옛 4스텝 config였음). `fetch(url,{cache:'reload'})`로 대상 파일을 강제 재검증한 뒤 재측정해야 실제 변경분이 잡힌다. 스크린샷 대신 **DOM 측정**으로 검증(P3).

| 위젯 | 재현 시나리오 | 실측 결과 |
|---|---|---|
| `CH04-L01-S01` | 시작 → 끝까지 스텝 | `0/10` → `10/10` · `gv_i=3` **·** `gv_q=2` 동시 표시(함정 대조 성립) · `gv_dec=2.50` · `gv_result=512` · `gv_date` `20260131` → **`20260201`** · 마지막 `.done` = **20 = 총 코드 줄** |
| `CH04-L02-S01` | 빈칸 수·aria·정답보기→채점 | 빈칸 **9** · aria 누락 **0** · 정답 `INTO,&&,AT,strlen,FIND,REPLACE,CONDENSE,+4(2),TRANSLATE` · 채점 "✓ 정답입니다!" · 9칸 전부 `.ok` |
| `CH04-L03-S01` | `pa_amt` = 2500 / 500 / 0 / -100 | 점등 노드 `d1,y1,b1` → `d1,n1,d2,y2,b2` → `d1,n1,d2,n2,b3` → 동일 · 출력 문장 3종 정확 · 프리셋 클릭 시 입력값·SVG 라벨 동기(`pa_amt = 500`) |
| `CH04-L03-S02` | 3000/-/- · 8000/-/- · 3000/VIP/- · 3000/VIP/BLOCK | 판정 보드 badge = 거짓·거짓·**거짓** / 참·거짓·**참** / 거짓·참·**참** / (NOT 거짓)→**거짓** · 결과 박스 ⛔→✅→✅→⛔ · 코드 스트립에 `pa_vip`·`pa_block` **선언 포함 확인** |
| `CH04-L03-S03` | [입력 안 함] → [0 직접 입력] → [500] | 점등 `y` → `y` → `n` · **둘 다 누른 뒤에만 트랩 경고 노출**(초기 hidden) · `'입력값이 없습니다'` **0건** / `'금액이 0입니다'` 존재 |
| `CH04-L03-S04` (신규) | `pa_amt` 500 → 2000 | 조건 거짓→참 · 한 글자 칸 `␣`→**`X`** · 캡션 `= abap_false (공백)` → `= abap_true ('X')` · IF 거짓→참 · `boolc` **0건** · `DATA gv_flag TYPE abap_bool` 선언 확인 |
| `CH04-L04-S01` | `CASE_CFG` OTHERS 분기 | `out: '확인 필요'` · code `WRITE: / '확인 필요'.` · `'등급 없음'` **0건** |
| `CH04-L04-S02` | mermaid 렌더 + 코드카드 | `.mermaid svg` 렌더 성공 · `'확인 필요'` 존재 · `'등급 없음'` **0건** |
| `CH04-L04-S04` | 두 칼럼 등급 수 | `'D'` **0건** · `'미흡'` **0건** · IF 칼럼 문장 4줄(A·B·C·그 외) · CASE 문장 = A·B·C + '확인 필요' |
| `CH04-L06-S01` | ①만들기→②틀린 이름→③`gv_total` 저장→④F8 반복→⑤삭제 후 F8→⑥F5 완주 | ①폼 열림 ②"gv_nope 라는 변수가 이 프로그램에 없습니다" ③칩 `👁 gv_total` 생성·버튼 라벨 "Watchpoint까지"로 변경 ④**정지 5회**: 6번 줄 `0→1200` · `1200→2400` · `2400→3600` · `3600→4800`, 그리고 루프 밖 **10번 줄 `4800→4320`**(할인 줄) → 이후 "끝까지" ⑤칩 삭제 후 F8 = "**걸어 둔 Watchpoint가 없으면 F8은 이렇게 끝까지 가 버립니다**"(본문 흔한 실수와 정합) ⑥F5 10스텝, 마지막 `.done` = **11 = 총 코드 줄**(C029 해소) · 시작 재시작 시 칩 유지(세션 한정 성격 재현) |
| `CH04-L07-S02` | 빈칸 aria | 5칸 = `빈칸1`~`빈칸5` · 누락 **0** · 반각 `~` **0** |
| `CH04-L07-S03` | 힌트 코드·문구 | 힌트 코드에 **`EXIT` 0건** · `ELSE` 존재 · 형태 = `IF … ELSEIF … ELSE … ENDIF` · '프로그램 종료' **0건** · '추가 규칙' 표기 존재 · 반각 `~` **0** · 빈칸 3칸 정상 |

**레슨 페이지(생성물) 로드 — 임베드 18/18 정상 · 자동 높이 일치**

| 페이지 | 임베드 | iframe 높이 / 내부 `.wrap` |
|---|---|---|
| `CH04-L01` | S01 | 1295 / 1293 |
| `CH04-L02` | S01 | 770 / 752 |
| `CH04-L03` | S01·S02·S03·**S04** | 456/454 · 509/506 · 381/379 · **741/738** |
| `CH04-L04` | S04·S02·S03·S01 | 666/664 · 602/600 · 435/433 · 598/596 |
| `CH04-L06` | S01 | 874 / 872 (Watchpoint 패널 포함) |
| `CH04-L07` | S01·S02·S03 | 527/508 · 630/612 · 784/765 |

**다크모드**(셸 `embedTheme`과 같은 방식으로 iframe `<html>`에 `.dark` 주입 후 computed style 측정) — Watchpoint 신규 표면 전부 적응 확인: `.wp` 배경 `srgb .96/.97/.99` → `.14/.16/.20` · `.wp__msg` 배경·글자 반전 · 칩 앰버 틴트 위 밝은 글자(`rgb(232,234,240)`) · 입력칸 `#1f2229` 배경 + 밝은 글자.

## 정합 유지 확인 (인계 표 항목)

- **L05 본문 ↔ S02·S03 코드카드**: 위젯 코드 **미변경**. 대조 결과 `DO 5 TIMES … ENDDO`·`DATA gv_n TYPE i VALUE 1. … ENDWHILE` **문장 전부 동일**(주석 문구만 S03이 "없으면 무한루프!"로 짧음 — 기존 상태, 코드 아님). 갈라짐 **없음**.
- **L07-S02 정본 유지**: `DO 8 TIMES` · `gv_dan = sy-index + 1` · 2～9단 구조 **무수정**(aria·전각 표기만 손봄).
- **L03 코드 노출형**: 코드를 드러낸 위젯(S02 스트립·S04 스트립) 모두 `pa_vip`·`pa_block` 또는 `gv_flag` **선언 포함형**.

## 무단 수정 0 확인

- 이 세션이 만든 변경 = **`embeds/abap/CH04-*.html` 12개**(신규 `CH04-L03-S04.html` 포함) + **`embeds/_engine/step-debugger.{js,css}`** + **`embeds/abap/_index.md`** + **`content/abap/CH04/CH04-L03.md` 1줄**.
- content 본문 문장 수정 **0** — `CH04-L03.md` diff에서 이 세션이 추가한 줄은 `::embed CH04-L03-S04 …` **한 줄뿐**(`git diff content/abap/CH04/` 의 `::embed` 변경 전수 = 이 1줄).
- `docs/**` **직접 수정 0** — 변경분은 전부 `npm run build:abap` 재생성물(R1 준수).
- 다른 챕터 위젯 **0** · `sample/**` **0** · `.project-docs/**` **0** · `reference/glossary.json` **0** · **git commit 미실행**.
- ※ 작업 트리에는 **동시 진행 중인 CH02·CH03 작업자의 변경**(content·embeds·엔진 `ddic-layer-board`/`domain-builder`/`param-screen-lab`/`var-box`/`const-lock-lab`/`int-div-lab`·glossary·`15_AUDIT_MATRIX`)이 함께 존재한다. 위 목록 밖 파일은 이 세션 소관이 아니다. 빌드는 전 챕터를 재생성하므로 `docs/**`에 그 작업분도 함께 반영돼 있다.

## 본선(또는 content 작업자)에 남기는 후속 1건

- **`CH04-L02.md`의 임베드 안내 문구가 이제 좁다** — 위젯은 9종을 다루는데 본문은 여전히 "지금까지의 **잇기·쪼개기·길이·찾기**를 직접 빈칸으로…" + `::embed … | 빈칸을 채워 문자열 함수 완성하기 — CONCATENATE · SPLIT · FIND::`. 문장·제목은 **content 범위**라 손대지 않았다. 한 줄 손질 권장(예: "…잇기·쪼개기·길이·찾기·바꾸기·다듬기를"). 기능·정합에는 영향 없음.
- (참고·수정 안 함) 다크모드에서 `.btn--primary`와 새 `.wp__b--go`는 **동일하게** `--brand`(#7aa2ff) 배경 + 흰 글자로 계산된다 — 대비가 낮은 편이나 **엔진 전반의 기존 관례**이며 이번에 생긴 차이가 아니다(둘의 computed style 일치 확인). 바꾸려면 엔진 공통 버튼 정책으로 다루는 편이 맞다.
