# CH02 content 반영 매핑 — 매트릭스 감사 백로그

> 작업일: 2026-08-03 · 실행자: Opus (CH02 본문 보강 실행자)
> 입력: `verdict/CH02.md` · `merged/CH02.json` · `raw/CH02/*.json`(suggestion 역참조) · 대상 `content/abap/CH02/**`(7파일)
> 범위: **content .md 7파일 + `reference/glossary.json` 키 1개 추가**(새 `[[마킹]]` 근거). embeds·타 챕터·`.project-docs` 미수정. git commit 미실행(본선 몫).
> 검증: `npm run build:abap` **통과**(오류 0 · glossary parity 미정의 0건 · pages 270).

## 집계

| 처리 | 수 |
|---|---|
| 반영 | **53** |
| 위젯 작업자 몫(미착수) | **9** (C009 · C017 · C028 · C030 · C031 · C034 · C061 · C062 · C063) |
| 보류 | **0** |
| 기각(미반영) | 1 (C006 — 지시대로 `7 / 2 → 3.50 (정확)` 주석 **무수정**) |

채택 62 = 반영 53 + 위젯몫 9. 누락 0.

---

## `_chapter.md` (2)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C010 | 반영 | front-matter `intro` + 본문 첫 문단 | intro를 L01 불편 한 줄에서 **챕터 관통 불편 사슬**(담을 곳 없음 → 그릇마다 타입 → 정의 반복 → 못 박을 값 → 언어별 화면 글자)로 확장. 본문 첫 문단도 같은 사슬로 이어 붙임(연동 — intro와 본문이 동일 문장이라 한쪽만 고치면 어긋남). |
| C032 | 반영 | 본문 콜아웃 신설 + 레슨별 분산 | 챕터 레벨 **경력자 경로** 1개 신설(CH01 패턴 `> 🧑‍💻 **…라면**` 그대로) — 훑을 곳 vs 시간 쓸 곳(소수 표기·`LENGTH` 의미·화면 글자 번호) 명시. 레슨 단위 표식은 L01·L02·L03·L04·L05에 각 1개씩(아래 각 행 참조). |

## `CH02-L01.md` (8)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C001 | 반영 | `## TYPE 말고 LIKE` 코드 | 미도입 `TYPE p LENGTH 8 DECIMALS 2` → **기학습 `TYPE i`** 로 교체. "가격 ↔ 합계"라는 짝 변수 의미를 본문 문장으로 살려 `LIKE`의 취지 유지. |
| C002 | 반영 | `## DATA로 변수 선언` 불릿 | 접두어 불릿과 스코프 불릿을 **분리**하고, '전역 = 이 프로그램 안 어디서나 보인다'를 그 자리에 풀이. **다른 프로그램과 공유가 아님**을 명시(시스템 전역 오독 차단). '로컬(`lv_`)'은 이름+나중이라는 L1 예고만. |
| C003 | 반영 | `## 정리` 신설 | 5불릿(리터럴→변수 / 전역·`gv_` / `VALUE`·기본값 / 체인 / `TYPE`↔`LIKE`). 기존 마무리 불편 체인 문단은 `---` 뒤에 그대로 유지. |
| C011 | 반영 | `## 선언과 동시에 초기값` 코드 | `DATA gv_tag TYPE c LENGTH 1 VALUE 'X'.` → `DATA gv_city TYPE string VALUE '서울'.`(기학습 `string`)으로 교체. 미도입 `C`·`LENGTH` 선행 노출 제거. |
| C012 | 반영 | 첫 절 도입 문장 | '리터럴' 첫 등장 자리에 **한 줄 풀이 + `[[리터럴]]` 마킹** 선치(키는 glossary에 기존 존재). front-matter `introduces`에 "리터럴" 추가. |
| C013 | 반영 | `## DATA로 변수 선언` 🧑‍💻 콜아웃 | 경력자 안심 1줄 — 지금 전역인 건 모듈화를 안 배워서일 뿐, **실무는 모듈화 후 로컬이 기본**. 이 레슨에서 새로 챙길 건 `LIKE` 하나라는 점프 안내 동반(C032 레슨 표식 겸). |
| C033 | 반영 | 기본값 문단 | '숫자는 0, 문자는 공백' → **'숫자 타입 0 · 문자 타입 공백'** 으로 좁히고, `N`·`D`·`T`는 공백이 아니라 `0`으로 채워진다는 단서 추가(이름+시점만 = L1). front-matter `foreshadow`에 등재. |
| C040 | 반영 | 기본값 문단(같은 절) | ABAP 기본 타입에 다른 언어의 `null`이 없다 → 선언만 해도 빈 값이 들어 있어 읽어도 오류가 없다는 대조 2줄. AG03 원 제안(‘L01 기본값 문단에’)대로 배치(merged `file`은 L02지만 본문 근거지가 L01). |

## `CH02-L02.md` (13)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C003 | 반영 | `## 정리` 신설 | 5불릿(Complete 정의 / 소수는 따옴표 / D·T는 문자 / F는 근삿값 / 자동 변환 주의). |
| C004 | 반영 | `## 숫자를 적는 법` 코드 **앞** | `> 🔎 **[선행 사용]**` 콜아웃을 **코드보다 먼저** 배치 — P는 다음 레슨 것이고 지금은 소수 표기만 보라는 안내. **P 선언은 유지**(F 대체 안 함, 지시대로). front-matter에 `advanceUse` 키 신설. 사용-예고 역전 해소. |
| C005 | 반영 | `## 정수 연산의 함정` 코드 뒤 | 🧑‍💻 대조 1개 — Java·C#·파이썬은 **버림**(7/2=3), ABAP은 **반올림**(4)로 방향이 정반대. `DIV`는 미학습이라 미언급. |
| C014 | 반영 | 자동 변환 콜아웃 | '잘림'만 있던 서술을 **조용한 잘림 + 실행 중단**(숫자로 못 읽는 문자·범위 초과 시 그 자리에서 멈춤)으로 이원화. 덤프 코드명·예외 클래스명 미노출. |
| C015 | 반영 | 타입 표 F 행 | `3.14` → `'3.14'`(같은 레슨의 "소수는 따옴표" 규칙과 정합). `I` 행 `42`·`-7`은 정수 리터럴이라 현행 유지. |
| C016 | 반영(완화) | 표·콜아웃·정리 전반 | **삭제·분할·이전 없이** 밀도만 낮춤 — F 행의 레퍼런스 수치를 `<details>`로 내리고, 흩어진 논점을 `## 정리` 5불릿으로 회수. 레슨 경계는 그대로. |
| C035 | 반영 | 표 아래 ⚠️ 콜아웃 신설 | F가 근삿값인 **이유** 2줄 — 이진법으로 딱 떨어지지 않는 `0.1` 예시, 열 번 더해도 1이 안 됨 → 돈 계산에 안 쓰는 근거로 연결. |
| C036 | 반영 | 타입 표 F 행 | 레퍼런스 톤(바이트·지수·유효숫자 나열)을 일상어 한 구절로 교체하고, 정확한 범위·유효숫자는 `<details>`로 이동. |
| C037 | 반영 | Complete 콜아웃 · 임베드 도입 문단 | '가변(내용만큼 자람)' 3회 반복 → 표는 유지, 콜아웃은 "알아서 늘어난다", 임베드 도입은 "담는 내용에 맞춰 크기가 변하고"로 각각 다른 말로 받음. |
| C038 | 반영 | 마무리 문단 | 조사 '처럼'이 소프트 줄바꿈으로 앞말과 떨어지던 것 → 줄바꿈 위치를 뒤로 옮겨 `"금액은 소수 2자리"처럼`이 한 줄에 붙게. |
| C039 | 반영 | `## 다섯 타입의 "모양"을 눈으로` | "선언문은 실행해 볼 로직이 없다" → "변수를 선언하기만 해서는 화면에 보이는 동작이 없다"로 구체화. |
| C041 | 반영 | Complete 콜아웃 괄호 | `INT8`에 릴리스 단서 추가 — 비교적 최신 시스템에만 있고, 연습 시스템에서 문법 오류가 나면 없는 타입으로 보라는 실전 안내(버전 숫자 대신 서술). |
| C042 | 반영 | 첫 절 도입 1문장 | "앞 레슨에서 `TYPE` 뒤에 무엇을 적을지는 미뤄 뒀다. 이제 그 자리를 채울 차례다." — 억지 불편 없이 **다음 도구** 연결. `prevRel: next` 유지. |

## `CH02-L03.md` (12)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C018 | 반영 | `P`의 LENGTH 콜아웃 | **결론 선치**(`LENGTH 8` = 숫자 15자리 · 금액은 `P LENGTH 8 DECIMALS 2`면 충분) + 칸 쪼개기·부호 반 칸·8×2−1 공식은 `<details>`("왜 하필 15자리인가")로 이동. 내용 삭제 없음. |
| C019 | 반영 | 마무리 문단 | "이제 표준 타입은 다 봤다" → **"자주 쓰는 표준 타입은 다 봤다"**. |
| C020 | 반영 | 흔한 실수 불릿 | 'N에는 숫자 문자만' → **오류가 아니라 숫자만 남기고 조용히 바뀐다**(`'A1B2'` → `00012`)로 실동작 교정 + "조용한 쪽이 더 위험" 톤. |
| C021 | 반영 | 흔한 실수 불릿 | '돈은 반드시 P' 단정 완화 — 이 과정 범위를 `P … DECIMALS`로 한정하고, **실무 금액은 통화별 소수 자릿수 정보가 더 필요**하다는 예고 1줄(코드·DDIC 세부 없음). front-matter `foreshadow`에 등재. |
| C022 | 반영 | 콜아웃 신설(1개) | `P`의 정확성 전제 — **고정소수점 연산**(Fixed point arithmetic)이 켜져 있어야 하고 SE38 신규는 기본 ON, 옛 프로그램이 이상하면 이 속성부터 확인. |
| C023 | 반영 | '왜 N을 쓰나' 콜아웃 | 한국 5자리 맥락 **유지**하되 기준임을 명시하고, 영문자가 섞이는 나라 우편번호는 `C`를 쓴다는 한 줄 추가. |
| C024 | 반영 | C·N·P 표 + `P` 불릿 | '압축 소수(packed)' → **'패킹 십진수(packed number)'** + "숫자 두 자리를 한 칸에 눌러 담는 방식이라 *packed*", **"정수부도 함께 담는다"** 로 소수 전용 오독 차단. |
| C042 | 반영 | 첫 절 도입 1문장 | "앞 레슨의 타입들은 길이를 신경 쓸 필요가 없었다. 이번엔 반대쪽 도구를 꺼낸다." `prevRel: next` 유지. |
| C043 | 반영 | offset 예제 **앞** 2줄 | 날짜 모양 값을 **일부러 문자(`C`)에 담았다**는 이유와 "실제 날짜는 `D`"를 선치. 변수명·값(`'20260623'`)은 **무수정**(위젯 C062가 본문에 맞추는 몫). |
| C044 | 반영 | 마무리 문단 | 소문자 인라인 `p length 8 decimals 2` → `TYPE p LENGTH 8 DECIMALS 2`(챕터 표기 통일). |
| C045 · C046 | 반영 | offset 절 🧑‍💻 콜아웃 1개 | 두 대조를 한 콜아웃에 — ① 괄호 안은 **끝 위치가 아니라 길이**(Java `substring(시작,끝)` 대비, C# `Substring(start,length)`에 가까움), ② **등호 왼쪽에도 쓸 수 있어** 문자열 일부를 그 자리에서 교체(immutable 언어 출신에게 낯선 지점). |
| C047 | 반영 | front-matter | `foreshadow: ["TYPES(CH02-L04)", "통화별 소수 자릿수·DDIC 금액 타입(CH03)"]` 신설(두 번째 항목은 C021 연동). |
| C003 | 반영 | `## 정리` 신설 | 5불릿(Incomplete 정의 / C 패딩·N 앞 0 / P의 LENGTH=칸 / 금액은 P / offset 규칙). |

> C003은 L01·L02·L03 3개 레슨 공통이라 각 파일 행에 나뉘어 있음(집계는 1클러스터).

## `CH02-L04.md` (3)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C025 | 반영 | `## 아직은 "로컬"이다` 절 + 콜아웃 신설 | 본문의 '전역(DDIC)'을 **"시스템 전체가 함께 보는 자리(DDIC)"** 로 바꾸고, `💡 "전역"이 두 번 나온다` 콜아웃으로 **두 층위**(한 프로그램 안 ↔ 프로그램들 사이)를 명시 대비. L01로 링크. |
| C048 | 반영 | 같은 절 + `## 정리` 불릿 | 명칭 3갈래를 **`Local Type` 한 표기로 통일**(제목은 R3의 "ABAP 고유 용어는 영어 원문"에 따라 그대로 두고, 본문 '로컬 타입'·정리 '프로그램 전용(Local)' 쪽을 맞춤). |
| C049 | 반영 | 도입부 🧑‍💻 콜아웃 | 타입 별칭을 아는 경력자용 지름길 — 첫 코드와 "아직은 로컬" 절만 보고 다음 레슨으로 가도 된다는 1줄(C032 레슨 표식 겸). |

## `CH02-L05.md` (11)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C026 | 반영 | 'Type Group' 콜아웃 | 대상 라벨을 **`📎 옛 코드를 읽을 때만`** 으로 바꿔 독자 분리 + 축약. **Type Group**은 이름만 남기고, '클래스'는 **이름 + 한참 뒤에 배움**(L1)으로 처리, "지금 외울 필요 없음" 명시. front-matter `foreshadow`에 클래스 등재. |
| C027 | 반영 | `## CONSTANTS로 선언하기` 코드 | `gc_10` → **`gc_max_seats TYPE i VALUE 10`**(한 회차 최대 좌석). 대입 방향·재대입 금지·산술(→10, →20) 흐름 그대로. 변수도 `gv_my_var` → `gv_seats`로 맞춤(연동 — 상수만 바꾸면 예제 의미가 어긋남). |
| C050 | 반영 | 도입부 🧑‍💻 콜아웃 | 매직 넘버 3대 문제는 경력자에겐 기지 사실 → `CONSTANTS` 문법과 'SAP 표준 상수' 절만 보고 넘어가도 된다는 1줄. |
| C051 | 반영 | 선언 절 불릿 재배열 | **상수 고유 성질(재대입 = 문법 오류, 컴파일 단계 차단)을 첫 불릿으로 올리고** 대입 일반론('오른쪽 → 왼쪽')을 마지막으로 내림. 중복 방지를 위해 `VALUE` 필수는 `흔한 실수` 쪽 1곳으로만 유지. |
| C052 | 반영 | 'SAP가 미리 만들어 둔 상수' 절 | 풀이 속 낯선 말 '플래그' 제거 → **"예/아니오를 표시하는 값에 쓴다"** 로 일상어 교체. |
| C053 | 반영 | 도입 문단 | ①②③ + 괄호 부연이 한 문장으로 이어지던 것을 **3불릿으로 분해**하고, 용어 도입 문장을 따로 세움. |
| C054 | 반영 | 선언 코드 주석 | 목적어 없던 해명 주석("동작만 보려는 이름…")을 **삭제** — C027로 이름 자체가 뜻을 담게 되어 해명이 불필요해짐. 자리에는 `" 한 회차 최대 좌석`. |
| C055 | 반영 | 도입 문단 + glossary | 이탤릭 *매직 넘버* → **`[[매직 넘버]]`** 마킹(`introduces`와 정합). `reference/glossary.json`에 **키 신설**(title·desc·analogy). |
| C056 | 반영 | front-matter `direction` | "…뜻은 분명하게 변경은 안전하게" → "…이름을 붙여 뜻은 분명하게 하고, 변경은 안전하게 한다." |
| C057 | 반영 | 선언 절 첫 문단 | '한번 정하면' → **'한 번 정하면'**(횟수 의미 띄어쓰기). |
| C058 | 반영 | 선언 절 불릿 | `[[컴파일]]` 첫 등장에 기능 풀이 — "프로그램을 실행하기 전에 **문법과 타입을 검사하는**" + "실행 중 사고가 아니라 아예 실행이 안 된다". |

## `CH02-L06.md` (5)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C007 | 반영 | `## 텍스트를 번호로 빼기` 코드 | `DATA gv_amt TYPE p LENGTH 8 DECIMALS 2 VALUE '1500.00'.` **선언 추가** — 그대로 쳐도 `is unknown` 없이 돈다. `P`는 L03 기학습이라 게이팅 문제 없음. front-matter `prereq`에 `CH02-L03` 추가(연동 — 선언이 L03 문법을 쓰므로 전제 명시). |
| C008 | 반영 | `## 실제로 등록해 보기` 절 신설 | 5단계 절차 — SE38에서 프로그램 열기 → **Goto → Text Elements → Text Symbols** → Sym `001` / Text `금액` 입력 → 저장·활성화 → 실행 확인. |
| C029 | 반영 | 두 문법 표 아래 ⚠️ 콜아웃 | **등록된 글자가 리터럴을 이긴다** — 소스의 `'금액'`만 고쳐도 화면은 안 바뀌고, Text Elements의 001을 고쳐야 한다는 유지보수 함정. |
| C059 | 반영 | 흔한 실수 불릿 | '뒤 챕터' → **[Chapter 15 · 입력 검증과 메시지](CH15-L04.html)**. 대상은 grep 실측 — `CH15-L04`가 `introduces`에 `MESSAGE 타입(I/S/W/E/A/X)`·`메시지 클래스(SE91)`를 갖고 본문이 "이제 정식으로"라 선언한 **MESSAGE 정식 도입 레슨**(CH15-L01 아님). |
| C060 | 반영 | 번호 빼기 절 💡 콜아웃 신설 | '언어별로 등록' 서술 삭제 → **원본(로그온) 언어 한 벌만 SE38에 등록**하고 번역은 **SE63** 별도 공정이라는 프로세스 교정. |

## `reference/glossary.json` (1)

| ID | 처리 | 수정 위치 | 한 줄 설명 |
|---|---|---|---|
| C055 | 반영 | `"매직 넘버"` 키 신설 | 새로 `[[마킹]]`한 용어라 허용 범위 내 **키 추가만**. 기존 키 수정·삭제 0. 빌드 glossary parity 미정의 0건 유지. |

---

## 위젯 작업자 몫 (9 · 본선 지시대로 미착수)

| ID | 파일 | 비고 |
|---|---|---|
| C009 | `embeds/abap/CH02-L01-S02.html` | 미도입 P 노출 + 본문과 다른 `p DECIMALS 2` 표기. |
| C017 | `embeds/abap/CH02-L02-S01.html`(신규 필요) | 정수 나눗셈 반올림 체험 부재. **`::embed::` 선언 미추가**(위젯 파일과 함께 추가하는 게 지시). |
| C028 | `embeds/abap/CH02-L05-S01.html` | 상수 재대입 오류 체험 부재(정적 패널). |
| C030 | `embeds/abap/CH02-L01-S02.html` | '정수 박스엔 정수만' 안내가 L02 자동 변환과 충돌. |
| C031 | `embeds/abap/CH02-L06-S01.html` | fill-blank 힌트가 오답(TEXT-001) 유도. |
| C034 | L01 후반 체험 공백 | VALUE·체인·LIKE 구간 위젯 부재. |
| C061 | `embeds/abap/CH02-L02-S01.html` | `gv_today` 시드 `'20260702'` ≠ 본문 `'20260623'`. **본문 무수정**(위젯이 맞춤). |
| C062 | `embeds/abap/CH02-L03-S01.html` | `gv_date` 시드 `'20260630'` ≠ 본문 `'20260623'`. **본문 무수정**. |
| C063 | `embeds/abap/CH02-L05-S01.html` | 비교 코드 선언 누락(`gv_c`·`gv_a`). |

### 위젯 작업자에게 넘길 좌표 변경 2건 (본문이 먼저 바뀐 지점)

1. **L06 코드 블록에 선언 1줄이 추가됐다**(C007) — `DATA gv_amt TYPE p LENGTH 8 DECIMALS 2 VALUE '1500.00'.`. `CH02-L06-S01`(빈칸 채우기)·`CH02-L06-S02`가 본문 코드를 그대로 비추면 이 줄을 반영해야 한다.
2. **L05 예제의 상수·변수 이름이 바뀌었다**(C027·C054) — `gc_10`/`gv_my_var` → `gc_max_seats`/`gv_seats`. `CH02-L05-S01`은 pi 비교라 직접 충돌은 없으나, 이름 규약이 달라진 점은 알고 있어야 한다.
3. `embeds/abap/CH02-L03-S01.html:25`의 라벨이 아직 **'압축 소수'** 표기다(본문은 C024로 '패킹 십진수(packed number)'로 바뀜). 표기 정합은 위젯 쪽에서 맞추는 게 맞다.

## 기각 1 (무수정 확인)

| ID | 확인 |
|---|---|
| C006 | `CH02-L03.md`의 `gv_amt  = 7 / 2.    " 3.50 (정확)` 주석 **바이트 단위 무수정**(grep 재확인 FOUND). 같은 체인의 `gv_amt TYPE p LENGTH 8 DECIMALS 2` 선언도 그대로. |

## 범위 밖 후속 포인터 → 본선 처리 완료 (부기: 2026-08-03, Fable 5)

- ~~glossary `"P"` title `P (압축 소수)`~~ → **본선이 직접 수정**(C024 연동): title `P (패킹 십진수)` + desc에 "정수부도 함께 담는다" 반영.
- ~~`CH02-L05.md` keywords `매직넘버`~~ → **본선이 직접 수정**(C055 연동): `매직 넘버`로 glossary 키와 표기 정렬.

---

## 무단 수정 0 — diff 자체 점검

`git diff --stat` 기준 이번 작업 산출 (`docs/**`는 `npm run build:abap` 재생성물이라 손수정 아님, R1 준수):

```
 content/abap/CH02/CH02-L01.md | 48 +++++++--------
 content/abap/CH02/CH02-L02.md | 52 +++++++++--------
 content/abap/CH02/CH02-L03.md | 57 +++++++++++-------
 content/abap/CH02/CH02-L04.md | 15 ++++---
 content/abap/CH02/CH02-L05.md | 47 ++++++++-------
 content/abap/CH02/CH02-L06.md | 27 ++++++---
 content/abap/CH02/_chapter.md |  9 ++++-
 reference/glossary.json       |  5 +++
```

- 전 헝크를 육안 대조해 **클러스터 ID 미매핑 헝크 0**을 확인했다. 클러스터에 직접 대응하지 않는 최소 연동은 4건뿐이고 모두 위 표에 사유를 적었다: ① `_chapter.md` 본문 첫 문단(intro와 동일 문장이라 동반 수정) ② `CH02-L05.md`의 `gv_my_var` → `gv_seats`(C027 이름 교체의 짝) ③ `CH02-L06.md` `prereq`에 `CH02-L03` 추가(C007이 L03 문법을 쓰므로) ④ `CH02-L01.md` `introduces`에 "리터럴", `foreshadow`에 N·D·T 항목(C012·C033의 메타 근거).
- 레슨 분할·리넘버·순서 변경 **없음**. 파일 추가·삭제 **없음**.
- `embeds/**` · 타 챕터 · `.project-docs/**` · `tools/**` **무수정**. (작업 중 `.project-docs/15_AUDIT_MATRIX.md`가 modified로 보이나 **이 작업자의 변경이 아니다** — 동시 실행 중인 다른 감사 세션 산출로 보인다.)
- 새 `::embed::` 선언 **추가 없음**(지시대로).
- `npm run build:abap` 통과 — glossary parity 미정의 0건 · pages 270 · 경고 0. 브라우저 검증은 지시대로 미실행(본선 몫).
