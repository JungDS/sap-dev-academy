# 진행 로그 — 2026-06-18 야간 자율 세션

작업 브랜치: **`phase-c/overnight-20260618`** (main 미병합·미푸시).
시각: 03:30 KST 시작 ~ 05:30 KST 종료 예정(06:00 사용량 리셋 전 중단).

## 이번 세션에 한 일

설계는 이전 세션에서 확정(아키텍처·classic-first SQL·B재그룹·제목 규칙). 이번엔 **구현**.

1. **콘텐츠 파이프라인** — `tools/build-curriculum.mjs`(gray-matter+marked).
   `content/abap/**.md` → `docs/abap/curriculum.json`·`lessons/CHxx.json`·`pages/*.html`·`glossary.json`.
2. **런타임 셸** — `assets/shell.js`/`shell.css`: 상단바 · 좌측 3탭 네비(이문서/레슨/챕터) · 이전다음 · 핵심용어 팝업.
3. **CH01~CH10 본문 50개 레슨 완성**(기초 흐름 전체): 환경/첫프로그램 → 변수·타입 → DDIC Domain/DE·PARAMETERS → Structure → Internal Table → Transparent Table → Open SQL(classic) → DDIC 관계·F4 → 모듈화 → SALV. 모두 동기부여형 + 용어마킹 + ABAP 코드 + 흔한실수. (CH11~CH35는 스텁 골격.)
4. **전체 spine 스캐폴딩** — CH02~CH35(34챕터 메타 + 187 레슨 stub). 후반부 레슨 제목은 v5.4에서 이식. 총 **35챕터/192레슨**.
5. **로드맵 페이지** `pages/abap.html`를 `curriculum.json` 기반 **데이터 주도**로 재작성(35챕터 자동 렌더 + CH18 classic→modern 경계 표시).
6. **글로서리** `content/abap/glossary.json` 44용어(구 repo 424용어에서 발췌·각색, CH01~10 용어 전부 커버).
7. 표준 문서 `.project-docs/AUTHORING.md`, `.gitattributes`(CRLF 정리).

## 검증 상태

- `npm run build:abap` 정상(35트랙챕터/192레슨/192페이지/13용어).
- 정적 점검: **href 192개 전부 실파일 존재**, **본문 data-term 전부 glossary 커버**, shell.js `node --check` 통과.
- ⚠️ **브라우저 시각 스모크테스트 미실시**(미리보기 도구의 깊은 URL 네비 제약). 깨어나면 가장 먼저 권장 ↓.

## 확인/실행 방법

```bash
git checkout phase-c/overnight-20260618
npm install                 # gray-matter, marked (이미 lock 있음)
npm run build:abap
python -m http.server 8137
```
- 로드맵:  http://localhost:8137/pages/abap.html
- 레슨:    http://localhost:8137/docs/abap/pages/CH01-L01-logon.html
- (fetch 사용 → 반드시 HTTP. file:// 로 열면 데이터 안 보임)

## 다음에 할 일(우선순위)

1. **시각 스모크테스트** — 네비 3탭·이전다음·용어팝업·로드맵 렌더 눈으로 확인.
2. **CH11~ 본문 채우기** — stub(🚧)을 실제 산문으로. (CH01~CH10이 패턴 견본 10개)
   CH11 SELECT-OPTIONS → CH12 JOIN·집계(★classic 유지) → … → **CH18부터 modern SQL**.
3. CH18 작성 시 **classic→modern 경계 규칙** 준수(.project-docs/AUTHORING.md 참고).
4. index.html(허브)에서 ABAP 카드가 로드맵으로 연결되는지 점검.
5. (선택) `git add --renormalize .`로 CRLF 일괄 정리, main 병합 여부 결정.

## 메모리 핸드오프

`~/.claude/projects/.../memory/`에 4개 저장: sda-architecture, abap-curriculum-design,
sapui5-readonly, user-working-style. 새 세션은 이걸로 재설명 없이 이어감.
