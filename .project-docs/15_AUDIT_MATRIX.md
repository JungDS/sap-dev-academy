# 15. AUDIT MATRIX — 4모델 × 10종 검수 매트릭스 프로토콜

> 📅 최종수정: 2026-08-15 01:32 KST
> 🎯 **목적:** 39챕터 전량을 **동일 설계 에이전트 10종 × 4모델 완전 중복**으로 감사해 ① 챕터별 품질 최종평가와 ② **모델별 성능 데이터(스코어카드)** 를 동시에 얻는다. **발견·평가까지만 — 수정 없음**(보강·재집필 = 챕터별 별도 지시).
> 📖 **읽을 때:** 매트릭스 감사 캠페인 실행·재개 시. §1이 사용자 확정 원장, §2가 발견자 정본 프롬프트(SSOT).

## §0 한눈에

```
[챕터 단위 — 챕터당 29회]
  발견자 7종(AG01～06·09) × 4모델 = 28벌  ──┐   블라인드(서로 못 봄)
                                            ├─→ AG10 수합(Fable 5, 1회)
[커리큘럼 단위 — 캠페인당 1회]              │      병합·중복제거·상충표시
  AG07 게이팅 × 4모델 · AG08 연속성 × 4모델 ┘          │
                                                       ▼
                                     본선(Fable 5) 검증 → 채택/기각/쟁점
                                                       │
                                                       ▼
                                    verdict/CHnn.md — 종합판정 4등급 + 수정 백로그
                                    (수정 실행은 별도 지시가 있을 때만)
```

- **모델 4계열**: `opus` · `sonnet`(Agent 도구) / `gpt-5.6-sol`(codex exec) / `gemini-3.6-flash-high`(agy). 판정층 Fable 5는 발견자 어느 계열에도 불참.
- **산술**: 챕터 스코프 28/챕터 × 39 = 1,092 + AG07·08(8벌) + 수합 39 ≈ **1,140회**. R2 없음(수정이 없으므로 — 재검은 추후 수정 작업의 검증 단계).
- **중복의 목적**: 프롬프트를 고정하고 모델만 바꾸는 **통제 실험** — 발견 차이 = 모델 차이. 프로필 분산안은 기각됨(§1).

## §1 확정 원장 (사용자 결정 — 변경은 사용자만)

| 날짜 | 결정 |
|---|---|
| 2026-08-03 | **중복 투자 감수** — 완료된 W0～W4 감사와 겹쳐도 전량 재감사한다(다중 점검 선호). |
| 2026-08-03 | **동일 프롬프트 × 4모델 완전 중복** — 모델별 차이 측정이 목적. 종내 프로필 분산(모델별 다른 페르소나)은 통제 변인 오염으로 기각. |
| 2026-08-03 | **수합·검증·최종평가 = Fable 5** — AG10 수합(서브에이전트) + 본선 판정. 발견 모델은 판정에 불참. |
| 2026-08-03 | **최종평가까지만 먼저** — 챕터 보강·재집필은 평가 후 별도 지시에만 실행. |
| 2026-08-03 | **모델 확정** — opus · sonnet · gpt-5.6-sol(codex) · **gemini-3.6-flash-high**(agy, pro 아님 — 사용자 지정). |
| 2026-08-03 | **백그라운드 병렬 발사** — 모든 발견자 호출은 계열별 백그라운드 작업으로 동시 발사, 상호 대기 금지. |
| 2026-08-03 | **서버·포트 규칙** — 발견자는 서버·프로세스를 기동/종료하지 않는다(수명주기 = 본선 소유). 로컬 fetch 검증을 부여하는 변형 시 계열별 고정 포트: 본선 확증 8137 · codex 8141 · agy 8142 · claude 8143～8144(공용 포트 공유 금지 — 타 계열 진행 중 서비스 중단 방지). |
| 2026-08-03 | **agy 저장소 직접 접근** — `~/.gemini/antigravity-cli/settings.json` permissions.allow에 `read_file(C:\SAP\sap-dev-academy)` 읽기 전용 1줄(쓰기·명령 없음)로 확정. Temp 스테이징 폐기 → 4계열 동일 조건(저장소 직접 읽기 + 프롬프트 제약). |
| 2026-08-03 | **AG02 도구 자유화** — 검증 도구의 4계열 동일성 요구 철회(소스 기반 통일안 기각). 각 스택은 자기 하네스에서 **허용된 도구 전부**를 동원해 가장 강한 검증을 한다(Claude=브라우저 패널 실조작 · codex=읽기 샌드박스 내 node 등 · agy=파일 읽기+URL 열람). 프롬프트 텍스트는 여전히 4모델 동일(과제 동일성 유지), 실제 사용 수단은 `method` 필드로 자가보고. 스코어카드의 AG02는 모델 비교가 아니라 **스택(모델+도구) 비교**로 해석. 권한 경계 불변 — 거부된 권한(agy 명령 실행 등)을 추가 부여하지 않는다. |
| 2026-08-03 | **agy 명령 실행 허용** — 사용자가 `command(*)`를 agy settings.json에 **직접** 등재(전면 명령 권한 부여는 분류기상 본선 대행 불가 — 사용자 손 원칙). 앞 행의 "agy 명령 미부여" 경계를 대체. 이로써 **agy AG02 상한 = Chrome 헤드리스 CDP 실브라우저 검증**(수동·자율 각 1회 실증: 렌더 `#bugs` 5카드·콘솔 인터셉트·`.mk` 클릭 반응·보고서 파일 쓰기 — 전부 사전 설치 도구만 사용). 방어선 = 지시서 제약(무설치·URL 허용목록·서버 무조작) + cwd 스크래치패드 + 사후 `git status` 검증. |
| 2026-08-03 | **4등급 기준 승인** — §5의 경계 규칙(유지=치명·높음 0 / 경미 수정=높음≤2 국소 / 보강 권장=높음 3+ 또는 구조 결함 / 재집필 권장=치명 또는 근본 결함) 그대로 확정. 경계 사례는 본선 재량 + 사유 명기. |
| 2026-08-03 | **파일럿 합격선 승인** — §8 그대로 확정(스키마 유효 ≥26/28 · 앵커 실존율 기준선 채집 · 수합 스팟 오류 ≤2/10 · 비용 실측→외삽 보고 후 전량 go/no-go는 사용자). **확정 대기 0 — 설계 완결.** |
| 2026-08-03 | **전량 go + 보강 자동 포함** — CH02～39 배치 진행(챕터별 지시 불요). 챕터 사이클 = 발견 28벌 → 수합 → 본선 판정·verdict → **보강(Opus 작업자 2: 본문·위젯, CH01 사이클이 템플릿) → 본선(Fable) 점검**(diff↔클러스터 전수 매핑·무단 수정 0·게이트·브라우저 실측) → 커밋. 사용자 에스컬레이션은 쟁점 발생 시에만. agy 쿼터 소진 시 해당 챕터 gemini 결측 기록 후 계속. |
| 2026-08-03 | **배치 1(CH02～04 + 커리큘럼 축) 완주** — 수거 99/100(챕터 84/84 · 축 15/16, 결측 = AG08-CH01-17/agy: CANCELED 2회 후 재발사에서 `escalate_admin` 헤드리스 자동 거부 — 권한 완화 대행 금지 원칙으로 결측 확정). 판정 3챕터 전부 보강 권장 → 보강·본선 점검 완료. 축 판정 = [verdict/CURRICULUM.md](../.archive/2026-08-03-matrix-audit/verdict/CURRICULUM.md)(수정은 전부 후속 배치 라우팅). **운영 관례 추가**: ① verdict 기계본에 발견 단위 `rejected` 배열 필수(클러스터 객체만으로는 스코어카드 기각 열 join 불가) ② AG10 수합은 인덱스 방식 프롬프트 표준 ③ 브라우저 레인 직렬 + 보강 위젯 작업자도 같은 직렬에 편입 ④ 분업(서브에이전트) 발견자는 최종 저장까지 부모 책임 — 재개 시 본선이 자식 보고 사본 경로를 직접 공급하는 폴백 확립. |
| 2026-08-09 | **배치 3(CH09～12) 완주** — 수거 112/112·결측 0(사용한도 중단 1회 → 파일 실사 재개: 저장 완료 벌 오인 방지·파손 1벌만 재발사, 스트림 스톨 5건 전부 SendMessage 컨텍스트 보존 재개). 판정: CH09 136/12·CH10 160/13·CH11 118/8 **보강 권장** · CH12 100/12 **경미 수정**(캠페인 첫 경미). 축 라우팅 이행: A4 시드 공연 3(C001～C003·ARTIST 정본 안유진 — 하류 CH16/19/20 실측 정합)·D CH09-L02 허위 회상 제거·CH09～11 위젯 404 링크 전량 해소(+CH10 작업자가 저장소 잔여 전수 소탕). **확정 기각 사전 추가 4**: ⑪ 'DE 부착 SH > Check Table' 서열 주장(ABENABAP_DYNPROS_VALUE_HELP_AUTO ascending 목록) ⑫ `TYPE dbtab-field` 참조 오류 주장 ⑬ CP 확장형 시비(ABENLOGEXP_STRINGS: "CP stands for **Conforms to Pattern**" — opus '확실' 마킹까지 틀린 사례) ⑭ `TYPE RANGE OF` 오류 주장. **운영 관례 추가**: ⑩ 본선 판정 문서의 방향 오류·라우팅 누락을 작업자 계약(무단 반영 금지·사실 우선)이 3회 적발(CH09 C069 아이유→안유진·C015 누락·CH12 C041 Cardinality 충돌) — 부기 정정 절차 정착 ⑪ 병렬 작업 트리에서 `git stash` 금지(타 워커 미커밋분 말림) ⑫ 시스템 패턴 발견: CH10+ 전 챕터 전역 위치 선언의 l 접두어(3챕터 연속·AG09 집중 적발) — 후속 배치 상시 점검 항목. |
| 2026-08-10 | **배치 3 R2 블라인드 재감사 + 확정 결함 일괄 보강 완주** — 사용자 지시("정말 잘 보강되었는지")로 CH09～12를 R1과 동일 조건 재감사(격리 캠페인 `.archive/2026-08-03-matrix-audit-r2/`, 발견자는 R1 산출물 접근 금지). 수거 112/112·결측 0·dropped 0, 발견 볼륨 R1 대비 62%(345/228클러스터)·CH12 높음 0 — **보강 효과 실증, 단 완전하지는 않음**(요약 = [verdict/R2-SUMMARY.md](../.archive/2026-08-03-matrix-audit-r2/verdict/R2-SUMMARY.md)). 사용자 결정: §3 전체 11건 일괄 교정(이행 완료 — 본문 A/B·위젯 W1/W2 작업자 4) + **R11 개정: Range Table 접두어 `gr_`/`lr_` 정식 등재**. 핵심 교훈: **R1 본선 지시가 만든 활성화 차단 결함**(CH10-L04 RETURNING 인라인 LENGTH/DECIMALS — ABENTYPING_COMPLETE: 메서드 시그니처 typing에 부가어 없음, 교정=명명 TYPES 경유)을 R2가 적발 — 보강 지시문의 코드 형태도 코퍼스 검증 대상. **확정 기각 사전 추가 2**: ⑮ 'METHOD IMPORTING 제네릭 c/n/p/x 불가' 주장(완전 타입 강제는 RETURNING만 — ABAPMETHODS_FUNCTIONAL) ⑯ 'FORM도 OPTIONAL/DEFAULT 지원' 주장(FORM 파라미터 인터페이스에 없음 — FM 전용, ABAPFORM). 재발 오판 자동 재기각 3계열(⑪·⑫·⑬) + 코퍼스 신규 확정: VALUE CHECK는 DDIC 참조면 합법·Check Table 검사는 'FK 테이블의 컴포넌트'일 때만(ABAPPARAMETERS_VALUE:64～66)·선택화면 F4 종속 불가는 코퍼스 문언 그대로(ABAPPARAMETERS_TYPE:30). **운영 관례 추가**: ⑬ R2 재감사 프로토콜 = 격리 캠페인 폴더+블라인드 발사 문구+판정은 R1 정보 승계(발견 블라인드/판정 승계 이원화) ⑭ 본선 지시 오류를 작업자 계약이 적발하는 선례 계속(CH09 공연명 C003 지시안이 시드 정본과 충돌 — 작업자가 정본 우선 채택) ⑮ 위젯 작업자의 부수 발견은 축 라우팅으로 이월(CH13-L02-S01·CH13-L08-S01 C002 아티스트 '신유빈' ≠ 시드 정본 ABAP Trio — 배치 4 CH13에서 처리). |
| 2026-08-10 | **배치 2 R2 블라인드 재감사 + 확정 결함 일괄 보강 완주** — 브랜치 `audit/r2-ch05-08`, 배치 3 R2와 동일 프로토콜(격리 캠페인·블라인드). 수거 112/112·결측 0·dropped 0, 발견 볼륨 R1 대비 68%(366/239클러스터)·치명 0·**활성화 차단급 0**(배치 3 R2와의 차이), 전 챕터 등급 의견 '보강'→'경미' 하향 — 요약 = [verdict/R2B2-SUMMARY.md](../.archive/2026-08-03-matrix-audit-r2/verdict/R2B2-SUMMARY.md). 사용자 결정: 전체 일괄 교정(애매 항목 사용자 경유 — 코퍼스 확정 3건 전부 명확해 에스컬레이션 0) → 13건 이행(본문 A 5·본문 B 7·위젯 W 3, §6 부기). **확정 기각 사전 추가 1**: ⑰ 'DDIC 이름 2·3위치 밑줄 제한 부정' 주장(ABENDDIC_STRUCTURES_NAMES — Structure·테이블 공통 명문 규칙, 발견자 3벌 전면 오판). 재발 오판 최대 사례 기록: ⑦ SORT-HASHED 재발 **x7**·⑨ IN 리터럴 재발 x4·BINARY SEARCH subrc 8 부정(ABAPREAD_TABLE: 4 또는 8). 코퍼스 신규 확정: CORRESPONDING FIELDS OF wa는 이름 일치 컴포넌트에만 대입 — 잔존이 정설(ABAPINTO_CLAUSE:52, 'wa 전체 초기화' 주장 기각). **운영 관례 추가**: ⑯ 작업자 부수 발견의 동형 결함은 본선 채택 후 SendMessage 연장 지시로 같은 계약 안에서 처리(CH05-L03 ZST_AUDIT·ZST_ORDER — 새 발사 없이 컨텍스트 보존) ⑰ 본선 지시문의 근거 인용 오기도 작업자 계약이 적발(CH09-L09 인용 불일치 — 이행 무영향이어도 부기). |
| 2026-08-10 | **배치 4(CH13～16) 완주** — 브랜치 `audit/matrix-batch4`. 수거 112/112·결측 0(codex 서버 capacity 결측 7건 → 시차 재발사 2회로 전량 회수 — "at capacity"는 쿼터 아닌 일시 현상, 시차 재시도가 정답). 수합 591발견/292클러스터(치명 2·높음 44). 판정: 4챕터 전부 **보강 권장**(채택 285/기각 7클러스터 — CH13 58/3·CH14 74/0·CH15 88/1·CH16 65/3). 치명 2 중 1 기각(CH13 LEFT OUTER JOIN ON `<>` — ⑱)·1 채택(CH15 `pa_secret` 9자 → `pa_adv` 개명). **확정 기각 사전 추가 2**: ⑱ 'outer join ON `=` 한정' 주장(ABENNEWS-740-ABAP_SQL:40 — 7.40 해제) ⑲ 'LOOP AT SCREEN INTO 불가' 주장(ABAPLOOP_AT_SCREEN:16 — INTO가 정식·단축형이 obsolete). 코퍼스 신규 확정: SET TITLEBAR WITH text1～9(9개 허용) · AUTHORITY-CHECK 12=사용자 마스터에 권한 객체 없음 · AUTHORITY-CHECK 미나열 필드=DUMMY와 동일 미검사 · 오류 복귀 시 AT SELECTION-SCREEN OUTPUT 미재실행. 이행 = 본문 4작업자 240건+위젯 W1/W2 34건+본선 직접(glossary 4키·ZPV_BOOKING 개명·시드 동기화). **축 라우팅 완결**: A3 잔여석 grain(L08 회차 grain 복원) · CH13 위젯 아티스트(신유빈→ABAP Trio·전지현→UI5 Night) · CH14 ZCONCERT_T 전제 = stale 확인(안유진 오독만 정정) · CH15 SSCRFIELDS(C010 풀이) · CH16 상태 R 발명(C006·C012 — N/C 정본화). **운영 관례 추가**: ⑱ 판정서 지시도 작업자 코퍼스 검증이 뒤집는 선례 2건(C034·C047 — 본선 지시 무근거 적발) — 채택 클러스터의 교정 '방향'까지 코퍼스 재검이 표준 ⑲ verdict 조립(기계본)은 판정 결정(본선 노트) 확정 후 fable 서브에이전트 위임 가능 — 결정과 조립의 분리로 컨텍스트 절약 ⑳ 본선 일괄 rm 명령이 타 배치 확정 결측 마커를 오삭제(git 즉시 복원) — 결측 마커 정리는 개별 경로로. |
| 2026-08-14 | **배치 4 R2 블라인드 재감사 완주(수합·판정 요약까지 — 수정은 사용자 결정 대기)** — 브랜치 `audit/r2-ch13-16`, 배치 2·3 R2와 동일 프로토콜(격리 캠페인·블라인드). 수거 112/112·결측 0·dropped 0(사용한도 중단 1회 = CH16 수합 — SendMessage 컨텍스트 보존 재개로 유실 0). 수합 442발견/284클러스터, R1 대비 75%·실질 치명 0(명목 3은 전부 ⑲ 계열 재발)·높음 32(R1 44)·등급 의견 전 챕터 '경미' 중심 하향 — **보강 효과 실증, 단 보강 유발·불완전 잔여 8건급**(요약 = [verdict/R2B4-SUMMARY.md](../.archive/2026-08-03-matrix-audit-r2/verdict/R2B4-SUMMARY.md)). 대표: CH13-L08 위젯↔본문 데이터 불일치(AG02 4모델 전원) · CH13-L03 '괄호 공백=습관' 사실 오류(보강 신설 콜아웃 — classic 문법 요구) · CH14-L04 Maintenance View 필드 목록 키 CONCERT_ID 부재(본선 실측) · CH16-L10 캡스톤 AT EXIT-COMMAND 미반영(x8 최다 합류). 재발 오판: ⑱ x4·⑲ 계열 x3(전부 자동 재기각). conflict 확정 대기 2(CH14 MANDT 자동 포함·Maintenance View 1:N 조인) = 수정 착수 시 코퍼스 확정. **이행 부기(2026-08-15)**: 사용자 확정 "§3 전체 일괄 교정" → 작업자 4(본문 A/B/C·위젯 W) 완주([R2B4-SUMMARY §6](../.archive/2026-08-03-matrix-audit-r2/verdict/R2B4-SUMMARY.md)). conflict 3건 전부 코퍼스 확정(에스컬레이션 0): C001 채택(CLNT 첫 컬럼 — abenddic_database_views)·C009 기각(FK 필드가 키면 secondary 합법)·C015 채택(**`COUNT(*)`만 예약어로 붙여쓰기 허용, 그 외 집계 괄호 공백 = 문법 요구** — abensql_agg_func Variant 13·14). **연장 채택(관례 ⑯)**: Maintenance View = **inner join** 명문(abenddic_maintenance_views "implements an inner join" — L04·L08의 'outer 보존' 서술은 사실 오류, outer는 Help View 성질) + L01 지도 2:2 정합. 관례 ⑰ 재발 1(본선 발사 문구 시드 인용 오기 0002=4석 — 작업자가 저장소 정본 1석 채택). se16n-tracker 엔진 cfg 주도화(단일 사용·회귀 0). 잔여 백로그 2 = §6 말미. |
| 2026-08-09 | **배치 2(CH05～08) 완주** — 수거 112/112·결측 0(외부 레인 codex/agy 56벌 전량 성공). 판정 4챕터 전부 **보강 권장**(채택/기각 발견: CH05 104/14 · CH06 121/14 · CH07 98/6 · CH08 174/6) → 보강(본문 4+위젯 4 작업자)·본선 점검(빌드·게이팅 STRUCT/PREREQ/R6=0·기각 바이트 보존·통합 실측) 완료. 축 라우팅 4건 이행: AX-1 LIKE 원장 중복(CH05-L01 introduces 정리) · AX-2 ts_person 드리프트 · AX-3 CH06-L02→CH07 약속 회수(`TYPE zgugudan` 데모) · AX-4 CH08-L02 게이팅([선행 사용]·introduces 정리). 신설 위젯 3(CH05-L04-S02·CH05-L05-S02·CH06-L04-S04)+체험 엔진 1(type-borrow-lab). **확정 기각 사전 추가 4**: ⑦ 'HASHED는 SORT 불가' 주장(ABAPSORT_ITAB: standard·hashed가 대상, 해시 정렬 예제 실재) ⑧ READ TABLE의 result(TRANSPORTING NO FIELDS) 선행 배치 오류 주장(ABAPREAD_TABLE 구문도: result가 키 지정보다 앞) ⑨ `IN (v1, v2)` 콤마 목록 classic 오류/규약 위반 주장(ABENWHERE_LOGEXP_IN 1변형·R6 콤마 금지는 SELECT 필드 나열 한정) ⑩ SELECT (SINGLE) 실패 시 Work Area 자동 초기화 주장(ABAPINTO_CLAUSE: remains unchanged). **운영 관례 추가**: ⑤ 발견자 발사 문구에서 "하네스 승인 쓰기" 권한 주장 제거 — 캠페인 지정 경로 사실 서술 + "판단 시 JSON 반환 허용"으로 정직화(AG05/sonnet가 §4와의 불일치를 정확히 지적·본선이 검증 후 대리 저장한 선례) ⑥ 발사 문구에 블라인드 규율(형제 산출물 미열람) 명시(자진 신고 1건 재발 방지) ⑦ 서버 기동은 PowerShell 도구로 — Git Bash `--directory C:\...` 백슬래시 소실이 배치 1 AG02 404 폴백의 원인이었음을 확정 ⑧ 위젯 실측 시 `?v=` 캐시버스팅은 `_engine/*.js`에 미적용 — 엔진 수정 후엔 `fetch(url,{cache:'reload'})`+performance 크기 대조로 재검증 ⑨ 본선 판정 누락은 작업자의 "무단 반영 금지" 계약이 잡는다(CH06 C055 — 작업자 보류 보고→본선 채택 확정·직접 반영). |

## §2 발견자 10종 정본

### §2.0 공통 규격

**12질문 → 에이전트 매핑(traceability)**

| 사용자 질문 | 담당 |
|---|---|
| 1 이해가 잘 되나 · 9 비전공자에게 생략·어려움 | AG01 |
| 3 체험이 적절한가 | AG02 |
| 10 전공자에게 과잉 디테일 | AG03 |
| 2 문장 자연스러움 · 4 용어 설명 충분 | AG04 |
| 5 복습·예습 · 6 챕터 구성 · 7 섹션 구성 · 12 교재 디자인 전문가 | AG05 |
| 11 20년 전문가 — 설명 부족·예시 적절성 | AG06 |
| 8 학습 순서 | AG07 |
| (신설 — W3 최대 결함원) 관통예제 연속성 | AG08 |
| (신설 — CH39 문법 적발 이력) 코드 정밀 | AG09 |
| (인프라) 수합 | AG10 |

**공통 프리앰블** — 모든 발견자 프롬프트 맨 앞에 그대로 붙는다. `{{ }}`는 하네스 치환 변수.

```text
[감사 신원]
- 너는 SAP 학습 사이트(비전공 20대 입문자 대상 ABAP 커리큘럼) 콘텐츠 감사 매트릭스의
  발견자 {{AGENT_ID}}이며, 모델 {{MODEL_ID}}로 실행 중이다.
- 감사 대상: {{TARGET}}
- 대상 파일(전체 경로):
{{TARGET_FILES}}

[제약 — 반드시 준수]
- 읽기 전용 감사다. 파일 수정·생성·삭제 금지. 셸/도구 사용은 위 대상 파일을 읽는 목적에 한정한다.
- 접근 허용 = 저장소 C:\SAP\sap-dev-academy 안, 위 대상 파일(및 이 프롬프트가 명시한 입력)만.
  저장소 밖 — 특히 C:\Users\gosts\.claude\ 이하(세션·메모리·설정) — 절대 접근 금지.
- 웹 검색·외부 네트워크 금지. 네 지식만으로 판단하고, 불확실하면 confidence="추정"으로 정직하게.
- 블라인드 감사다: 다른 감사자의 보고, 과거 감사 산출물(.archive/·.project-docs/ 등)을 찾아 읽지 마라.
- 억지 발견 금지: 없으면 findings=[]가 정답이다. 평가는 발견 수가 아니라 정확도로 한다 — 오탐은 감점이다.

[출력 — 스키마 준수]
- 아래 JSON 스키마를 정확히 따르는 JSON 객체 **하나만** 출력한다. 마크다운 펜스·설명문·인사말 금지.
- finding.quote는 대상 파일에 **실제로 존재하는 원문을 그대로 복사**(240자 이내)한다 — 앵커로 기계 검증된다.
- 작업 일부가 실패해도 **스키마 구조를 유지**한다 — 실패 사유는 errors 배열에 기록하고, 스키마를 이탈한 응답을 만들지 마라(검증 못 한 것을 발견으로 만들지도 마라).
- agent="{{AGENT_ID}}", model="{{MODEL_ID}}", target="{{TARGET}}" 값을 그대로 넣는다. 모든 서술은 한국어.

{{SCHEMA_JSON}}
```

- **대상 파일 규격(챕터 스코프 공통)**: `content/abap/CHnn/_chapter.md` + 레슨 `.md` 전체. AG02는 임베드 HTML(`embeds/abap/CHnn-*.html` + `embeds/_engine/` 해당 엔진)을, AG07·08은 종별 명시 입력을 추가로 받는다.
- **모델 4벌 공통**: 프롬프트 텍스트는 4모델 완전 동일(§1). codex/agy는 CLI 스키마 강제를 **추가로** 걸지만 프롬프트는 불변 — 통제 유지.
- **페르소나 복창**: 출력 `persona_ack`에 자기 역할을 1문장으로 복창시켜 프롬프트 주입 성공을 확인한다.

---

### AG01 입문자 이해도 — "은서" (질문 1·9)

스코프 챕터 · 4모델 · 입력 = 챕터 MD 전체.

```text
[역할] 너는 '은서'다. 개발·전공 경험이 전혀 없는 24세 문과 출신 입문자로, 이 사이트로 ABAP을
처음 공부한다. 노션·인스타에 익숙하고, 모르는 단어가 나오면 그 자리에서 멈추는 성격이다.

[과제] 대상 챕터의 레슨을 첫 레슨부터 순서대로, 처음 읽는 사람으로서 통독하라.
읽다가 걸리는 지점을 하나씩 기록한다:
1) 멈춘 곳 — 여러 번 읽어도 이해가 안 되는 문장/문단. 왜 막혔는지를 evidence에.
2) 생략 — 아는 척하고 건너뛰어서 비전공자가 따라갈 수 없는 곳(중간 단계·전제가 빠진 설명).
3) 단절 — 이 챕터 안에서 앞 내용만으로는 이해할 수 없는 곳(갑자기 나온 개념·코드).
4) 과부하 — 한 번에 새 개념이 몰려서 소화 불가능한 곳.

[주의]
- 네 실제 지식으로 빈틈을 메꾸지 마라. 은서가 모르는 것은 모르는 것이다.
  용어의 풀이가 그 자리(또는 같은 챕터의 앞 레슨)에 없으면 막힌 것으로 기록한다.
- 특히 잘 읽힌 장치(비유·그림·체험)가 있으면 overall.summary에 1문장으로 남겨라.

[severity 기준] 치명=이 지점에서 학습을 포기할 수준 / 높음=여러 번 재독해도 불확실 /
중간=재독하면 이해됨 / 낮음=거슬리는 정도.
```

### AG02 실습·체험 정합 — "준호" (질문 3)

스코프 챕터 · 4모델 · 입력 = 챕터 MD + 임베드 HTML + 해당 엔진 JS.

```text
[역할] 너는 '준호'다. 배운 건 그 자리에서 직접 해봐야 직성이 풀리는 실습형 학습자다.
이 사이트의 원칙: 본문에 코드가 1줄이라도 나오면 그 페이지에 체험(임베드 위젯)이 있어야 한다.

[과제] 각 레슨에서 본문 코드·설명과 체험 위젯(소스 첨부됨)을 대조 감사하라:
1) 체험 공백 — 본문에 코드가 있는데 그 페이지에 체험이 없는 곳.
2) 불일치 — 위젯의 변수명·데이터·값·시나리오가 본문 코드/설명과 어긋나는 곳.
3) 헛체험 — 위젯이 레슨 목표와 동떨어져 '해보나 마나'인 곳(클릭은 되지만 배우는 게 없음).
4) 위젯 결함 — 설정(JSON cfg)·안내문·정답 판정의 오류(소스 정독으로 판단 가능한 것).

[검증 수단] 네 실행 환경에서 사용이 허용된 도구는 무엇이든 전부 동원해, 가능한 가장 강한
수준의 검증을 하라(가능한 순서대로: 브라우저 실조작·렌더 DOM 측정 → 스크립트 실행 →
URL 열람 → 소스 정독). 검증용 로컬 서버 주소가 주어지면 아래에 있다(서버·프로세스의
기동/종료는 금지 — 인프라는 하네스 소유다):
{{SERVE_URL}}
실제 사용한 검증 수단을 출력의 method 필드에 정직하게 기록하라. 검증하지 못한 사안을
실행한 것처럼 서술하지 말고 confidence="추정"으로 남겨라.
```

> **비고(결정 이력)**: ① 당초 B안 = Opus/Sonnet만 브라우저 실조작 → ② 통제 순도·패널 경합을 이유로 "4모델 소스 기반 통일"을 제안 → ③ 리허설 실증(2026-08-03, CH37-L04-S01)에서 스택별 실제 능력 차가 확인 — codex는 자작 인메모리 DOM 하네스로 엔진 실행·클릭 검증까지 수행(+실결함 1건 적발 — 기제·재현은 codex 자가검증으로 확인: `node -e`+`vm.runInNewContext`+최소 DOM 심, 파일 쓰기 불요), agy는 원본 HTML 수신만(`#bugs` 빈 div 판별 · 상호작용 도구 부재 · **agy 자가검증으로 판정 전량 확인**, 공식 명세 "No JavaScript execution") → ④ **사용자 결정(2026-08-03): 도구 자유화 채택**(§1) — 각 스택이 가진 수단 전부로 검증하고 method를 자가보고, AG02 결과는 스택 비교로 해석. "추정" 발견의 최종 확증은 여전히 본선 브라우저가 맡는다.

### AG03 경력자 독자 (질문 10)

스코프 챕터 · 4모델 · 입력 = 챕터 MD 전체.

```text
[역할] 너는 타 언어(Java/C# 계열) 백엔드 3～5년차 개발자다. ABAP을 빠르게 훑어 익히려고
이 사이트를 읽는다. 프로그래밍 기본기(변수·조건문·루프·SQL·OOP)는 이미 안다.

[과제] 경력자의 눈으로 통독하며 기록하라:
1) 과잉 — 이미 아는 개념을 지나치게 풀어 답답한 곳. 어디까지 압축 가능한지 suggestion에.
2) 오개념 유발 — 입문자용 단순화가 개발 상식과 어긋나서 경력자를 오히려 헷갈리게 하는 곳.
3) 지름길 부재 — "이 절은 경험자는 건너뛰어도 됨" 같은 안내가 있으면 좋았을 곳.
4) 용어 충돌 — 일반 개발 용어와 ABAP 용법이 다른데 그 차이를 안 짚어준 곳.

[주의 — 채택 기준] 이 사이트의 1독자는 비전공 입문자다. "입문자에게도 불필요한 장황함"만
높음 이상으로, "입문자에겐 필요하나 경력자에겐 아는 내용"은 낮음 또는 suggestion으로 분류하라.
```

### AG04 문장·용어 에디터 (질문 2·4)

스코프 챕터 · 4모델 · 입력 = 챕터 MD 전체.

```text
[역할] 너는 20년차 한국어 교열·기술 용어 감수자다. IT 입문서를 다수 다듬었다.

[과제] 문장과 용어를 감사하라:
1) 문장 — 번역투·비문·중의문·과도하게 긴 호흡·같은 표현의 어색한 반복. 대안을 suggestion에.
2) 용어 첫 등장 — 전문 용어가 처음 나올 때 한 줄 풀이가 있는가. 풀이가 있어도
   입문자에게 실제로 이해되는 풀이인가(풀이 속에 더 어려운 말이 있으면 결함).
3) 용어 일관성 — 같은 개념이 레슨마다 다른 이름으로 불리는 곳. ABAP 고유 용어를
   영어 원문 대신 어색한 직역으로 쓴 곳(예: Internal Table을 "내부 표"로만 지칭).
4) 톤 — 친근한 입문서 톤에서 갑자기 논문·매뉴얼 톤으로 이탈하는 구간.

[주의] 스타일 취향 강요 금지 — 입문자 가독성을 실제로 해치는 것만. 이모지 사용 자체는 결함이 아니다.
```

### AG05 교수설계 감수 (질문 5·6·7·12)

스코프 챕터 · 4모델 · 입력 = 챕터 MD 전체(_chapter.md 포함).

```text
[역할] 너는 20년차 학습 교재·코스 설계 전문가다. 기업 IT 교육 커리큘럼을 다수 설계했고,
인지부하 관리와 동기 설계(불편을 먼저 겪고 해결책으로 개념을 배우는 구성)에 정통하다.

[과제] 챕터를 교수설계 관점에서 감사하라:
1) 챕터 구성 — 레슨 분할·순서가 학습 단위로 적절한가(과대 레슨/파편 레슨/순서 역전/
   빠진 징검다리). 챕터 intro가 이 챕터를 배우는 이유를 실제로 팔고 있는가.
2) 섹션 흐름 — 각 레슨이 "왜 필요한가→무엇인가→어떻게 쓰나→실수·주의→정리" 흐름을
   이루는가. 빠진 단계, 순서가 뒤집힌 곳.
3) 복습 장치 — 이전 개념을 다시 쓰는 지점에서 상기(리마인드)가 있는가. 예습 장치 —
   레슨 끝의 다음 예고가 1～2문장 수준으로 적절한가(과도한 선행 설명은 반대로 결함).
4) 불편→해결 서사 — 도입부 불편이 진짜 불편인가(억지 불편·형식적 칸 채우기 감지).
5) 인지 부하 — 한 레슨의 신규 개념 수가 과다한 곳, 예제 하나에 변수가 너무 많은 곳.

[주의] 형식 강제 금지 — 흐름 5단계가 자연스럽게 변형된 것은 결함이 아니다.
학습 효과를 실제로 해치는 구조 문제만 높음 이상으로.
```

### AG06 SAP 기술 감수 (질문 11)

스코프 챕터 · 4모델 · 입력 = 챕터 MD 전체.

```text
[역할] 너는 20년차 SAP ABAP 아키텍트다. ECC부터 S/4HANA까지 개발·운영·성능·이관을
두루 겪었고, 후배가 쓴 교재의 기술 검수를 자주 맡는다.

[과제] 기술 사실을 감사하라:
1) 사실 오류 — 문법·시스템 동작·T-code·테이블·프로세스 서술이 실제와 다른 곳.
2) 예시 적절성 — 예제가 실무에서 그렇게 안 하는 패턴인데 '표준'처럼 서술된 곳.
   더 나은 표준 예시를 suggestion에.
3) 위험한 생략 — 전문가 눈에 사고로 이어질 수 있는 누락(COMMIT 규율, 잠금, 권한 검사,
   성능 함정 등)을 이 수준 학습자에게도 짚었어야 할 곳.
4) 안티패턴 — 나쁜 습관을 교정 없이 시연하는 곳.

[표기 — 필수] confidence를 정직하게: "확실" = 버전 무관하게 단언 가능 /
"추정" = 릴리스·설정 의존이거나 기억이 불확실. 웹 검색이 금지돼 있으므로
추정을 확실로 포장하지 마라 — 확실 판정의 오류는 가장 크게 감점된다.
```

### AG07 게이팅·학습 순서 판정 (질문 8) — 커리큘럼 스코프

캠페인당 1회 × 4모델 · 입력 = `.archive/_generated/GATING_AUDIT.md`(도구 출력 — 블라인드 예외로 명시 허용) + 후보가 가리키는 레슨 원문.

```text
[역할] 너는 학습 순서 감사관이다. 이 커리큘럼의 규칙: 레슨은 그 시점까지 배운 것만으로
이해·실습 가능해야 하며(게이팅), 미래 개념 노출은 4단계로 관리된다 —
L0 노출금지 / L1 예고(1～2문장·코드 없음) / L2 선행사용([선행 사용] 표기) / L3 정식 도입.
추가 경계: modern 문법(인라인 DATA()·VALUE·NEW·+=·문자열 템플릿)은 CH18부터,
New Open SQL(@·콤마)은 CH19부터만 허용(코드 블록 기준). &&와 classic 산술 명령은 CH04부터 허용.

[입력] 정적 도구가 뽑은 선노출 후보 목록(파일 첨부)과 대상 레슨 원문.

[과제] 후보를 하나씩 실물 대조해 분류하라(category에 기입):
"위반" = 정의·구조·코드 수준의 선노출 / "L1적법" = 예고 수위로 적법 /
"오탐" = 동음이의·일반어 오인 / "원장결함" = 본문이 아니라 introduces/prereq 선언이 틀림.
위반만 severity 높음 이상, 나머지는 낮음(기록용). 도구가 못 본 순서 문제를 통독 중
발견하면 category="신규"로 추가하되 근거를 명확히.
```

### AG08 관통예제 연속성 (신설) — 커리큘럼 스코프

캠페인당 구간 3분할(CH01～17 / CH18～24 / CH25～39) × 4모델 · 입력 = 구간 챕터 MD 전체.

```text
[역할] 너는 연속성 감사관이다. 이 커리큘럼은 '콘서트 예매 앱'을 CH09부터 CH39까지
이어 만들며, 보조 스레드로 구구단(CH04→08, CH25에서 회수)과 SFLIGHT 읽기(CH08→13→19)를
쓴다. 독립 모듈(관통 밖) = CH32·34·35·38. 아래가 데이터 정본이다:

- ZCONCERT(공연 마스터): concert_id(C4, 'C001'계) · artist · venue · capacity.
  공연명은 텍스트 테이블 ZCONCERT_T. 정원: C001=100(아티스트 안유진)/C002=50/C003=80.
- ZPERF(회차): 키 concert_id+perf_no(N3, '001'계) — 공연의 자식. perf_date 보유.
- ZBOOKING(예매): booking_id(C4, '0001'계 zero-pad) · concert_id · perf_no · customer ·
  seats · status(C1: N=신규, C=취소) + 감사 필드. 잔여석 = 공연 정원 − 그 회차 소진
  (status<>'C'인 seats 합계).
- 인물: 1번 예매자는 항상 정훈영. 등장인물은 지정 이름 풀 안에서만.
- 구구단 스레드 정본: 2～9단 = 72줄.

[과제] 구간({{TARGET}}) 챕터를 순서대로 통독하며 기록하라:
1) 정본 위반 — 위 구조·키 체계·수치·인물과 어긋나는 본문/코드/위젯 언급.
2) 이어받기 단절 — 앞 챕터 산출물이 뒤 챕터에서 다른 구조·이름으로 재등장하는 곳.
3) 약속 파기 — 앞에서 "나중에 만든다/쓴다"고 한 것이 실제로 회수되지 않는 곳(구간 안 기준.
   구간 밖이 회수처로 보이면 confidence="추정"으로 그 사실을 명시).
4) 시점 모순 — 그 시점에 아직 없는 테이블·필드·기능을 이미 있는 것처럼 쓰는 서술.

[주의] 독립 모듈(CH32·34·35·38)이 콘서트를 안 쓰는 것은 정상이다 — 결함 아님.
```

### AG09 코드 정밀 검사 (신설)

스코프 챕터 · 4모델 · 입력 = 챕터 MD 전체(코드 블록만 검사).

```text
[역할] 너는 ABAP 정적 검사기를 시늉하는 코드 전용 감사자다. 산문은 무시하고
```abap 코드 블록만 줄 단위로 본다.

[과제] 각 코드 블록에서:
1) 활성화 차단급 — 미선언 변수 사용, 문장 마침표 누락, 타입 불일치, 존재하지 않는
   구문/애드온, 시그니처 오류. (환경이 없으니 '컴파일 확신'이 아니라 정적 판단 —
   confidence 정직하게.)
2) 네이밍 규약 — 이 커리큘럼의 규칙: CH01～09는 선언이 사실상 전역이라 gv_/gs_/gt_,
   로컬 스코프(CH10+의 FORM/METHOD 안)만 lv_/ls_/lt_. it_는 Header Line 테이블 전용.
   PARAMETERS=pa_(8자 이내), SELECT-OPTIONS=so_(so_+5자). TYPES: ty_(스칼라)/ts_(구조)/
   tt_(테이블). 예외: ok_code/save_ok는 관례명 유지.
3) 경계 규약 — CH01～17 코드에 modern 문법(인라인 DATA()·VALUE·NEW·+=·|…|) 금지,
   CH01～18 코드에 New Open SQL(@·콤마) 금지. &&·classic 산술(ADD 등)은 CH04부터 허용.
4) 주석·출력 문자열의 명백한 오류(변수명 오타로 본문과 어긋남 등).

[주의] 스타일 선호(빈 줄·정렬)는 결함 아님. 의사코드로 명시된 블록([선행 사용] 등
표기가 있는 축약 예시)은 문법 잣대를 완화하고 category="의사코드"로만 기록.
```

### AG10 수합 — Fable 5 전용 (모델 중복 없음)

챕터당 1회 · Agent 도구(`model: fable`) · 입력 = 해당 챕터 raw JSON 전벌(결측 가능) · 출력 = merged JSON(§3-2).

```text
[역할] 너는 감사 매트릭스의 수합 편집자다. 입력은 한 챕터에 대한 발견자 보고 JSON
최대 28벌(에이전트 7종 × 모델 4종, 결측 있을 수 있음)이다.

[과제]
1) 같은 지점·같은 취지의 발견을 클러스터로 병합 — 대표 claim은 가장 명확한 서술을 고르고,
   supporters에 (agent, model, seq)를 전부 보존한다.
2) 같은 지점에 상반된 판정이 있으면 병합하지 말고 conflict=true로 두 주장을 나란히.
3) 클러스터 severity = 구성원 최고값, confidence = 구성원 최저값.
4) 스키마 오염·대상 밖·중복 제출 등 명백한 불량 발견은 dropped에 사유와 함께.
5) 판정하지 마라 — 실물 재검증·채택/기각은 본선 몫이다. 요약으로 정보를 잃지 마라.

[제약] 읽기 전용. 입력 JSON 외의 파일 접근 금지. 출력은 지정 스키마 JSON 하나만.
```

## §3 출력 스키마 (정본)

### §3-1 발견자 보고 스키마 — `tools/audit-matrix/schema.json` (확정 후 파일화)

codex `--output-schema`·agy `--json-schema`에 그대로 전달. Claude 발견자는 프롬프트 내 동일 스키마로 준수(본선이 검증).

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "additionalProperties": false,
  "required": ["agent", "model", "target", "persona_ack", "method", "errors", "files_read", "findings", "overall"],
  "properties": {
    "agent":       { "type": "string", "enum": ["AG01","AG02","AG03","AG04","AG05","AG06","AG07","AG08","AG09"] },
    "model":       { "type": "string", "enum": ["opus","sonnet","gpt-5.6-sol","gemini-3.6-flash-high"] },
    "target":      { "type": "string" },
    "persona_ack": { "type": "string" },
    "method":      { "type": "string", "description": "실제 사용한 검증 수단 — 도구를 쓰는 종(특히 AG02)은 반드시 기입" },
    "errors":      { "type": "array", "items": { "type": "string" }, "description": "부분 실패 사유 — 작업이 일부 실패해도 스키마를 유지하고 여기에 기록" },
    "files_read":  { "type": "array", "items": { "type": "string" } },
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["seq", "file", "quote", "category", "severity", "confidence", "claim", "evidence", "suggestion"],
        "properties": {
          "seq":        { "type": "integer" },
          "file":       { "type": "string" },
          "quote":      { "type": "string", "maxLength": 240 },
          "category":   { "type": "string" },
          "severity":   { "type": "string", "enum": ["치명", "높음", "중간", "낮음"] },
          "confidence": { "type": "string", "enum": ["확실", "추정"] },
          "claim":      { "type": "string" },
          "evidence":   { "type": "string" },
          "suggestion": { "type": "string" }
        }
      }
    },
    "overall": {
      "type": "object",
      "additionalProperties": false,
      "required": ["grade_opinion", "summary"],
      "properties": {
        "grade_opinion": { "type": "string", "enum": ["유지", "경미 수정", "보강 권장", "재집필 권장"] },
        "summary":       { "type": "string" }
      }
    }
  }
}
```

- `quote` = 위치 앵커(줄번호 대신 원문 인용 — 모델이 안정적으로 낼 수 있고, **실존 여부를 기계 검증**할 수 있다 → §6 앵커 실존율).
- **전 키 required** — OpenAI strict 구조화 출력이 properties 전 키의 required 등재를 요구(2026-08-03 E2E에서 400 `invalid_json_schema` 실측 후 확정). 선택 의미는 빈 값(`""`·`[]`)으로 표현: `method`(도구 미사용 종은 빈 문자열)·`errors`(정상 시 빈 배열)·`suggestion`(제안 없으면 빈 문자열).

### §3-2 수합본 스키마(요지) — AG10 출력

```json
{
  "chapter": "CHnn", "inputs_present": 28, "inputs_missing": ["AG03-sonnet"],
  "clusters": [{
    "cluster_id": "CHnn-C001", "file": "...", "quote": "...", "category": "...",
    "severity": "높음", "confidence": "추정",
    "claim": "대표 서술", "conflict": false, "conflict_note": "",
    "supporters": [{ "agent": "AG01", "model": "opus", "seq": 3 }]
  }],
  "dropped": [{ "agent": "...", "model": "...", "seq": 1, "reason": "..." }],
  "grade_opinions": { "opus": ["유지", "..."], "sonnet": ["..."], "gpt-5.6-sol": ["..."], "gemini-3.6-flash-high": ["..."] }
}
```

## §4 실행 하네스 (모델별 호출 정본)

프롬프트 조립 = 프리앰블(§2.0, 변수 치환) + AGxx 본문 + 스키마 — 조립본을 `_prompts/CHnn/AGxx.md`로 저장(4모델 공용 1벌, 재현성).

| 계열 | 호출 | 동시성 |
|---|---|---|
| opus / sonnet | Agent 도구(`claude` 타입, `model` 지정, 백그라운드) — 반환 JSON을 본선이 검증 후 저장 | 2～4 병렬 |
| gpt-5.6-sol | `cat <prompt> \| codex exec -m gpt-5.6-sol --sandbox read-only --ephemeral -C /c/SAP/sap-dev-academy -c 'web_search="disabled"' -c 'approval_policy="never"' --strict-config --color never --json --output-schema tools/audit-matrix/schema.json -o <고유출력명.json> - > <trace.jsonl>` (Bash 경유 — UTF-8 보장 · trace = method 교차검증, codex 자가권고 2026-08-03) | 2 병렬 |
| gemini-3.6-flash-high | `agy -p "Read the UTF-8 file at <prompt.md> and follow ALL instructions exactly." --model gemini-3.6-flash-high --sandbox --output-format json --json-schema tools\audit-matrix\schema.json --print-timeout 15m` (**ASCII 포인터 필수** — 한글 인라인 `-p` 금지, 지시서는 UTF-8 파일로 · cwd=**스크래치패드 고정**, stdout→저장) | 1～2 순차 |

- **발사 방식**: 전 계열 **백그라운드 작업으로 병렬 발사**(§1) — 어떤 계열도 다른 계열을 기다리지 않는다. 수거는 완료 알림 기준.
- **서버·포트**: 발견자 프롬프트에 서버·프로세스 기동/종료 금지를 명시(인프라 수명주기 = 본선 소유). fetch 검증 변형 시 포트 배정은 §1 규칙.
- **인코딩(구현 확정)**: codex 프롬프트 전달은 `run-one.ps1`이 `$OutputEncoding = UTF-8(no BOM)` 명시 후 `Get-Content -Raw -Encoding UTF8` 파이프로 수행(codex 자가권고 채택 — Bash `cat` 경유는 동등 대안). 레인 프롬프트 파일도 BOM 없는 UTF-8로 기록.
- **하네스 구현체**(`tools/audit-matrix/` — 2026-08-03): `preamble.md`+`AG01～10.md`(프롬프트 정본은 이 문서 §2와 동일 — 어긋나면 이 문서가 정본) · `schema.json`/`schema-merged.json` · `assemble.mjs`(조립 — {{MODEL_ID}}/{{SERVE_URL}}은 발사 시 치환) · `run-one.ps1`(외부 레인 실행기 — 래퍼 절차 내장, AG02 레인별 프로필 분기) · `validate.mjs`(스키마+**앵커 실존** 검사) · `scorecard.mjs`(§6 재생성). AG02 조립본에는 4레인 동일 텍스트의 [환경 정보] 블록(서버 URL·playwright 절대 경로·CDP 세션 패턴·temp 규칙·무설치)이 포함된다 — 스택별 힌트를 공통 블록으로 제공해 프롬프트 동일성 유지.
- **실행 능력 현황(스코어카드 각주)**: Claude=브라우저 패널 실조작 · codex=**Playwright Chromium 실브라우저**(AG02 전용 프로필, 그 외 read-only+node) · agy=**Chrome 헤드리스 CDP 실브라우저**(§1 command(*) 등재 후) — **AG02는 전 스택 실브라우저 검증 체계**. 해석은 §1 도구 자유화에 따라 스택 비교.
- **CLI 출력 래퍼 절차(codex 자가권고 채택)**: 실행별 **고유 출력 파일명** → exit code 확인 → JSON 파싱+스키마 재검증 → 최종 이름으로 이동. 실패 시 이전 성공 파일 오인 차단. 결과에는 원문 DOM·전체 로그 대신 **요약+증거 위치만**(토큰 한도로 구조화 출력 자체가 실패할 수 있음). `--ephemeral`은 resume 불가 — 재시도 = 전체 재실행(기존 1회 재시도 규칙과 부합).
- **AG02 도구 자유화 운영**: 본선이 배치 시작 시 스택별 포트(§1 규칙)로 로컬 서버를 기동해 `{{SERVE_URL}}`로 치환 제공(종료도 본선만). Claude 계열 AG02는 브라우저 패널이 세션당 1개이므로 **opus → sonnet 직렬 실행**(그동안 본선은 패널 미사용, P3/P4 회피는 DOM 측정 우선 [07](07_BROWSER_TESTING.md)). codex의 web.run은 localhost 미도달(실증 — 별도 웹 조회 서비스)이나, **AG02 한정 Playwright 실브라우저 프로필로 승격**(스모크 성공 2026-08-03: Chromium 캐시·렌더 5카드·콘솔 0·클릭 반응·temp 쓰기 격리): `--sandbox workspace-write -C <스크래치패드 작업폴더>`(쓰기 = 그 폴더+temp 한정, 저장소는 읽기 전용 유지) + `-c 'sandbox_workspace_write.network_access=true'` + 전역 Playwright **절대 경로 require**(`C:\Users\gosts\AppData\Roaming\npm\node_modules\playwright` — 저장소 로컬 require는 실패, codex 실측) + 포트 8141 서버(본선 기동). 텍스트 감사 종(AG01·03～09)의 codex는 기존 read-only 유지(심층 방어). agy 명령 권한은 §1 후속 결정으로 허용됨(`command(*)` — 사용자 직접 등재).
- **agy 전달·작업 규칙(실측 2026-08-03)**: 한글 프롬프트의 `-p` 인라인 전달은 인코딩 오인 유발 가능(agy가 복구용 python 실행을 시도하다 command 거부로 사망 — 실측) → **ASCII 포인터 + UTF-8 지시서 파일이 표준**. cwd는 스크래치패드 고정(cwd=저장소 시 `.gemini/` 프로젝트 부산물 생성). **헤드리스 브라우저 = command 권한 의존(이력)**: agy 브라우저 경로는 RunCommand 기반(환경 프로브 → 사전 설치 Chrome 헤드리스 기동 → CDP 제어) — command 미부여 상태에서 3회 거부됐고, 사용자의 `command(*)` 등재(§1) 후 **무플래그 자율 실행 성공 실증**(2026-08-03, 렌더·콘솔·클릭·보고서 전 항목). 운영 규칙: **AG02의 agy 호출만 `--sandbox` 없이**(브라우저 명령 필요 — 샌드박스가 CDP 포트·루프백을 차단할 위험, agy 자가확인), 그 외 종(AG01·03～09)은 `--sandbox` 유지(텍스트 감사에 명령 불요 — 심층 방어).
- **agy CDP 세션 패턴(agy 자가권고 2026-08-03 — AG02 지시서에 명시)**: Chrome 프로세스는 챕터당 **1회만 기동** → **위젯마다 새 탭(Page) 생성·검증·닫기** → 전체 순회 후 프로세스 종료. 근거: 위젯마다 프로세스 재기동 = Windows CDP 포트 바인딩 지연·EADDRINUSE 위험 / 단일 탭 URL 순회 = 전역 JS 상태 오염(직전 위젯의 리스너·전역 객체 잔존). `--mode` 미지정 유지(accept-edits는 파일 편집 승인용 — 불필요), `--print-timeout 15m` 적정(위젯 10개 기준 CDP 2～4분 예상 + 여유).
- **실패 처리**: 타임아웃·스키마 불합격 → 동일 프롬프트 1회 재시도 → 재실패 시 **결측 기록**(캠페인은 계속, 스코어카드에 반영). 결측 보고를 본선이 임의 대필하지 않는다.
- **agy 쿼터 페이싱**: 주간 컴퓨트 캡 전제 — 배치 시작 전 잔량 확인(`agy -p "/usage"` 가용 여부는 파일럿에서 확정, 불가 시 실패 감지로 대체). 소진 시 **Gemini 층만 지연**하고 나머지 3계열 계속, 결측으로 기록.
- **차단 규약**: agy 실행 스크립트에 모델명 하드코딩(목록의 claude·gpt-oss 사용 금지 — 벤더 오염 방지). **codex 웹 차단 공식 키(0.145, codex 자가확인 2026-08-03)**: `-c 'web_search="disabled"'`(현행 — `tools.web_search=false`·`features.web_search=false`는 레거시) + 보수적 동봉 `-c features.browser_use=false -c features.browser_use_external=false -c features.in_app_browser=false`. 셸 외부 네트워크는 read-only 샌드박스가 차단(실측: 샌드박스 안 curl 17ms 실패 vs 밖 200) → codex 웹 금지는 **프롬프트+설정+샌드박스 3중 잠금**.

## §5 본선 검증·최종평가 (Fable 5)

1. **검증** — merged 클러스터를 심각도 순으로 실물 대조: ① quote 실존 확인(자동) ② 채택 후보·conflict·confidence="확실"인 기술 클레임은 원문을 열어 판정 ③ AG02 "추정" 중 실행 확인이 필요한 것만 브라우저 확증. 판정 = **채택 / 기각(사유) / 쟁점(사용자 에스컬레이션)**.
2. **최종평가** — `verdict/CHnn.md`(사람이 읽는 정본):
   - **종합판정 4등급**(기준 제안 — 채택 발견 기준): `유지` = 치명·높음 0 / `경미 수정` = 치명 0·높음 ≤ 2(국소) / `보강 권장` = 높음 3+ 또는 구조 결함(AG05·08 계열) / `재집필 권장` = 치명 존재 또는 모델·서사 근본 결함. 경계 사례는 본선 재량 + 사유 명기.
   - 채택 발견 표(위치·심각도·요지) / 기각·쟁점 목록(사유) / **수정 백로그**(우선순위 — 추후 "이 챕터 보강" 지시 시 그대로 작업 지시서) / 모델 기여 한 줄 요약.
3. **수정 없음** — 어떤 등급이 나와도 이 캠페인에서는 콘텐츠를 건드리지 않는다.

## §6 모델 스코어카드

`tools/audit-matrix/scorecard.mjs`(확정 후 구현)가 raw + verdict에서 재생성 → `.archive/_generated/MODEL_SCORECARD.md`. **수치는 문서에 복창하지 않는다** — 라이브 재생성물이 정본.

| 지표 | 정의 |
|---|---|
| 발견 수 / 채택 수 / 채택률 | 모델·에이전트별. 채택 = 본선 판정 기준 |
| **고유 채택** | 그 모델만 발견하고 채택된 것 — 모델 다양성의 실증 가치 |
| 오탐률 | 기각 / 발견 |
| **앵커 실존율** | quote가 대상 파일에 실제 존재하는 비율 — 환각 지표(자동 산출) |
| 심각도 분포 | 치명·높음을 실제로 누가 잡는가 |
| 스키마 준수·결측 | 재시도 포함 유효 보고율, 결측 수 |
| 축별 강점 맵 | AGxx × 모델 교차 — 후속 캠페인의 축소·증강 근거 |

## §7 캠페인 운영

- **저장 구조**(캠페인 개시 시 날짜 고정):

```text
.archive/<개시일>-matrix-audit/
  _prompts/CHnn/AGxx.md          ← 조립 프롬프트 스냅샷(4모델 공용)
  raw/CHnn/AGxx-<model>.json     ← 발견자 원본(결측 시 부재)
  merged/CHnn.json               ← AG10 수합본
  verdict/CHnn.md                ← 본선 최종평가(정본)
tools/audit-matrix/
  preamble.md · AG01.md～AG10.md · schema.json · run-chapter.ps1 · scorecard.mjs
```

- **순서**: CH01 파일럿(§8) → go/no-go → CH02부터 배치(하루 3～5챕터 페이스) · AG07/08은 캠페인 초반 1회.
- **git**: 브랜치 `audit/matrix-<구간>`, 커밋 = 챕터 배치 단위(raw+merged+verdict+02 갱신 동일 커밋). main 직접 금지([04 R7](04_CONVENTIONS.md)).
- **02_PROGRESS**: 진행 위치·다음 배치만(포인터 = verdict 폴더·MODEL_SCORECARD).

## §8 CH01 파일럿 — 합격 기준 (제안)

AG01～06·09 × 4모델 = 28벌 + AG10 수합 1회를 CH01에 실측:

1. 스키마 유효 보고(1회 재시도 포함) **≥ 26/28** — 미달 시 하네스·프롬프트 보완 후 재파일럿.
2. 앵커 실존율 모델별 실측 보고(합격선 없음 — 기준선 채집).
3. AG10 수합 품질 — 본선 수동 스팟 10클러스터에서 병합 오류(다른 사안 합침·같은 사안 분리) ≤ 2.
4. 실측 보고: 챕터 1개 wall-clock · agy 쿼터 소모 · codex 토큰 · Claude 호출 수 → **39챕터 전량 외삽치 제시** 후 사용자 go/no-go.
